# Requirements/Domain Challenge Exposure Preflight Evidence

Date: 2026-07-24
Status: current-host exposure and public-packet boundary passed; no task turn

## Result

Codex Desktop app-server `0.145.0` exposed the same 111 Skill identities in all
three metadata profiles. The native profile disabled all 105 configurable user
Skills while preserving six system Skills. The selected profile enabled only
the exact CC `grill-with-docs` path at SHA-256 `e1078020...`.

Both ephemeral thread profiles reported `gpt-5.3-codex-spark/low`, provider
fallback disabled, approval `never`, and a read-only no-network sandbox.
Plugins, Apps, remote Plugin features, and static MCP startup entries were
disabled for the probe; zero MCP startup failures were observed.

No `turn/start` was sent and no model request or MCP tool call occurred.
Therefore this proves no loader invocation, candidate instructions reaching the
model, behavior, value, causation, product discovery, or requirements
completeness.

## Packet and mutation boundary

The native and candidate packets contain the same public task prompt. The
native packet names no selected Skill; the candidate packet pins
`grill-with-docs`. Three private sentinels and any oracle-named file are absent
from both trial roots.

The candidate file, global Codex configuration, and both fixture trees were
stable before and after the probe. Raw report bytes are not vendored; the
machine record retains both the report-file SHA-256 and the report's internal
canonical digest.

## Next gate

The technical preflight permits preparation of the first bounded pair, not
automatic credit. The shared runner must be extended without changing the
pre-registered fixture or oracle. The parent then scores the written structured
artifact and final single question independently of Agent self-report. Three
valid pairs remain required for any bounded comparison.
