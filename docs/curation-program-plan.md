# Curation Program Plan

This is the human-readable projection of the machine program authority in
`registry/curation-program-plan.json`. The objective, acceptance, verification,
and evidence mapping lives in `registry/program-acceptance-map.json`. Bounded
execution rounds live in `registry/curation-expansion-rounds.json` and follow
`registry/round-lifecycle-contract.json`.

## Program Position

`agent-skills-curated` governs reviewed Skill intake, decisions,
implementation, testing, acceptance, and release. Skills are the first terminal
MVP because they are
portable, comparatively low burden, usable by internal and external consumers,
and able to carry instructions, resources, scripts, and deterministic checks.
They are not the boundary of resource discovery or future terminal types. The
program accepts bounded read-only discovery and research inputs while retaining
all curated disposition, implementation, acceptance, and release decisions.

The authority and evidence flow is:

```text
evidenced demand and shortfall coordinates
-> native, official, runtime, curated, and installed capability baselines
-> broader resource discovery and clustered candidate signals
-> native / single / composition / non-Skill alternative comparison
-> residual-gap decision
-> reviewed Skill intake, adaptation, repository authoring, release, and lifecycle governance here
-> optional consumer-owned installation, projection, backup, restore, and live checks
-> read-only CALIBRATION research input and provenance references
-> applicable project-authority admission and final carriage
```

This repository owns the curated decision path and accepts discovery or
research material only as bounded evidence. It does not own live Agent state,
consumer configuration, discovery-source operations, or project hard-standard admission. Consumer
configuration repositories are consumption, validation, and feedback surfaces;
they are not the long-term carrier for cross-project research or standards.
The pinned corpus at
`YIYUAN-CALIBRATION/common/human-ai-collaboration-shortfalls/` is a read-only
candidate, evidence, and research input. CALIBRATION does not decide Skill or
Manager product behavior. Only after the Skills and Hooks chain shows stable
repeated value across independent sources, Agents or hosts, task classes, and
real feedback cycles may this repository consider a traceable standard
candidate. Net benefit over context, control, and maintenance cost plus a
separate authority decision are required; admission and final carriage remain
with the applicable project authority. Any cross-repository write remains a
separately authorized transaction.

## Stable Program Architecture

The program keeps six control layers distinct:

1. Strategic objectives and authority boundaries define why the system exists.
2. Stable operating lanes define the reusable decision and evidence flow.
3. Sequence gates prevent downstream work from skipping prerequisites.
4. Bounded initiatives define current result packages and decision gates.
5. Historical rounds retain what actually happened without becoming permanent
   architecture.
6. Consumer integration and any cross-repository handoff remain separately
   authorized external transactions.

This separation matters because Round 02 local runtime alignment is valid dated
consumer evidence, but it is not a mandatory terminal for every future curation
round.

## Strategic Objectives

The program currently has fifteen required core objectives and permits reviewed
extensions without weakening reference closure:

1. Operate Skills as the first terminal MVP without turning the broader funnel
   into a Skills-only system.
2. Preserve broad general and specialist coverage beyond software engineering
   and any one human-AI shortfall chain.
3. Maintain an open-world, solution-neutral human-AI collaboration mother
   framework, with software engineering as a priority lifecycle specialization
   rather than the whole domain. Historical STM/P/SG coordinates and current
   self-authored Skills remain bounded evidence, not the problem universe.
   Treat process fidelity and cumulative transformation loss as a cross-domain
   risk spanning intake, decomposition, routing, delegation, compression,
   handoff, review, acceptance, and lifecycle transitions.
   Treat accepted domain language and consequential decision records as a
   cross-lifecycle semantic authority plane. Grilling is a selective
   elicitation method, domain modeling maintains that state, and composed
   entries remain replaceable rather than becoming a mandatory universal
   workflow.
4. Evolve the approved release through governed retention, reuse, replacement,
   supersession, composition, admission, deprecation, or retirement; unchanged
   is valid when no alternative proves positive net value.
5. Bind capability work to dated demand evidence, uncertainty, held claims,
   refresh rules, and promotion firewalls.
6. Reuse reliable native, official, runtime, curated, external, composed, or
   non-Skill paths before authoring new capability.
7. Model full-chain native, single, composed, non-Skill, human-authority,
   fallback, conflict, and lifecycle coverage.
8. Produce decision-ready topology that reduces unknown-Skill enumeration,
   routing burden, collision, and maintenance rent without owning invocation.
9. Govern third-party Skill bodies through provenance, license, security,
   portability, overlap, adaptation, validation, release, and lifecycle gates.
10. Keep releases agent-neutral while requiring evidence-backed consumer
   mappings for each Agent's instruction, Skill, precedence, projection,
   verification, backup, and restore behavior.
11. Improve collaboration reliability through the smallest sufficient layered
   combination of instructions, Skills, deterministic mechanisms, project
   standards, evidence, and accountable human decisions.
12. Consume CALIBRATION research read-only and defer standard-candidate work
   until the chain-maturity gate is met; only then consider extraction while
   preserving applicable project admission and final-carrier authority,
   including a graph-scoped revalidation cascade after a standard is accepted.
13. Support update, conflict handling, supersession, deprecation, migration,
   rollback, and retirement instead of accumulating Skills indefinitely.
14. Govern source-preserving cross-Agent capability consumption while reusing
   CC Switch where supported and leaving host configuration and runtime
   authority with consumers.
15. Preserve superseded custom Manager decisions and bounded experiments as
   historical evidence without retaining them as current product identity or
   reactivation authority.

Each objective maps to stable acceptance, verification, and evidence IDs in
`registry/program-acceptance-map.json`. `verified` means the stated criterion
has checked-in evidence; it does not upgrade a partial implementation or dated
runtime observation into current live truth.

## Stable Operating Order

The reusable core path is reuse before build:

```text
evidenced demand or shortfall
-> native / official / runtime baseline
-> discovery, clustering, deduplication, and representative shortlist
-> representative deep review
-> native / single-Skill / composition / non-Skill comparison
-> residual-gap decision
-> adopt, adapt, merge, compose, reference, reject, hold, adapter-only, or repository-author
-> admission, verification, and release
```

Before that path can support a generalized coverage, residual-gap,
repository-authoring, or hard-standard claim, the demand model must pass the
solution-neutral coverage gate. The versioned baseline in
`registry/human-ai-collaboration-coverage-rebaseline-2026-07-24.json` defines
nine open-world mother-framework axes, a fourteen-slice software-engineering
specialization, an end-to-end process-fidelity cross-cut, explicit
uncovered-cell and counterexample expectations, and an AI-independent
hard-standard boundary. It preserves valid narrow evidence
without converting a closed coordinate inventory into whole-domain closure.
The first executable comparison boundary is recorded in
`registry/human-ai-collaboration-comparative-protocol-batch-01-2026-07-24.json`;
candidate-specific Matt exposure and one native/Matt weak-Agent pair are now
recorded, but loader causation and any preference decision remain unproved.
The paired observation is in
`registry/human-ai-collaboration-weak-agent-live-comparison-batch-01-2026-07-24.json`
and now contains three runs per arm with a mixed process result. Treatment
fidelity remains required before any retention or self-authored-chain
decision. The second software-lifecycle scenario has started under
`registry/human-ai-collaboration-weak-agent-live-comparison-batch-02-2026-07-24.json`.
All three native/CC `diagnose` pairs passed the functional oracle in both arms.
The native arm passed the strict process boundary zero times and `diagnose`
passed twice. A separate three-pair synthetic canary assay now proves that the
bound Codex host mechanism can deliver body-only project-Skill content.
Independent loader notification and exact installed-`diagnose` delivery remain
unproved. The bounded association therefore still does not support preference,
causation, current-upstream-value, portfolio, or self-authored-chain decisions.
Identical incident and canary repetitions stopped. The next comparison used
disposable source-pinned current-Matt and Superpowers projections under
`registry/human-ai-collaboration-weak-agent-live-comparison-batch-03-2026-07-24.json`.
Across three valid pairs, both candidates passed visible tests 3/3; full hidden
contract passed Matt 1/3 and Superpowers 2/3, while complete strict process
passed 1/3 each. One earlier pair is retained as classifier-invalid and is not
counted. The mixed result favors neither candidate and proves no loader
causation, full-Superpowers orchestration, portfolio decision, residual gap, or
self-authored-chain change. Identical repetitions stop; the next comparison
must change the falsifiable lifecycle scenario or treatment-fidelity question.
The first general research baseline is now recorded in
`registry/human-ai-collaboration-read-only-claim-live-comparison-2026-07-26.json`.
Three native Spark/low read-only repetitions satisfied the observed host
boundary but failed the fixed private claim oracle in the same way: production
readiness was treated as contradicted rather than unknown. This is a bounded
synthetic failure only. It does not prove general weak-model failure, any
candidate's value, or a residual self-authored gap; one suitable existing
source/uncertainty treatment must be tested before authoring changes.
The next programming-heavy slice is preregistered in
`registry/human-ai-collaboration-new-feature-tdd-protocol-2026-07-26.json`.
It separates native Spark/low, current remote-revision Matt `tdd`, and
OpenAI-curated Superpowers 6.2.0 `test-driven-development`. Only the exact TDD
sub-capabilities are eligible; full Matt/Superpowers workflows remain excluded.
The protocol pre-binds Matt's required public seam and makes ordered valid RED
plus mutation-sensitive Agent tests hard gates. The dependency-free fixture
builder and a seven-case normalized offline RED timeline classifier now pass.
Raw app-server event normalization, exact projections, exposure, and live runs
remain open.
The bounded successor is now preregistered in
`registry/human-ai-collaboration-tdd-noncomparative-dispatch-successor-contract-v2-2026-07-27.json`
and documented by
`docs/strategy/HUMAN-AI-COLLABORATION-TDD-NONCOMPARATIVE-DISPATCH-SUCCESSOR-CONTRACT-V2-2026-07-27.md`.
Its pure offline builder and exact contract/document bindings remain `NO-GO`
for live effects. A future authorized slice must establish one shared ledger
authority before snapshot and preflight capture, then receive a separate
independent grant. This does not authorize candidate materialization,
app-server start, model dispatch, formal acceptance credit, or portfolio
change.
The next scenario was pre-registered in
`registry/human-ai-collaboration-maintenance-migration-protocol-batch-01-2026-07-24.json`.
It uses a synthetic affected-consumer model and a configurable CC
`deprecation-and-migration` candidate. Three valid Spark/low pairs are now
recorded in
`registry/human-ai-collaboration-maintenance-migration-live-comparison-batch-01-2026-07-24.json`:
visible tests passed 3/3 in both arms, while the hidden contract passed native
3/3 and the candidate 1/3. This supports only a bounded native association on
the fixture, not loader causation, a general preference, or portfolio action.
Identical repetitions stop. The security slice is deferred because the
suitable OpenAI proprietary system Skill currently lacks a clean disabled
control and must not be vendored.

The next materially different slice is pre-registered in
`registry/human-ai-collaboration-requirements-domain-challenge-protocol-batch-01-2026-07-24.json`.
It tests source-bound requirements and domain-language challenge with
Spark/low native-disabled versus the exact CC `grill-with-docs` payload. The
fixture, wording-neutral oracle, and five offline classifier cases now pass;
the exact no-turn native-disabled and selected-candidate host exposure also
passes with Plugin and static MCP startup entries disabled. Live runs remain
gated. This selection does not add the self-authored chain as a
requirements-method arm and does not change any hard standard.

The later
[semantic-authority layer reconciliation](strategy/HUMAN-AI-COLLABORATION-SEMANTIC-AUTHORITY-LAYER-RECONCILIATION-2026-07-28.md)
confirms that current Matt upstream now separates `grilling`,
`domain-modeling`, and the thin `grill-with-docs` composition entry. The exact
CC treatment remains the historical adapted monolith and lacks the separate
current-wrapper primitives. The program therefore adds persistent semantic
authority continuity as an open acceptance subgate while preserving the exact
current requirements protocol. No silent update, dependency installation,
replacement, portfolio preference, or self-authored residual-gap claim follows.

This display is a dependency graph projection, not a one-way conveyor. The
eight lines above are the core path. Consumer projection is an optional branch
from an approved release. Lifecycle metabolism is cross-cutting: governed
candidate, release, upstream, community, validation, or consumer evidence can
trigger it without requiring a consumer installation. Standard extraction is
a conditional branch from repeated evidence and is not the universal terminal
of every Skill round.

Native and runtime baselines may run in parallel by bounded host context;
candidate metadata triage may run in parallel by cluster; and independent,
source-pinned representative reviews may run in parallel before joining at the
alternative-comparison gate. A third-party deep review requires an exact source
pin plus license and provenance boundaries. Inspection never authorizes code
execution.

Host-native child tasks or Agents may provide independent observations for
fresh-context recovery, paired tests, weak-model floor probes, and parallel
read-only review. Their results join at a parent-owned reconciliation gate and
are not automatic truth, acceptance, or completion evidence. Shared-checkout
tasks are read-only by default; concurrent writes require a verified isolation
mechanism plus explicit branch and cleanup ownership.

Discovery is bounded and consumer- or repository-owned. Retired radar output
has no privileged path. Every signal becomes decision input only after this
repository binds demand, baseline, source trust, review scope, and acceptance.
Feedback, new evidence,
an upstream update, a security or license change, a consumer failure, or a
project-standard conflict can return work to the affected upstream lane.

No repository-authored gap-fill Skill can begin from discovery or from an old
draft alone. It first requires a supported residual gap after alternative
comparison, then design provenance, license ownership, security, portability,
overlap, tests, and owner approval. This is a candidate-origin rule inside the
same non-executable candidate state; it does not create a shortcut around
curated admission.

Consumer projection readiness and local alignment are optional consumer-owned
actions, not mandatory stable program lanes.
They require a pinned release, read-only inventory, owned-path classification,
backup, rollback, explicit local-write authority, and fresh post-action evidence.
This repository may retain dated evidence without claiming it owns or currently
controls the consumer environment.

## Historical Execution Projection And Current Initiatives

The six existing `program-01` through `program-06` records are retained as the
historical execution projection for Rounds 01 and 02. They do not define the
complete stable architecture above.

Round 02 is not active source-intake execution anymore. The repository contains
evidence for candidate review, admission decisions, approved payload and routing,
and a bounded local runtime sync transaction:

- `registry/round02-release-admission-candidate-review.json`
- `registry/round02-approved-payload-routing-proposal.json`
- `registry/round02-local-runtime-sync-execution.json`

Those records prove that the bounded actions occurred on their recorded dates.
They do not prove current live parity. A later distinct Round 02 stage-closeout
reconciliation was accepted and closed the bounded round without upgrading the
dated runtime evidence into current live truth.

The program-control completeness initiative was accepted by the owner and
merged locally into `main`; the bounded event is recorded in
`registry/program-control-acceptance-event-2026-07-15.json`. The owner also
accepted the requirement-by-requirement Round 02 review with bounded outcome
`complete`; that decision is recorded in
`registry/round02-stage-closeout-acceptance-event-2026-07-15.json`. Round 02 is
closed, while its disclosed residual risks and deferred work remain active.

Round 03 rebaseline has been owner-accepted and the round is active for the
bounded read-only capability survey. Its former planned
adaptation-and-admission contract overlaps work already completed through
separately approved Round 02 follow-on gates. The machine-bound current
initiative is `initiative.capability-survey-gap-proof`. The accepted rebaseline
review package is `registry/round03-capability-survey-rebaseline.json`, and its
decision is recorded in
`registry/round03-capability-survey-rebaseline-acceptance-event-2026-07-15.json`.
Its demand inputs are pinned separately by
`registry/round03-demand-coordinate-source-contract.json`; the source contract
does not activate discovery or promote the consumer research into a standard.

The capability-survey and residual-gap initiative is active. Demand records and
a dated native/runtime baseline must be verified before bounded public candidate
metadata discovery begins. The survey must produce all ten declared result components before any adoption,
adaptation, repository authoring, or standards-extraction decision.

The 2026-07-18 broad public discovery and balanced structure preflight are
recorded in `registry/public-skill-source-discovery-preflight-2026-07-18.json`.
The user-provided public GitHub star list is a supplemental, non-exclusive
discovery signal in
`registry/user-starred-skill-source-list-intake-2026-07-18.json`; membership
raises review priority but proves no quality, safety, or admission state. The
first five-source, source-pinned, non-executing static review is recorded in
`registry/public-skill-source-static-review-batch-2026-07-18.json`. It approves
no source or component and left only the `loopy` body as a
candidate-with-limits for demand-level comparison. That comparison is
recorded in
`registry/loopy-demand-level-alternative-comparison-2026-07-18.json`: the only
distinctive value hypothesis is general one-shot-versus-loop qualification plus
a bounded execution receipt, while full-body superiority, residual gap, and
admission remained unproven. The next gate was fixture-only paired behavior
evidence, not live installation, catalog, save, publication, Hook, or consumer
mutation. That deterministic gate is recorded in
`registry/loopy-contract-fixture-protocol-2026-07-18.json`: 18/18 contract
fixtures passed and isolated one ordinary reversible iterative scenario. The
owner then separately authorized the exact-pinned body for fresh disposable
Agent tasks. The completed 12-run result is recorded in
`registry/loopy-disposable-agent-trial-result-2026-07-18.json`: all runs were
correct and boundary-preserving, but Loopy showed no material benefit over both
native and current-chain baselines at proportionate context cost. The body is
therefore reference-only and not admitted; the one-time authorization is
consumed and grants no further candidate execution.

The five sources contributed by the public star list but absent from the broad
capture are preflighted in
`registry/user-starred-new-source-preflight-2026-07-18.json`. The index remains
discovery-only; Huashu and PM Skills reuse historical component reviews but
require current-revision delta review; Karpathy guidance remains a
license-incomplete overlapping reference; and `vercel-labs/skills` remains an
external tooling/path-map baseline rather than a Skill admission candidate.
This narrows the next marginal batch without treating list membership, suite
breadth, or management tooling as capability quality.

The pinned index has also been parsed into exact child-source coordinates in
`registry/user-starred-index-child-source-extraction-2026-07-18.json`. It
contains 20 direct-install sources, of which 16 were new relative to the broad
capture. The read-only structural preflight resolves 14 and preserves two 404
coordinates as stale in
`registry/user-starred-index-stale-source-resolution-2026-07-18.json`; no
possible successor is substituted automatically. The current PM Skills
revision delta is separately closed in
`registry/pm-skills-current-revision-delta-review-2026-07-18.json`: two Skill
bodies changed by five lines, so prior suite decomposition remains reusable,
while larger host-command changes stay outside the Skill-body decision.

The 14 available child sources are now classified in
`registry/user-starred-index-child-source-classification-2026-07-18.json`
across domain design/frontend/media, operational tooling, broad suites, and
external API/account ecosystems. None is selected for a new deep-review batch
because the current coordinate envelope has no supported residual gap. This
verifies the bounded discovery stop rule on marginal yield while explicitly
leaving ecosystem completeness, future trigger-driven discovery, body quality,
and runtime effectiveness open.

Lifecycle metabolism is reconciled in
`registry/lifecycle-metabolism-reconciliation-2026-07-18.json`. An observed
consumer cycle returned verified results to candidate disposition and exact
reproposal suppression, while the policy binds consumer, community, security,
license or provenance, upstream-refresh, and validation triggers. The feedback
return acceptance is verified at repository-governance scope. Approved-Skill
deprecation, migration, rollback, and retirement are verified at repository
policy and deterministic-fixture scope; a live approved-Skill consumer event
is still required before claiming observed runtime maturity or cross-consumer
effectiveness.

Final program closeout now has a distinct cleanup acceptance gate rather than
being inferred from a green stage or the latest PoC. Before any declared final
closeout, the program must rebuild an exact-target inventory across bound
repositories, workspaces, branches, worktrees, temporary downloads, backups,
generated trial artifacts, consumer or tool state, and retained historical
evidence; bind ownership, recoverability, sensitivity, and disposition; retain
authoritative or ambiguously owned evidence by default; obtain separate
authority for cleanup mutations; and reverify the post-cleanup repository and
evidence state. The current
`registry/closeout-cleanup-debt-preview-2026-07-24.json` is only a partial,
repository-local read-only preview and authorizes no deletion.

Cross-Agent claim limits are reconciled in
`registry/cross-agent-claim-limit-reconciliation-2026-07-18.json`. Nine current
evidence classes are ledgered by host, model, reasoning, loader, activation,
permission, workspace, date, evidence class, counterexample, and recheck
trigger. This verifies the claim-governance firewall without upgrading
cross-Agent parity, current consumer mapping, implicit activation, loader
precedence, Hook effectiveness, or production behavior.

Consumer-mapping evidence is reconciled separately in
`registry/consumer-mapping-evidence-gap-reconciliation-2026-07-18.json`.
Codex now has current-static-partial evidence pinned to consumer repository
HEAD `0c93458d48cb1ebaa6d0d289e3a21f46d2f61f65`, including five passing
disposable install, verify, retirement, and rollback tests; Claude Code is a
conceptual chain example only. A subsequent read-only shared-root audit found
73 Skill directories: all 19 bodies claimed by the old curated transaction
have drifted, a separate schema-3 lock declares 27 non-overlapping Lark Skills,
and 27 directories remain foreign or unknown to those two ledgers. The old
routing index and backup still match the old transaction. This is ownership
and drift evidence, not overwrite or rollback authority. Dirty consumer files
were excluded, and no external repository, Skill body, transaction, Hook, or
live Agent state was written. Loader precedence, implicit activation, current
source ownership for the 19 drifted claims, installed release parity, and live
restore remain open. The acceptance remains partial and non-vacuous until the
ownership conflict is reconciled and at least one consumer-side task verifies
all eight mapping dimensions at the required live scope.

User sovereignty and foreign-state coexistence are reconciled in
`registry/user-sovereignty-and-foreign-coexistence-reconciliation-2026-07-18.json`.
Repository governance plus historical disposable-root transaction evidence and
a disposable Codex-shaped read-only preview verify foreign-by-default
ownership, explicit previewable reversible transitions, and the no-account or
telemetry boundary. Multi-Agent operational coexistence remains partial until
consumer-owned multi-host conversion, drift, collision, backup, rollback, and
sentinel-preservation evidence exists.

The current ten-component survey package is
`registry/round03-capability-survey-result-package-2026-07-18.json`. It carries
all 62 STM/P/SG coordinate IDs are now mapped through eight source-supported
demand records; none remain unassessed, while demand-model and longitudinal
evidence closure remain explicitly open. The added
intent-binding lane is recorded in
`registry/round03-intent-binding-demand-review-2026-07-18.json` and currently
routes through native, current-chain, approved curated, recipe, runtime,
fallback, and human-authority alternatives without supporting a residual gap.
The added authority-boundary lane is recorded in
`registry/round03-authority-boundary-demand-review-2026-07-18.json`; it keeps
advisory guidance, host runtime enforcement, and accountable authority separate
and likewise supports no residual Skill or Hook gap.
The added premise-challenge lane keeps native and open-divergent fast paths,
uses document grilling only as a narrow opt-in, and supports no residual gap.
The final cognitive-offload and monitoring lane separates immediate assistance,
longitudinal human capability, live operations evidence, maintainer learning,
and anti-accretion; it supports no residual Skill gap.
The bounded coordinate corpus is now decision-ready: every row has a demand
binding, disposition, verification method, limitation, and recheck trigger.
The reconciliation is recorded in
`registry/round03-complete-coordinate-envelope-reconciliation-2026-07-18.json`.
The demand-coordinate contract itself is reconciled separately in
`registry/demand-coordinate-contract-reconciliation-2026-07-18.json`: all
eight demand records now carry explicit evidence state, held claims,
uncertainty, affected subjects, and recheck triggers, so the bounded contract
is verified. Demand extraction, whole-demand-model exhaustiveness, survey
closure, and ecosystem completeness remain explicitly open.
The native/runtime baseline remains partial. Its gap reconciliation is
`registry/native-runtime-baseline-evidence-gap-reconciliation-2026-07-18.json`:
the dated single-host metadata artifact covers its original four-record batch,
while EL-05 through EL-08 have alternative reviews but no per-demand,
dimension-complete live baseline or raw probe outcomes. The historical
"every demand record" field is therefore batch-scoped, not current-corpus or
cross-host evidence.
Residual-gap proof also remains partial for a different reason, recorded in
`registry/residual-gap-proof-evidence-gap-reconciliation-2026-07-18.json`.
The rejection firewall is exercised across the bounded survey and the Loopy
trial, but no supported residual gap has exercised the complete positive proof
path. Zero supported gaps cannot vacuously prove that path, and open runtime,
research, or governance evidence classes remain explicitly non-Skill gaps.
This is not whole-demand-model or ecosystem closure. It proves no supported
residual Skill gap in the bounded envelope and authorizes no candidate,
repository-authored Skill or Hook, hard standard, runtime change, cross-repo
write, commit, or push. The authorized Loopy follow-up is now resolved
reference-only. The operating mode is now evidence-triggered monitoring and
recheck while demand-model, longitudinal, production, and cross-host evidence
limits remain open; closed lanes are rechecked only after their recorded
triggers.

The repository-side decision projection now has a bounded structural and
fixture evaluation at
`registry/decision-ready-consumer-projection-evaluation-2026-07-18.json` with
its human projection at
`docs/decision-ready-consumer-projection-evaluation-2026-07-18.md`. It records
105 passing routing scenarios, all seven decision classes, fail-closed conflict
handling, digest-bound maintenance, and a structural enumeration proxy. The
acceptance remains partial because measured consumer benefit and consumer-owned
verification require a separately authorized consumer task.

## CC Switch Reuse And Custom Manager Retirement

The current owner-accepted strategy is recorded in
`registry/cc-switch-source-preserving-skill-pool-strategy-acceptance-event-2026-07-17.json`
with its human projection at
`docs/cc-switch-source-preserving-skill-pool-strategy-2026-07-17.md`.

CC Switch owns operational Skill source registration, installation, update
detection, supported-Agent distribution, backup, and restore. This repository
does not duplicate those functions. It owns upstream source evidence, license
and executable-surface preflight, safety, quality, superiority, overlap,
redundancy, disposition, shortfall mapping, and the residual-gap decision.

The current live state is reconciled separately in
`registry/cc-switch-live-source-ownership-reconciliation-2026-07-18.json`.
Read-only inspection confirms CC Switch distribution through 43 shared-root
targets, but 42 are local database rows, none are source-backed, and one lacks
a database row. The 19 legacy curated targets exactly match this repository's
current adapted release. Therefore the reuse strategy remains accepted while
`acceptance.cc-switch-source-preserving-skill-pool` is partial until per-Skill
source-backed migration dispositions and later authorized migration evidence
exist.

The first per-Skill review is recorded in
`registry/legacy-curated-skill-source-migration-review-2026-07-18.json`.
It classifies 16 of the 19 legacy derivatives as provisional candidates for
reviewed upstream-exact replacement and three as provisional retirement or
supersession candidates. It does not authorize a live migration.

Third-party Skill bodies remain unchanged by default so their source identity
and CC Switch update path stay intact. Candidates are downloaded only into a
non-active pool after metadata, license, provenance, and executable-surface
preflight. Download does not authorize execution or installation. A candidate
that is unsafe, inferior, redundant, or incompatible is rejected or replaced;
adaptation requires a separate supported exception. Repository authoring is the
last resort after a reproducible human-AI collaboration residual gap.

The narrow collaboration chain remains Agent native capability, scoped
instructions or rules, Skills, optional Hook, then verification, feedback, and
rollback. Physical directories belong to Agent-specific adapter documentation
rather than product architecture. Shared Skills, the portable chain, Hook
policy, and host-profile contracts remain Agent-neutral authority here; they
must not be consolidated wholesale into `codex-user-config`. Each consumer
configuration owns only its Agent-specific instruction carrier, installation,
runtime paths, Hook deployment, verification, and rollback adapter.

The layered-reliability and project-standard precedence projection is now
verified at its declared governance-only scope. The machine reconciliation is
`registry/layered-reliability-projection-reconciliation-2026-07-18.json` and
the human projection is
`docs/layered-reliability-projection-reconciliation-2026-07-18.md`. This does
not upgrade consumer mappings, cross-host behavior, Hook modes, standard
candidate maturity, or operational standard migration evidence.

The owner-accepted adaptive Harness, source-suite selection, and user-sovereignty
contract is recorded in
`registry/adaptive-harness-source-suite-and-user-sovereignty-acceptance-event-2026-07-18.json`
with its human projection at
`docs/adaptive-harness-source-suite-and-user-sovereignty-2026-07-18.md`.

Admission is strict while consumption remains free. A source suite is not a
single automatic approval unit: source trust, suite atomicity, component quality,
and cross-source capability comparison are reviewed separately. Selected
components remain unchanged, bundled Hooks require independent admission, and
updates trigger bounded re-review rather than automatic trust. Users may add,
remove, combine, fork, or modify Skills; modified derivatives do not inherit the
curated digest or verification without separate review.

The Harness uses open, assist, and guarded postures. Native reasoning, no Skill,
no Hook, and no additional structure remain valid. Structure is advisory for
open or reversible work and mandatory only where safety, authority, privacy,
external writes, destructive actions, publication, release, or comparable impact
requires a hard boundary. Every added constraint needs reproducible failure and
net-benefit evidence, must be reversible, and must be relaxed or retired when it
becomes unnecessary or counterproductive.

Any future residual-gap repository-authored Skill or Hook must dynamically and
explainably select among native/no-Skill, open, advisory assist, structured
assist, guarded, human-confirmation, and safe-fallback outcomes using task,
risk, permission, host-capability, user-posture, and feedback facts. Dynamic
adaptation cannot silently rewrite code or policy, expand permissions, or
auto-publish repository changes; host-specific behavior requires an explicit
profile and safe degradation.

These modes, fields, and review checks are current working hypotheses and
observation protocols, not hard standards. Standards extraction is deferred
until stable value repeats across independent sources, Agents or hosts, task
classes, and real feedback cycles, with net benefit over context, control, and
maintenance cost. Any later hard-standard proposal requires separate authority
and adoption review.

The current strategic objective is source-preserving cross-Agent capability
governance, not custom Manager production. The exact identity migration and
reactivation boundary are recorded in
`registry/custom-manager-retirement-reconciliation-2026-07-18.json` with the
human projection at
`docs/custom-manager-retirement-reconciliation-2026-07-18.md`.

The separate custom Manager direction is superseded. Its written design,
topology package, Rust foundation, and disposable Codex preview remain dated
historical evidence. No further Manager implementation is authorized. After
this governance rebaseline passed the full verification set, the exact local
repository path was deleted on 2026-07-17; the pre-delete audit recorded zero
commits, zero remotes, and zero tracked files, and the post-delete check returned
absent. Deletion does not erase the preserved historical records or convert
them into current product authority.

## Verification And Closeout

The global deterministic set remains:

```text
python -B scripts/verify.py
python -B scripts/build_release_manifest.py --check
python -B scripts/build_topology.py --check
python -B scripts/simulate_routing.py --all
python -B -m unittest discover -s tests -v
```

Passing these commands proves only the contracts they actually cover. Program
closeout additionally requires the mapped acceptance evidence and an honest
closeout outcome. A dated local sync execution record must never be treated as
current live Agent availability or as global completion.

## 2026-07-18 Runtime-Control And Disposable-Manager Checkpoint

CC Switch v3.17.0 source registration, enablement-state persistence,
same-directory collision rejection, selective projection, backup, restore,
and migration-snapshot behavior now have disposable-root evidence. The stock
Windows `skill_sync` suite was not fully isolated because the Skill service
bypassed `CC_SWITCH_TEST_HOME` in five path groups; a temporary extracted-source
diagnostic correction made all seven tests pass. This verifies the bounded
contract, not a real CC Switch migration, source-backed network update, or live
rollback. The source-preserving acceptance therefore remains partial.

A subsequent loopback archive fixture verified source-backed v1 installation,
v2 update detection, pre-update backup, successful replacement, and manual v1
recovery. Failure injection also confirmed that `update_skill` is not atomic:
it ignores backup errors, deletes the old SSOT before copying the replacement,
and can leave a missing SSOT with a stale database row. The first fixture run
exposed a second Windows isolation defect. Tests run before the correction wrote
two test Skill rows, one test source row, one `demo` SSOT, and three fixture
backups to the real CC Switch home. Two exact identity-checked cleanup
transactions, each preceded by a consistent database copy, returned the live
database to 248 Skill rows and five source rows with no test payload or
projection. A second run under corrected test-home precedence created no new
live residue. The first real canary must therefore be one Skill, non-automatic,
pre-snapshotted, separately authorized, and recovery-rehearsed.

The decision-ready read-only canary package selects `handoff` after comparing 16
active exact-source replacement candidates. It has no executable surface and the
smallest reviewed semantic delta: invocation becomes explicitly manual, `PRDs`
becomes the more general `specs`, and OpenAI display metadata is added. Exact
live and target tree/file hashes, the existing symbolic-link projections, the
stale local database hash, the same-directory collision, acceptance checks, and
rollback steps are recorded. CC Switch stores only a moving `main` branch and
does not persist the reviewed commit, so the external pin must be rechecked
immediately before a separately authorized mutation. No live canary is yet
authorized in the preview artifact.

The subsequently authorized live canary is now locally verified. CC Switch's
own source download, uninstall backup, install, projection, and update-check
paths converted `local:handoff` into
`mattpocock/skills:skills/productivity/handoff`. The first attempt rejected a
Windows-checkout versus GitHub-archive line-ending hash mismatch and restored
the local state; the second used the exact archive-byte hash without weakening
the semantic or security gate. The Skill count stayed at 248, the source count
moved from five to six, 247 unrelated Skill rows and 14 other database tables
remained unchanged, and `handoff` reports no update. Twenty separate
`larksuite/cli` update signals were observed but not executed. The owner later
authorized normal CC Switch WebDAV synchronization. CC Switch is running, the
source-backed `handoff` state remains intact, and the observed runtime-only
delta removed `fetch`, `sequential-thinking`, and `time` MCP rows while logs
advanced. Cross-device equality and fresh-session invocation remain open.

Directory consistency means one explicit operational SSOT plus governed
projections, not deletion or forced merging of every same-named directory.
Native/runtime roots, plugin caches, project-local Skills, user-added Skills,
and consumer-specific directories must be inventoried with authority,
precedence, source, collision, update, and rollback status. Every selected
third-party Skill must be source-backed and update-checkable. Security, quality,
superiority, overlap, redundancy, naming, and consumer-impact review precedes
adoption or update.

Residual-gap authoring starts only after mapped human-AI collaboration and
software-engineering lifecycle demand cannot be covered by native, official,
runtime, or reviewed source-pool combinations. The existing `intent-contract`,
`capability-router`, `closure-contract`, AGENTS carriers, and Hooks are not
presumed permanent: evidence may justify retaining, replacing, relocating,
composing, or retiring them. `agent-skills-curated` remains the portable
authority; consumer configuration repositories should consume a pinned one-way
projection wherever possible and add only host-specific adapters. Cross-repo
writes remain separate authorized transactions.

Codex currently exposes native startup controls for standalone and plugin MCP
servers and for per-server tool filtering. Mid-session hot switching, unloading
an already-running server, one complete CLI control plane for desktop/runtime/
plugin-injected MCPs, and exact context/compaction telemetry remain unproven.
No repository-authored Skill or Hook is justified until disposable evidence
shows a residual gap after native control and supplies safe actuation,
verification, and rollback.

Instruction carriers are intentionally split by authority. This repository
owns the product-neutral portable contract. A private `codex-user-config`
version may later own personal Codex paths, preferences, and live wiring under
separate cross-repository authority. A public consumer repository must not be a
third manually maintained full authority copy; if public reuse later proves
valuable, it may receive only a sanitized generated non-authoritative
projection with explicit provenance. No cross-repository write is authorized
by this checkpoint.

## Unknown-quadrant research mapping

The
[unknown-quadrant process-fidelity mapping](strategy/HUMAN-AI-COLLABORATION-UNKNOWN-QUADRANT-PROCESS-FIDELITY-MAPPING-2026-07-27.md)
adds Known Knowns, Known Unknowns, Unknown Knowns, and Unknown Unknowns as a
research and task-framing lens across intake, implementation, review, handoff,
and process-loss measurement. It does not become a hard standard, globally
mandatory workflow, or new Skill. Hard standards remain mandatory common
controls; Matt, Superpowers, and self-authored phase controls remain separately
attributable method candidates or controls. Zero-model fixtures remain first,
and any weak-Agent dispatch still requires its own exact current-task grant.

The
[unknown-quadrant attribution-oracle PoC](strategy/HUMAN-AI-COLLABORATION-UNKNOWN-QUADRANT-ATTRIBUTION-ORACLE-POC-EVIDENCE-2026-07-27.md)
adds 22 deterministic zero-model classifications without rewriting the
historical overlap fixtures. It rejects hard-standard credit, multi-method
confounding, phase-control promotion, infrastructure credit, terminal
correctness as a process-loss rescue, and residual-gap self-authorization.
Passing the oracle makes a packet eligible for further compatibility testing;
it does not prove live behavior or candidate value.

The
[unknown-quadrant packet-overlay PoC](strategy/HUMAN-AI-COLLABORATION-UNKNOWN-QUADRANT-PACKET-OVERLAY-POC-EVIDENCE-2026-07-27.md)
then binds the oracle privately to five concrete zero-model packets. Four reuse
existing executable packets; the Unknown Knowns creative row required one
derived packet because the prior matrix contained only a planned scenario.
Five overlay faults fail closed. This closes a fixture-coverage gap only and
changes neither public prompts, hard standards, live authority, nor the
residual-gap decision.

The
[parent-oracle seam reuse decision](strategy/HUMAN-AI-COLLABORATION-UNKNOWN-QUADRANT-PARENT-ORACLE-SEAM-REUSE-DECISION-2026-07-27.md)
finds that existing weak-Agent, process-fidelity, and context packet builders
already separate Agent-visible inputs from parent-only oracle material. A new
generic adapter or runtime override is therefore not justified. Future live
integration must remain protocol-owned and scenario-specific.
