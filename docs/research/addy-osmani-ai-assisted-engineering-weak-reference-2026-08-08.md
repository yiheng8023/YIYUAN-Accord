# Addy Osmani AI-Assisted Engineering Weak Reference

## Disposition

The user-supplied X post was traced to Addy Osmani's 2026-01-04 article,
[My LLM coding workflow going into 2026](https://addyosmani.com/blog/ai-coding-workflow/).
The article is retained as a `PRACTITIONER-REFERENCE` with empirical strength
`ES0`, normative authority `NA0`, bounded-analogy applicability, and held
adoption state. It is a weak reference, not primary empirical evidence, an
open standard, or a standard-admission input.

The X post is a secondary Chinese rendering. The original article is publicly
readable, but no declared open license was observed on the page. The repository
therefore records the locator and bounded assessment without copying or
vendoring the article body.

## Bounded relevance

The practitioner's workflow reinforces the existing direction that faster code
generation does not remove the need for intent clarity, bounded decomposition,
context, testing, review, accountable ownership, traceability, or rollback.
These are candidate-invariant hypotheses only. The article does not provide
repeated, independent, cross-host, lifecycle-wide, or controlled evidence that
would promote them into a hard standard.

Its concrete workflow choices remain adaptive examples: `spec.md` and
`plan.md`, small implementation chunks, granular commits, branches or
worktrees, project rule files, second-model review, MCP-assisted runtime
inspection, and CI/CD feedback loops. They are not a mandatory Harness
workflow.

The source is relevant to process loss because it emphasizes alignment and
intermediate feedback. It does not define or measure per-hop semantic or
authority loss, cumulative loss, recovery, downstream impact, or human review
burden. It therefore cannot advance `acceptance.end-to-end-process-fidelity`.

## Repository impact

No README, program-order, acceptance, or goal-mode prompt change is required.
No acceptance evidence identifier is created, no standard candidate is
created, and the 46 verified / 15 partial / 0 planned inventory remains
unchanged.

CI/CD is only one adaptive feedback carrier. Local deterministic checks and
host-native verification remain valid primary surfaces. GitHub Actions is
neither required nor sufficient as the sole acceptance authority.

This record authorizes no source-body vendoring, acceptance mutation,
hard-standard admission, Skill or Hook mutation, model dispatch, external
capability mutation, cross-repository write, or release.
