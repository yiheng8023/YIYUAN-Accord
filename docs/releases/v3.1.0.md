# YIYUAN Accord v3.1.0 — publication deferred, historical development baseline

This is not an active release candidate. Exact revision
`ae7294652761abceb753f0571ee82c7ddeae06af` is retained as an unreleased,
historically verified development baseline. A later accountable consensus
reopened the product at the complete self-bootstrapping boundary, so its prior
candidate-ready status and exact-SHA checks do not qualify the new program.
The immutable public v3.0.1 release remains the current recommended version.
Bounded P0-P4 logic feasibility, semantic, isolation, evaluation-design and
product-form work is now complete. P4 admits only a deterministic,
side-effect-free plain-data reference core; its one policy-driven interface is
implemented. Exact checkpoint `553f5a97e08390117e877e7b913c7a501018bfa5`
retains failed GT-14 through GT-16 attempts: fixture execution and cleanup are
observed, but required composition, wheel/provenance and full-vector source
facts are missing. Exact revision `84447a7a1b9557e22ef5585d159459e8701fa40e`
then supplied finite former-contract GT-14 through GT-16 evidence. Fresh
current-contract GT-14 through GT-16 at exact
`1bbcc9542c92674dc0b5adcb032d6f9b01248531` now pass only one exact
environment admission, one primary-source no-dependency feasibility decision
and one disposable pure-data minimum-form bootstrap; failures and costs remain
counterevidence, and no live mechanism or release claim follows. The later
`f0ed9ce715afdbc5d9eb75e08225e9a1e46c554c` GT-17/18 attempt and its
`08eb72a57cdb4b0d27de1df16ebeccccd1e04f9e` former-verifier acceptance
were rejected after independent review found GT-17 cleanup unbound and GT-18
missing per-episode immutable full-vector and explicit carrier source/target-
state evidence. It remains counterevidence while the strengthened GT-17/18
contract fails closed on those omissions. At exact revision
`7d7a7e57b7eea02afcda21880d2f018cbc7dda0c`, a fresh source-complete
replay passes GT-17 only for the bounded evidence correction and GT-18 only for
one synthetic four-episode full-vector sequence with actual carrier states,
rollback, replacement, retirement and zero task resources. Live mechanisms,
durable evolution and representative value remain gated. The source-bound
whole-system balance audit and affected-surface deterministic reacceptance are
complete. At exact revision `2460adcff02bd56144f8d3f647ecef27cd5fefd0`,
fresh current-contract GT-18 passes only one synthetic four-episode immutable-
vector sequence with rejected proxy regression, rollback, bounded native-no-add
retention, later invalidation and three exact disposable carrier edges. Fresh
GT-19 passes only one Codex-local sense-environment responsibility-allocation
sequence: declaration and sparse incidence do not retire; an observed same-
responsibility native successor permits only reversible scoped retirement; and
evidence expiry restores the Accord allocation. Their blocked arm, transcription
and query corrections, initial failed GT-19 review and high evaluator costs
remain counterevidence. A genuinely new independent full-system review remains
active. Any future candidate and semantic version will be chosen only
after model, isolation, evaluation, product-form, implementation,
representative and longitudinal validation, a source-bound whole-system
balance self-audit, minimum justified remediation, affected-surface
reacceptance and independent review; publication remains the last phase. The
self-audit first binds a versioned claim-scoped baseline, target and
counterfactual; it cannot average away a hard failure or substitute for
independent review.

The whole-system audit was refreshed again from exact cut `59a340f` after
independent review found that generic longitudinal shape checks still admitted
meaning-changing GT-18/19 mutations and malformed cleanup could take an
exception path. The minimum repair adds role-level semantic validation,
recomputes GT-19 closure decisions, fails closed on cleanup type and corrects
stale replay, exact release-task and burden wording. It does not add a Runtime,
durable state or another product form. The repaired tree still requires a new
independent review and is not an exact candidate.

Exact cut `7860fee` then passed the independent specification mapping but
failed implementation and standards review: GT-07 carrier meaning could still
be self-attested, GT-18/19 semantics overfit fixed route names and failure
sets, malformed carriers could raise, and `sequenceSha256` was not bound to a
recomputable projection. The next repair validates GT-07 inheritance and
destination-before-release relations, dispatches known longitudinal task kinds
with unknown kinds failing closed, accepts semantically irrelevant extra routes
and varying regression dimensions, type-narrows carriers, binds event revision,
and hashes the canonical whole event excluding the digest field itself. It
preserves the same finite behavior facts and does not replay the expensive
GT-07 fixture. Capacity, summarization and handoff remain non-exhaustive risk
examples; host vocabulary is normalized before capability equivalence. A fresh
exact independent re-review is still required.

Exact `fcefee4` passed that specification re-review but failed standards review
on three residual counterexamples: GT-07 still trusted contradictory narrative
text and a superset of the minimum transfer, GT-18 accepted a human-burden-only
failure outside its declared adversarial regression, and GT-19 did not bind all
episodes to one outcome family. The next repair normalizes the already retained
GT-07 independent narratives into canonical typed facts while preserving their
source hashes, cross-binds the exact minimum fields, derives GT-18 regression
eligibility from the task contract and locks GT-19 to the first outcome ID. No
behavior was replayed or newly claimed. Goal mode remains paused; another fresh
exact independent specification and standards re-review is required.

The audit later exposed a missing dynamic-retirement seam at exact baseline
`f898ebd6c14580fb9236cd01440b88314b3c2fab`: native no-op selection existed,
but a non-selected redundant Accord route had no responsibility-scoped,
evidence-bound retirement result. The working tree now carries a pure-data
contract and deterministic regression for same-responsibility successor
coverage, prior independent consequence and cleanup, reversible observation,
independent retirement post-state and recheck triggers. At exact `fd4b99a`,
revised GT-19 passes only one finite source-bound Codex-local four-episode
host-drift sequence; its failed routes, token cost and absence of live field
control remain counterevidence. No live host mechanism, Runtime, durable store
or global uninstall was added.

The first BAL-04 paired repository-Skill/no-Accord correction task closed
equally in both arms with zero human action and residue. It therefore selects
no added form for that condition and does not establish incremental Accord
value. Reviewing the exact treatment exposed and corrected a scalar route rule
that conflicted with the current hard-admission, lifecycle-Pareto semantics.
Prior behavior evidence remains bound to its exact Skill hashes. Current local
behavior evidence remains finite and claim-scoped; the Codex and Claude package
projections are not an exact release candidate and cross-host behavior remains
unverified.

The candidate reopens the product at the collaboration-model boundary. It does
not define Accord as a capability set difference. Finite autonomy derives the
smallest relevant graph across outcome obligations, people, Agents,
capabilities, carriers, authority, evidence, state, resources, effects and
lifecycle; it then closes unfulfilled or unreliable relations through the
lowest-total-burden feasible authorized path. A capability gap remains a local
diagnostic view, not the product definition. More host adapters are not a
substitute for consequence-level outcome delivery.

Product-form neutrality remains dynamic rather than form-indifferent. A plugin
or Skill is neither presumed sufficient nor declared inadequate by identity.
Named responsibilities may be closed by one form, several replaceable forms or
no Accord-owned artifact; shared outcome, authority, evidence and lifecycle
contracts provide system coherence without preselecting a Runtime or state
layer.

This also raises three explicit assurance pressures from external review of the
immutable v3.0.1 release: a declared contract must be connected through
activation, execution, effect and evidence rather than mistaken for enforcement;
internal rule identifiers and machine-readable authority records must not become ordinary-user cognitive load;
and cross-session continuity must bind an observed state carrier rather than
pretending static instructions are current state. The review is an input, not
proof of current behavior.

The baseline's current-Skill Codex-local GT-07, GT-11, GT-12 and GT-13 observations
pass their bounded Windows tasks or workspaces and independently verified cleanup. GT-13
is a single-arm dogfood result for the product-definition correction; it is not
comparative population evidence. Installed Hook firing or trust, automatic
implicit invocation, current-session hot reload, Claude, client, Cloud,
cross-host, field-value and production behavior remain unproved. Released
v3.0.1 observations remain historical inputs rather than inherited proof.

Future candidate promotion also requires a source-recorded ancestor
`evaluatedRevision` and unchanged tracked bytes for each task's sparse
`behaviorSubjectFiles`, in addition to current task, evaluation and projection
identity. Historical observations without that binding retain only their finite
claims and cannot be renamed into candidate evidence.

## Selected first mechanism

The historical baseline Codex and Claude packages include one stateless
`SessionStart` context Hook. It is configured to emit a compact activation
reminder on supported new, resumed, cleared and compacted sessions; Claude also
covers a forked session. The reminder points nontrivial or stateful outcomes to the shared
`deliver-demand-driven-outcome` Skill, requires a sufficient native or simple
route to stay silent, reads no transcript, writes no state, starts no background
process and adds no interpreter dependency.

The Claude `fork` matcher covers a host event only. It does not make a copied-
history fork a handoff or a context-relief mechanism, and it proves neither
firing nor continuity. Compact is same-carrier memory reduction; causal fork is
copied-history branching; sequential load relief requires a fresh zero-history
conversation carrier, minimum verified state transfer, destination verification
and only then source release. None of these conversation operations changes
checkout, branch, worktree, repository fork or local/cloud execution placement.

This is only a candidate activation definition, not firing or outcome evidence. Codex requires explicit
trust for the exact current non-managed Hook definition and repeats that review
when its hash changes. Claude currently supports in-session plugin reload;
equivalent active-session Codex hot reload is not established, so update and
rollback remain host-capability-conditioned rather than falsely uniform.

## Prospective finite claim ceiling

Only after every declared gate passes on one unchanged exact revision may the
candidate claim:

- Context-adaptive collaboration-closure conformance for v3.1.0.
- Static package conformance for the current Codex and Claude projections.
- One bounded single-arm collaboration-closure dogfood result.
- Replayed local regression results for continuity, repair and resource stewardship.
- Reproducibility from a clean v3.1.0 checkout.

It does not imply:

- Universal correctness for human-AI collaboration.
- Universal or unbounded Agent autonomy.
- A complete or permanent capability inventory.
- Broad field effectiveness.
- Comparative burden reduction across populations.
- Equivalence across Agents, hosts, clients, models or providers.
- Automatic activation on every message.
- Current-session hot reload that was not directly observed.
- Automatic whole-system resource optimization or reclamation.
- Longitudinal human or organizational outcomes.
- Domain-professional correctness.
- Production deployment safety.
- A release tag or publication before every exact gate passes.

The historical Claude GT-07 cleanup failure is excluded from retained behavior claims.
GT-14 did not retain a provenance-bound exact environment composition.
GT-14 did not retain provenance-bound host, model, tool, extension and Accord exposure facts.
GT-15 did not retain its primary-source, existing-wheel, license, maintenance and provenance ledger.
GT-16 did not retain the complete health, update, rollback, release, cleanup and retirement record.
GT-16 did not retain evidence of shared, unknown and unrelated state preservation.
GT-16 did not retain burden, interference, cost, unknown and claim-limit source facts.
GT-16 did not retain native, official, maintained and composable route-search provenance.
GT-16 did not retain the complete effect, latency, durability, isolation, recovery and lifecycle comparison.
GT-16 did not retain the named environment, outcome, authority and capability-gap source facts.
