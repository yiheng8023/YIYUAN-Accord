# YIYUAN Accord v3.1.0

This is the tracked body prepared for the immutable, non-prerelease v3.1.0
GitHub Release. Repository bytes describe the release but cannot self-attest
clean exact-local review, hosted verification, named-human authority, tag or
publication; the matching public GitHub tag and Release establish those
external facts when they exist. Exact revision
`ae7294652761abceb753f0571ee82c7ddeae06af` remains an unreleased,
historically verified development baseline whose former candidate-ready state
does not qualify the current program. Until the matching v3.1.0 public objects
exist, immutable v3.0.1 remains the latest published recommendation.
Bounded P0-P4 logic feasibility, semantic, isolation, evaluation-design and
product-form work is now complete. P4 admits only a deterministic,
side-effect-free plain-data reference core; its one policy-driven interface is
implemented. Exact checkpoint `553f5a97e08390117e877e7b913c7a501018bfa5`
retains failed GT-14 through GT-16 attempts: fixture execution and cleanup are
observed, but required composition, wheel/provenance and full-vector source
facts are missing. Exact revision `84447a7a1b9557e22ef5585d159459e8701fa40e`
then supplied finite former-contract GT-14 through GT-16 evidence. Fresh
current-contract GT-14 through GT-16 at exact
`1bbcc9542c92674dc0b5adcb032d6f9b01248531` now pass only one exact
environment admission, one primary-source no-dependency feasibility decision
and one disposable pure-data minimum-form bootstrap; failures and costs remain
counterevidence, and no live mechanism or release claim follows. The later
`f0ed9ce715afdbc5d9eb75e08225e9a1e46c554c` GT-17/18 attempt and its
`08eb72a57cdb4b0d27de1df16ebeccccd1e04f9e` former-verifier acceptance
were rejected after independent review found GT-17 cleanup unbound and GT-18
missing per-episode immutable full-vector and explicit carrier source/target-
state evidence. It remains counterevidence while the strengthened GT-17/18
contract fails closed on those omissions. At exact revision
`7d7a7e57b7eea02afcda21880d2f018cbc7dda0c`, a fresh source-complete
replay passes GT-17 only for the bounded evidence correction and GT-18 only for
one synthetic four-episode full-vector sequence with actual carrier states,
rollback, replacement, retirement and zero task resources. Live mechanisms,
durable evolution and representative value remain gated. That earlier source-
bound whole-system balance audit and affected-surface deterministic
reacceptance completed for its historical contract; it is distinct from the
later `85a917a` live-state/lifecycle reacceptance. At exact revision
`2460adcff02bd56144f8d3f647ecef27cd5fefd0`,
fresh current-contract GT-18 passes only one synthetic four-episode immutable-
vector sequence with rejected proxy regression, rollback, bounded native-no-add
retention, later invalidation and three exact disposable carrier edges. Fresh
GT-19 passes only one Codex-local sense-environment responsibility-allocation
sequence: declaration and sparse incidence do not retire; an observed same-
responsibility native successor permits only reversible scoped retirement; and
evidence expiry restores the Accord allocation. Their blocked arm, transcription
and query corrections, initial failed GT-19 review and high evaluator costs
remain counterevidence. GT-21 and GT-20 now each have only bounded exact passes;
source-bound reacceptance from exact cut `85a917a` is complete. The independent
exact-tree review cycle at pre-remediation cut
`523f2ef47df96b76c132525ff432d044e7b1b4b3` covered product, specification,
implementation and standards and produced remediation findings. It did not form
a candidate or transfer review completion to changed bytes. The later
candidate-forming review completed after the source-bound corrections and
selected v3.1.0. At that stage it did not transfer to changed exact-tree bytes,
and four fresh independent exact-tree reviews remained required. That selection followed model-variable, isolation, evaluation,
product-form, implementation,
representative and longitudinal validation, a source-bound whole-system
balance self-audit, minimum justified remediation, affected-surface
reacceptance and independent review; publication remained the last external
phase at that stage. The
self-audit first binds a versioned claim-scoped baseline, target and
counterfactual; it cannot average away a hard failure or substitute for
independent review.

The next local remediation cut `ef10100` also failed fixed-point review. It
still allowed synchronized task, evaluation and wrapper edits to rebind old
GT-20/21 records; did not freeze seven failed or partial attempts; could raise
on null nested observer text; accepted a structurally valid fictional public
Release tail; mislabeled the human history projection as authority; and had no
machine promotion or retirement state for the provisional current-byte gate.
The bounded repair freezes the ordered nine-record ledger and selected semantic
digests, cross-checks the GT-21 record subject, type-narrows nested text, admits
only the exact externally observed release snapshot, validates its derived
human projection and adds active, promoted and post-release-retired lifecycle
states. Promotion requires a ready program with complete current R3 evidence;
retirement requires the corresponding externally reconciled full Release. At
that point the changed tree still required a fresh exact independent review.

Before candidate formation, the latest product correction also makes research
dynamic like every other capability. The existing `reconcile_closure` Interface
now has deterministic cases for a sufficient route with no evidence-acquisition
responsibility, a material gap that allocates on-demand source Adapters, a
public-lead-only route that holds unknown, and bounded authorship only inside
the current task authority. No second research API, provider/model binding,
account connection, persistent service or default network search is admitted.
The same remediation rejects retiring provisional evidence against an older
public Release and requires the human-readable HISTORY ledger to match the
admitted ordered snapshot exactly. These are deterministic product and release
guardrails; they do not claim live online research, current Adapter fitness,
field value, candidate status or publication.

The final candidate calibration also makes stage management explicit.
Baseline, plan, process, acceptance and goal projection are versioned,
evidence-bound stage surfaces; a material change preserves history and replays
the earliest affected boundary without silently weakening acceptance. A stage
snapshot binds the closed node through a stable locator and
`containing-git-commit` relation without embedding its own SHA; after commit the
exact external reference combines that SHA and locator, so it is neither
self-referential nor self-attesting. A separate, non-authoritative evolution horizon is derived on demand from the
project panorama, latest accepted snapshot and fresh environment facts. It
covers maintenance, iteration, updates, bounded refactoring, host adaptation,
retirement or replacement and later development, but adds no automatic roadmap,
new service, second authority source or precomputed capability matrix.

The whole-system audit was refreshed again from exact cut `59a340f` after
independent review found that generic longitudinal shape checks still admitted
meaning-changing GT-18/19 mutations and malformed cleanup could take an
exception path. The minimum repair adds role-level semantic validation,
recomputes GT-19 closure decisions, fails closed on cleanup type and corrects
stale replay, exact release-task and burden wording. It does not add a Runtime,
durable state or another product form. At that cut the repaired tree still
required a new independent review and was not an exact candidate.

Exact cut `7860fee` then passed the independent specification mapping but
failed implementation and standards review: GT-07 carrier meaning could still
be self-attested, GT-18/19 semantics overfit fixed route names and failure
sets, malformed carriers could raise, and `sequenceSha256` was not bound to a
recomputable projection. The next repair validates GT-07 inheritance and
destination-before-release relations, dispatches known longitudinal task kinds
with unknown kinds failing closed, accepts semantically irrelevant extra routes
and varying regression dimensions, type-narrows carriers, binds event revision,
and hashes the canonical whole event excluding the digest field itself. It
preserves the same finite behavior facts and does not replay the expensive
GT-07 fixture. Capacity, summarization and handoff remain non-exhaustive risk
examples; host vocabulary is normalized before capability equivalence. A fresh
exact independent re-review is still required.

Exact `fcefee4` passed that specification re-review but failed standards review
on three residual counterexamples: GT-07 still trusted contradictory narrative
text and a superset of the minimum transfer, GT-18 accepted a human-burden-only
failure outside its declared adversarial regression, and GT-19 did not bind all
episodes to one outcome family. The next repair normalizes the already retained
GT-07 independent narratives into canonical typed facts while preserving their
source hashes, cross-binds the exact minimum fields, derives GT-18 regression
eligibility from the task contract and locks GT-19 to the first outcome ID. No
behavior was replayed or newly claimed. At that cut, goal mode was paused and
another fresh exact independent specification and standards re-review was required.

Exact `f3c9b09` closed all six requested semantic mutants, but specification
review found two stale complexity-budget statements and standards review showed
that the retained narrative SHA remained self-attested. The follow-up binds each
typed report to the same independent-result identity and original narrative in
exact untyped ancestor `fcefee4` through a bounded Git read, rejects synchronized
facts/report/hash substitution, and reconciled every then-active budget statement to
400000 bytes with at least 5% headroom. This still added no behavior replay or
claim. A new exact independent re-review remains required.

The audit later exposed a missing dynamic-retirement seam at exact baseline
`f898ebd6c14580fb9236cd01440b88314b3c2fab`: native no-op selection existed,
but a non-selected redundant Accord route had no responsibility-scoped,
evidence-bound retirement result. That repair added a pure-data contract and
deterministic regression for same-responsibility successor
coverage, prior independent consequence and cleanup, reversible observation,
independent retirement post-state and recheck triggers. At exact `fd4b99a`,
revised GT-19 passes only one finite source-bound Codex-local four-episode
host-drift sequence; its failed routes, token cost and absence of live field
control remain counterevidence. No live host mechanism, Runtime, durable store
or global uninstall was added.

The first BAL-04 paired repository-Skill/no-Accord correction task closed
equally in both arms with zero human action and residue. It therefore selects
no added form for that condition and does not establish incremental Accord
value. Reviewing the exact treatment exposed and corrected a scalar route rule
that conflicted with the current hard-admission, lifecycle-Pareto semantics.
Prior behavior evidence remains bound to its exact Skill hashes. Current local
behavior evidence remains finite and claim-scoped; the Codex and Claude package
projections are not an exact release candidate and cross-host behavior remains
unverified.

The v3.1.0 repository payload closes the product-form
decision at the collaboration-model boundary. It does
not define Accord as a capability set difference. Finite autonomy derives the
smallest relevant graph across outcome obligations, people, Agents,
capabilities, carriers, authority, evidence, state, resources, effects and
lifecycle; it then closes unfulfilled or unreliable relations through the
lowest-total-burden feasible authorized path. A capability gap remains a local
diagnostic view, not the product definition. More host adapters are not a
substitute for consequence-level outcome delivery.

Product-form neutrality remains dynamic rather than form-indifferent. A plugin
or Skill is neither presumed sufficient nor declared inadequate by identity.
Named responsibilities may be closed by one form, several replaceable forms or
no Accord-owned artifact; shared outcome, authority, evidence and lifecycle
contracts provide system coherence without preselecting a Runtime or state
layer.

Official Codex and Claude surfaces are broader than one API. Current Codex App
Server documentation exposes structured model/provider, feature, Skill, Hook,
MCP, installed/callable App, effective-config and thread state plus distinct
thread actions; Hooks and notifications expose lifecycle invalidation events.
Claude exposes its own non-uniform Hook, MCP, plugin and session surfaces. Accord
must consume every supported official surface relevant to the current obligation
before fallback observation or authorship, while preserving host provenance and
leaving unexposed fields unknown. A disposable pure-data PoC normalized two
carrier-shaped envelopes, accepted monotonic same-writer updates and rejected
dual writers, expiry and raw conversation fields without touching a live host.
This proves contract and adapter-shape feasibility only, not live permission,
freshness, Agent consumption, behavior or value.

The v3.1.0 correction keeps responsibility implementation and state
coordination orthogonal. Routes may mix the three implementation modes, while a
typed field receipt prefers a supported official structured query or state
interface, then other current official host-owned state, Accord state and
bounded observation for unexposed facts. Each binding has a normalized
decision target and value, one writer, bounded readers, explicit fallback
precedence, source and current generation; events only invalidate. Signal-only
invalidation may return only an independently bound last-safe allocation. Sharing,
observation or injection cannot substitute for Agent use, execution,
consequence, independent evidence or value. Earlier GT-19 evidence
predates this contract and remains bounded history. A then-working-tree source
retains schema preflight and two zero-rule exec-policy blocks, then one
ephemeral read-only Codex arm over exact sparse bytes matched an independent
synthetic four-episode current-receipt probe and removed its task copy. It has no
`evaluatedRevision`; the later source-bound reacceptance does not promote it, so
a future exact-revision observation remains required.
An official carrier that proves an exact same-duty state responsibility at lower
total burden opens reversible subtraction of the displaced Runtime, adapter or
wheel; this is a lifecycle decision, not automatic deletion from API presence.
The v3.1.0 guardrail is 105 tracked files and 680000
Python-and-test bytes with at least 5% headroom and three tracked-file slots.
In the v3.1.0 state projection, 102 tracked files preserve
all three slots and the current Python/test byte count and headroom are
recomputed by the canonical verifier after every code or test change. These
values must retain at least five percent headroom; they are not inferred from
an earlier revision.
The final 90000-byte increase over the former 590000-byte ceiling pays
only for canonical snapshot-origin discovery, the self-contained schema-v1
historical predicate, revision-bound authority, current and historical evidence,
declared projection package and marketplace identity, deterministic malformed-
history failure closure, complete transition-state comparison, aligned admitted
revision-blob limits, one bounded reusable history index, and regressions that
prevent carry rebinding or change-then-restore washing; it is not general growth
allowance and does not imply a complete
historical verifier. The full budget
pays for typed source/conflict validation, the minimum live-adapter boundary,
anchored visible-history context and exact scoped-evaluator successor checks,
the failed `523f2ef` review's unconditional provisional-source and complete-
package checks, and the failed `ef10100` review's retained-attempt, lifecycle,
malformed-input and non-self-attested public-history regressions. It does not
pre-authorize a Runtime, store, client bridge, unconditional network search or
fixed model route.
The final pass adds policy-driven no-research, corroborated acquisition and
bounded-authorship cases through the existing closure Interface, binds each
frozen provisional contract record after promotion or retirement, rejects
non-string release identities and requires a visible exact HISTORY ledger.
The representative decision rule now makes research and prototypes conditional
on dynamic applicability. The former evaluator digest remains admitted only
for unchanged GT-20 lifecycle and GT-21 continuity claims and proves no live
research behavior.

The latest distribution correction distinguishes product form from lifecycle
experience. Multiple internal forms remain permitted, but “plug-and-play” and
“remove-and-leave” now require one user lifecycle intent after source trust,
host-native orchestration of all selected Accord components, preservation of
existing and concurrent user configuration plus shared and foreign state,
atomic failed-update rollback, zero active Accord registration, exposure,
process and data state, and a non-callable bounded automatic host-cleanup
contract for any retained host-owned inert cache. Accord neither calls such a
cache physical zero residue nor deletes it outside supported lifecycle. Static
plugin-scoped package contents do not prove that chain.
The v3.1.0 packages add one shared short-lived `SessionStart` event
adapter behind their Skills. `startup` and `clear` stay silent; `compact` and
`resume` convert only supported event fields into non-authoritative hints in
typed minimum continuity context, with no persistent process or state. Its public seam, identical package
bytes, isolated native discovery and structured App Server query feasibility
pass. One disposable Claude session-only `SessionStart:startup` event also
triggers the exact copied adapter and completes silently. A separate ephemeral
Codex App Server arm disables the enumerated installed Hook, invokes official
`thread/compact/start`, observes one working-projection `sessionFlags` context with
`signal.source=compact`, and sees the Agent return exactly the injected schema
value with zero tool use. Installed-package `compact`, sessionFlags `resume`,
Node portability, semantic directive use, product consequence and topology actuation remain
unproved. A later exact-package zero-turn arm proves goal-backed resumability
and a direct official resume-response-to-current-receipt-to-core chain, while
showing that installed `SessionStart:resume` does not execute before the next
turn. The first installed-package turn remains an enabled-but-untrusted
`NO_CONTEXT` control. After two pre-model path and generated-response-shape
corrections, one separately authorized model-variable read-only turn supplied
runtime-only `bypass_hook_trust` through both thread request `config` objects.
Exactly one installed synchronous `SessionStart:resume` Hook started and
completed, injected `yiyuan-accord-hook-context/v1` with `signal.source=resume`,
and the Agent returned exact `HOOK_CONTEXT_RESUME` without tools; cleanup passed.
General inventory trust remained modified and the event reported
`permission_mode=bypassPermissions`, so the broad bypass is test-only rather than
a production trust route. Hook-caused official re-sensing and core invocation,
named consequence, independent post-state and comparative value remain unproved.
GT-21 now passes only one bounded current-host case: exact arms cover the live
event-to-consequence route, a no-model-bound sufficient simple route and one
fresh zero-history handoff with destination verification before source release.
GT-20 now passes only one bounded disposable Windows lifecycle from tracked
`v3.0.1` to exact `4c8bcc3`: injected native source failures retain the exact
old package, recovery installs exact current bytes, user and foreign state
survive, native removal leaves zero active Accord state, and task roots are
removed. Codex retains no Accord cache; Claude's unlisted `.orphaned_at` caches
remain host-owned and are subject to the exact local binary's 14-day live-
session-aware automatic sweep. Neither task alone proves production trust,
ordinary or actual Claude host activation, cross-host value, whole-system balance,
candidate selection or release readiness. At exact cut `85a917a`, source-bound
whole-system reacceptance was complete only for these bounded claims and
candidate-forming reviews were still required. Those reviews later completed and
selected v3.1.0, but did not transfer to changed exact-tree bytes. At that cut,
the next gate was exact local verification and four fresh independent exact-tree
reviews on one replacement SHA.

Exact `0988b22` then failed that exact-tree review. An identical snapshot could
be carried into a later commit while silently rebinding changed plan or evidence,
and a successor could treat a malformed latest node as accepted after checking
only a few predecessor fields. The remediation derives one canonical origin from
the oldest commit in each contiguous first-parent node run, checks every carry
and freezes every bound authority/surface/evidence value to that origin. It
recomputes a bounded, schema-versioned repository snapshot-lineage predicate for
each carry and recursively for the latest different predecessor origin, rejects
carry or stale locators and non-contiguous replay, and covers oversized SemVer
plus bounded Git batch input. The predicate reads the exact revision's authority,
declared projection packages and marketplaces, and current and historical
referenced evidence bytes; it does not replay
the complete historical verifier or attest historical behavior, live hosts,
human authority, publication or other external gates. It changed no model route
or product carrier; a new exact candidate and all four reviews were therefore
required at that cut.

Exact `0da23a2` then failed implementation review. Its historical reader used a
smaller implicit Git-output ceiling than the one-megabyte JSON admission rule,
its gate-transition comparison omitted host projection and marketplace state,
and recursive predecessor discovery repeated the same first-parent scans. The
correction uses the admitted one-megabyte limit for each referenced revision
blob, compares the complete normalized transition state plus raw marketplace
identity, and builds one bounded first-parent history index for reuse across the
lineage walk. The index admits at most 4096 revisions and 67108864 aggregate Git
batch bytes, so both time and memory remain finite; exceeding either bound fails
closed rather than weakening the predicate. Neither failed SHA is a release
candidate or publication authority.

Exact `b202c40` then failed exact-tree review for two independent reasons. A
deeply nested but sub-one-megabyte historical JSON object could raise a
structural parser exception outside the verifier's fail-closed boundary, and
the prospective public README plus this tracked Release body still directed a
v3.1.0 reader to v3.0.1 or described publication as pending. The correction
normalizes declared structural failures before they escape the lineage reader,
tests a generated 600001-byte deep-history object without storing a large
fixture, points immutable source-install commands to v3.1.0 while retaining GUI
routes as explicitly historical evidence, and makes publication wording
conditional on the external tag and Release. The hosted job budget is 120
minutes because the expanded 36-test cross-platform seam no longer fits the
historical 20-minute ceiling; this changes no check or finite claim. `b202c40`
remains a failed lineage predecessor and grants no publication authority.

Exact `f5f281c` and `6d91360` simple-route arms each ran one installed startup
Hook silently with zero context and zero tools but failed before Agent output.
The latest request named no concrete model, retained the host-resolved value only
as observation provenance and captured official structured class `other`, ten
same-turn error notifications and server retry. Model identity and version are
runtime variables, never product identity, route authority or an oracle
condition. Those attempts expose no residual Accord duty and admit neither a new
client nor model-label substitution.

An exact Claude session-only plugin recheck at `7a4c932` completed one startup
Hook silently, then stopped at isolated-host authentication before any model API
request, Agent consequence, token use or cost. Its task root was removed. This
is retained as GT-21 host-admission counterevidence only; it authorizes neither
credential access, account escalation, model-label retry nor a new Accord
mechanism. The simple consequence and release gate were open at that cut.

The user then explicitly authorized one current-account Claude lane with a
`$0.25` ceiling. Official auth status reported logged in, but the corrected exact
session-only invocation at `5473c43` returned CLI exit 1 and a structured result
error after one silent startup Hook and before any model API request, Agent
consequence, token use or cost. Zero tools ran, no credential, account identity
or failure message was retained, and the task root was removed. A preceding
shell-shape correction also used zero API requests and left no task root. This
repeated pre-model boundary stopped same-lane retry and is retained only as
historical counterevidence; it is not the current candidate blocker.

A later explicitly authorized disposable current-account Codex 0.151.0 lane
installed exact tracked `fded9a6` package bytes and ran one ephemeral read-only
turn without requesting a concrete model. The process exited 0, the Agent
returned exact `ACCORD_SIMPLE_NATIVE_OK`, and the completed stream contained no
tool-like items. Two completed items were also typed `error`; their message
content was neither read nor retained. Exact Codex 0.151.0 source classifies
this wire shape as non-fatal diagnostics while the processor remains running;
fatal stream errors are top-level `error` events and failed turns are
`turn.failed`, neither of which occurred. The observed PowerShell shell-
snapshot warning is not assigned as either item's specific cause. Installed
Hook/runtime bytes matched the tracked archive, and the disposable credential,
marketplace, plugin and task root were removed. The sufficient simple route now
passes. Exact `3878968` then uses a separate official `thread/start` destination,
transfers only goal, authority, code binding, current state, evidence and claim
limit, and consumes one read-only turn without requesting a concrete model. The
destination calls one task-owned verifier, returns exact
`ACCORD_FRESH_HANDOFF_VERIFIED`, observes the source unreleased, and is
independently verified before source release. No copied-history fork, Git
branch, worktree, repository fork, code-topology or execution-placement change
occurs; source, destination, App Server, disposable account/plugin state and the
exact task root are removed. This closes GT-21 only for the bounded current-host
claim and selects the existing Skill, short-lived Hook adapter, pure core and
manifests without adding a browser bridge, MCP, SDK/App Server client or
persistent service. The separate GT-20 evidence closes only the disposable
Windows transactional lifecycle described above. The later source-bound review
selected the v3.1.0 component set without expanding those claims. Production
trust, live Claude behavior, cross-host value, updated-client entry
compatibility and release remained open at that review cut; any current-entry
claim or installation guidance still requires read-only re-sensing first.

A zero-turn disposable App Server sequence retained unsupported ephemeral goal
state, a non-persisted no-turn thread and a recovered isolation-variable
collision as counterevidence. One task-owned official goal then made the thread
resumable. The resume response directly triggered structured configuration,
Hook, Skill, permission and goal queries, a current five-field receipt and pure
core invocation. The core returned `hold-unknown` because the wrapper could not
independently prove its own product consequence. This is bounded direct
event-to-state-to-core feasibility, not an admitted product form, GT-21
observation, candidate or release evidence.

This also raises three explicit assurance pressures from external review of the
immutable v3.0.1 release: a declared contract must be connected through
activation, execution, effect and evidence rather than mistaken for enforcement;
internal rule identifiers and machine-readable authority records must not become ordinary-user cognitive load;
and cross-session continuity must bind an observed state carrier rather than
pretending static instructions are current state. The review is an input, not
proof of current behavior.

The baseline's current-Skill Codex-local GT-07, GT-11, GT-12 and GT-13 observations
pass their bounded Windows tasks or workspaces and independently verified cleanup. GT-13
is a single-arm dogfood result for the product-definition correction; it is not
comparative population evidence. Installed Hook firing or trust, automatic
implicit invocation, current-session hot reload, Claude, client, Cloud,
cross-host, field-value and production behavior remain unproved. Released
v3.0.1 observations remain historical inputs rather than inherited proof.

Future candidate promotion also requires a source-recorded ancestor
`evaluatedRevision` and unchanged tracked bytes for each task's sparse
`behaviorSubjectFiles`, in addition to current task, evaluation and projection
identity. Historical observations without that binding retain only their finite
claims and cannot be renamed into candidate evidence.

## Selected live-mechanism set and retained alternatives

Exact historical baseline `ae729465` includes an earlier stateless
`SessionStart` context Hook. The v3.1.0 package replaces
its echo with a shared short-lived Node event adapter
and changes its matcher; specifically, the working projection removes the earlier Claude
`fork` matcher and covers the currently documented new, resumed, cleared and
compacted session sources. These current bytes are not attributed back to the
historical revision. The adapter uses the package's shared behavior module,
requires a sufficient native or simple route to stay silent, reads no
transcript, writes no state, starts no background process and depends on a
host-path Node executable rather than an Accord-managed Runtime. `startup` and
`clear` stay silent. `compact` and `resume` emit only non-authoritative event
hints plus a requirement for permitted task-time re-sensing before receipts or
allocation; invalid input fails visibly without echoing it. The injection is
not a live receipt, Agent use, execution, consequence, evidence or value.

No SessionStart `fork` matcher is claimed. A copied-history fork remains a
causal branch rather than a handoff or context-relief mechanism. Compact is
same-carrier memory reduction; causal fork is
copied-history branching; sequential load relief requires a fresh zero-history
conversation carrier, minimum verified state transfer, destination verification
and only then source release. None of these conversation operations changes
checkout, branch, worktree, repository fork or local/cloud execution placement.

This bounded v3.1.0 mechanism is not production-trust, broad
firing or field-value evidence. Codex requires explicit
trust for the exact current non-managed Hook definition and repeats that review
when its hash changes. Claude currently supports in-session plugin reload;
equivalent active-session Codex hot reload is not established, so update and
rollback remain host-capability-conditioned rather than falsely uniform.

The v3.1.0 component set is the existing Skill, short-lived event Hook, pure
core and host manifests. Direct official query and action surfaces remain
inputs; the App Server client is retained only as an evaluator/reference route.
No browser bridge, second API, MCP, SDK/App Server client, plugin-private durable
state or persistent service is admitted. Future responsibilities may reopen the
route only when current evidence establishes a new gap and the same lifecycle
oracle justifies it. The candidate-preparation task did not hot-load mutable
treatment bytes and could not serve as its own baseline.

## Finite claim ceiling

The list below is the v3.1.0 finite claim ceiling. If the matching immutable
public tag and Release exist and every declared external gate closed on the same
exact revision, it is the release claim; otherwise it remains prospective.
Repository text cannot decide which external state currently holds:

- Context-adaptive collaboration-closure conformance for v3.1.0.
- Static package conformance for the current Codex and Claude projections.
- One bounded single-arm collaboration-closure dogfood result.
- Replayed local regression results for continuity, repair and resource stewardship.
- Reproducibility from the v3.1.0 clean exact checkout.

For transparency, the machine-readable compatibility projection carries the
following two exact v3.1.0 strings. They are public-claim markers, not proof
of tag creation or publication:

- Context-adaptive collaboration-closure conformance for v3.1.0.
- Reproducibility from a clean v3.1.0 checkout.

It does not imply:

- Universal correctness for human-AI collaboration.
- Universal or unbounded Agent autonomy.
- A complete or permanent capability inventory.
- Broad field effectiveness.
- Comparative burden reduction across populations.
- Equivalence across Agents, hosts, clients, models or providers.
- Automatic activation on every message.
- Current-session hot reload that was not directly observed.
- Automatic whole-system resource optimization or reclamation.
- Longitudinal human or organizational outcomes.
- Domain-professional correctness.
- Production deployment safety.
- A release tag or publication before every exact gate passes.

The historical Claude GT-07 cleanup failure is excluded from retained behavior claims.
The retained historical first source-sufficiency attempt for GT-14 through GT-16
omitted the provenance-bound environment and host exposure, wheel/license/
maintenance ledger, and complete effect, state, burden, route and lifecycle
vectors recorded by that failed attempt. Those omissions remain counterevidence
only for the historical attempt; the current formal GT-14 through GT-16
observations are qualified separately and do not inherit those exclusions.
