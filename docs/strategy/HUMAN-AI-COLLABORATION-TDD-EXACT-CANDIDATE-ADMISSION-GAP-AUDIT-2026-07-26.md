# TDD Exact-Candidate Admission Gap Audit

Date: 2026-07-26
Status: static gap audit complete; both candidates remain blocked

Neither candidate is admitted or rejected by this audit. It classifies the
exact source review into pass, partial, and blocking evidence and defines the
minimum remaining gate for a possible one-time non-comparative diagnostic. No
candidate Skill was invoked, no model request or task turn was sent, and no
projection, installed capability, configuration, CC Switch, Git, admission, or
portfolio state was changed.

## Matt `tdd` at `ed37663…`

Matt's current record contains three static pass observations and five blocking
gates. The current main revision, MIT license, and three projected files match
the dated source observation. The body bundles no script
or concrete command, asks for no network, account, dependency installation,
configuration change, or permission expansion, and substantially aligns with
the frozen public-seam, behavior, independent-expectation, vertical-slice,
RED-before-GREEN, minimal-implementation, and boundary-mocking standards.

That is not exact execution admission. The current projection is not the
approved six-file adapted release payload. The repository's approved
`skill.curated.tdd` is byte-different and contains six files rather than the
current three-file projection. The dated observation binds no signature or
distribution attestation evidence, so attestation remains unknown. Existing security,
portability, overlap, and notice evidence binds historical revision
`6eeb81b5…`, not `ed37663…`. Current Matt also moves refactor out of the loop
into the excluded `code-review` workflow, while the approved adapted Skill
refactors after GREEN. The trial neutralizes that scoring conflict but does not
resolve exact-candidate operational admission.

The remaining gate must bind the current license and attribution, exact
security/portability/executable surface, overlap with native and adapted TDD,
frontmatter and relative references, frozen Python commands, the three mutable
files, and explicit exclusion of code-review, network, dependencies, Git,
configuration, and external systems. Any later admission must be diagnostic
only and identity-bound, not release-inventory promotion.

## Superpowers 6.2.0 `test-driven-development`

The local OpenAI-curated package manifest, MIT license, and two selected files
match the dated source observation. The selected content needs no MCP, App,
Hook, account connection, network, or dependency installation, and shares valid
RED, minimal GREEN, real-behavior, independent-expectation, and mutation-check
principles with the hard gates.

Superpowers has a concrete deletion conflict with the frozen fixture. Its body
says pre-test production code must be deleted and started over, while the
fixture intentionally contains a public signature, docstring, and sentinel
before the first valid RED. Misclassifying that scaffolding could violate the
no-production-mutation-before-valid-RED gate. The body also demonstrates
`npm test`, while this fixture supplies immutable Python test commands, and
directs the Agent to fix other failing tests, which can expand writes beyond the
three mutable files. Its examples are TypeScript/Jest-oriented and a reference
to `superpowers:writing-skills` is outside the exact projection.

No repository Skill, admission, or release entry exists for this exact
candidate. Its remaining gate must resolve command priority, protect the
preloaded scaffolding, prohibit broader fixes and deletions, exclude full
Superpowers orchestration and unprojected Skills, and complete exact provenance,
notice, security, portability, overlap, and validation review. If those
constraints cannot be accepted without changing the treatment identity, the
diagnostic stays blocked.

## Shared live-transition gate

Even a future exact-candidate admission is insufficient by itself. The current
one-dispatch and no-replacement rules are preregistration, not operational
enforcement. A tested consuming runner and append-only identity ledger must
reserve one candidate/thread/turn identity before any model request and reject a
second or replacement dispatch before starting a thread.

This audit proves no body delivery, loader event, causation, value, relative
improvement, pairwise difference, preference, superiority, formal weak-Agent
acceptance, general TDD competence, production readiness, cross-host value,
self-authored residual gap, admission/rejection decision, or portfolio action.
