# Claude consumer Skill projection snapshot

## Scope

This dated read-only snapshot narrows the former conceptual-only Claude Code
consumer state to the CC Switch projection surface actually observed on the
local host. `C:/Users/15521/.claude/skills` contains 44 names matching the
common root: 43 links into CC Switch storage and one materialized
`kimi-webbridge` directory. The same 44 database rows carry both
`enabled_claude=1` and `enabled_codex=1`; 24 bind
`mattpocock/skills@v1.2.2`, while 20 retain local identities.

The three control contracts use CC Switch links on Claude but materialized
consumer-repository trees on the common and Codex-specific roots. This is a
verified physical projection difference, not evidence that either host loaded,
invoked, delivered, or benefited from those instructions. Host projection byte
parity is not required by the current portfolio contract.

The Claude Plugin root was observed to exist, but Plugin-managed Skill roots,
payloads, settings, enablement, and behavior were not inventoried in this
slice. The snapshot therefore does not claim a complete Claude consumer map.

## Claim and authority boundary

This evidence proves only a dated physical projection, its database-flag
reconciliation, and one cross-consumer projection difference. It does not prove
instruction discovery, loader precedence, enablement, invocation, instruction
delivery, behavior, Plugin mapping, backup/restore, cross-device parity, value,
or production readiness.

No database, Skill, projection, consumer, Plugin, source registration,
installation, enablement, backup, restore, deletion, or release mutation was
authorized or performed. All supported acceptance criteria remain `partial`.
