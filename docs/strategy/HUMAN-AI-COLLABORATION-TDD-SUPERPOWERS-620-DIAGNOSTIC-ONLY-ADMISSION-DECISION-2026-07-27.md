# Superpowers 6.2.0 TDD Diagnostic-Only Admission Decision

Date: 2026-07-27

Status: `admit-diagnostic-only-current-dispatch-still-blocked`

## Decision

The exact two-file `test-driven-development` projection from the locally
installed OpenAI-curated Superpowers `6.2.0` package is
`admit-diagnostic-only` for the single preregistered, non-comparative
`SE-TDD-SUPERPOWERS-6.2.0` diagnostic.

This is an identity-bound repository-governance decision. It is not an
approved release admission, does not add an entry to `registry/admissions.json`
or `release-manifest.json`, and does not make the candidate currently
dispatchable. No candidate projection was materialized or executed, no model
request was sent, and no network, installation, global configuration, CC
Switch, portfolio, Git, or external-write action was performed.

## Exact package, source, and license

The decision binds package version `6.2.0`, upstream `obra/superpowers`,
release commit `3dcbd5c4b48e02263fbf4a3c01e3fe4f81d584d9`, package-tree
identity, manifest identity, MIT license identity, and the exact
`SKILL.md` and `writing-good-tests.md` file identities. The current source
reconciliation found all 14 packaged Skill entries byte-equal to the release.

The attribution for this bounded decision is:

- upstream: `obra/superpowers`;
- release: `v6.2.0`;
- commit: `3dcbd5c4b48e02263fbf4a3c01e3fe4f81d584d9`;
- license: MIT, SHA-256
  `a37e0e9697144819e1d965176ac4ae5bc3fa02d11e7812036bbcadf6dafe2400`;
- copyright: Copyright (c) 2025 Jesse Vincent.

No candidate source is redistributed. The repository-level third-party notice
is not changed or promoted into release notice evidence by this diagnostic
decision. Signature and distribution attestation remain unknown, and fresh
package/source verification remains mandatory at any future dispatch.

## Security and executable surface

The two selected files are Markdown guidance and contain no bundled script,
binary, Hook, MCP, App, account, credential, network, dependency-installation,
configuration, or external-system requirement. The package manifest declares
`Interactive`, `Read`, and `Write`, but has an empty Hooks object. Those
package-level capabilities are observed permission surface, not execution
authorization for the selective two-file diagnostic.

Plugin features and the full Superpowers bootstrap remain disabled and
excluded. Future file writes and test commands would be controlled by the
frozen fixture and tested runner, not inherited executable assets.

## Explicit conflict disposition

The admission keeps candidate bytes unchanged and applies the higher-priority
fixture and project hard standards:

1. The exact command
   `python -B -m unittest -v test_feature.py` overrides TypeScript, Jest, and
   npm examples.
2. The preloaded public signature, docstring, and sentinel are authorized
   fixture scaffolding. They must not be deleted or mutated before a valid RED.
3. If an unrelated test fails, the diagnostic aborts. It must not repair that
   test or broaden write scope.
4. `superpowers:writing-skills` and full Superpowers orchestration are
   excluded.
5. Only `feature.py`, `test_feature.py`, and `PROCESS_EVIDENCE.json` may
   change.

These controls make the exact candidate suitable only for the named diagnostic
under the frozen fixture. They do not show that unrestricted Superpowers TDD
is portable or that the candidate instructions caused any result.

## Why dispatch remains blocked

`admit-diagnostic-only` closes only the exact candidate's repository-governance
admission gate. It does not authorize materialization, a task turn, or a model
request. A future dispatch still requires fresh package, release, selected-file
and toolchain revalidation; source-snapshot-to-factory freshness; configured
live ledger authority; formal-runner integration of the tested authorization
adapter and append-only identity ledger; and the remaining real app-server
handle and cleanup boundaries. Separate fresh dispatch authority is required.

Until every downstream gate passes, the candidate remains metadata-only and
must not run.

## Claim boundary

Source or package identity is not behavior or value. Diagnostic admission is
not invocation, causation, comparative improvement, preference, superiority,
general TDD competence, production readiness, cross-host value, approved
release admission, portfolio authority, or proof of a self-authored residual
gap.

The machine-readable authority is
[`registry/human-ai-collaboration-tdd-superpowers-620-diagnostic-only-admission-decision-2026-07-27.json`](../../registry/human-ai-collaboration-tdd-superpowers-620-diagnostic-only-admission-decision-2026-07-27.json).
