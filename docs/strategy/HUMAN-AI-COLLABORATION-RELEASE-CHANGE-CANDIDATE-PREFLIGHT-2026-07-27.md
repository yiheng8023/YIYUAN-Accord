# Release/Change Candidate Preflight

Date: 2026-07-27
Scenario: `SE-RELEASE-CHANGE-01`
Status: repository source/license/admission/overlap validated; live arms blocked

## Decision

The exact candidate universe is `native`,
`skill.curated.ci-cd-and-automation`, and
`skill.curated.shipping-and-launch`.

The two curated candidates pass only the repository-local source, license,
admission, release-payload identity, and overlap preflight. This result does not
prove current CC presence, current-host availability, candidate-specific
exposure, loader invocation, instructions reaching a model, behavior, value,
release competence, preference, superiority, or a residual self-authored gap.
No candidate is materialized or executed.

Machine authority:
`registry/human-ai-collaboration-release-change-candidate-preflight-2026-07-27.json`.
The parent protocol binds that file at 12007 bytes and
`7f8ac35c2ad21486da322e29ac8847e8ae352ab22f3acacfaf0c057accdb8e66`.
Its ten source-binding paths are an exact set; removing a provenance, license,
admission, manifest, routing, capability, or overlap authority invalidates the
preflight even if the remaining file digests still match.

## Native control

Native represents the portable baseline of general reasoning, synthesis,
instruction following, ordinary planning, and normal use of exposed tools. It
has no repository Skill payload and no Skill-loader event. The current
host/version, actual model route, task-scoped route, and instructions reaching
the model remain explicitly `unknown`.

Native is the first future arm. Shared hard standards and the frozen oracle are
controls, not native or Skill value.

## `ci-cd-and-automation`

- Repository payload:
  `skills/ci-cd-and-automation/SKILL.md`;
- SHA-256:
  `7aa008e4be26068c9e61ea8a9303711020e376c6cbfdf10d581a9fd400acf8ea`;
- bytes: `11470`;
- upstream:
  `github:addyosmani/agent-skills` at
  `17214a29c429a19f7a9607f2c06f9d650ea87eb0`;
- original path: `skills/ci-cd-and-automation`;
- license: MIT;
- adaptation: cross-agent, text-only, no adopted executable;
- admission: `approve`, `validated=true`, native baseline compared, overlap
  reviewed.

Its admitted increment is a repeatable delivery-pipeline design, staged gate,
rollback, and validation contract beyond ordinary code editing. Its primary
scope is pipeline policy, automated quality gates, packaging, staged delivery,
and deployment safeguards.

Current CC body identity, candidate-specific exposure, loader invocation, and
instructions reaching a model remain explicitly `unknown`; behavior and value
are unproved.

## `shipping-and-launch`

- Repository payload:
  `skills/shipping-and-launch/SKILL.md`;
- SHA-256:
  `195a1fad5612627464df4581954727b8ebd649b0ce4bfe91e06655bcc32302b0`;
- bytes: `11464`;
- upstream:
  `github:addyosmani/agent-skills` at
  `17214a29c429a19f7a9607f2c06f9d650ea87eb0`;
- original path: `skills/shipping-and-launch`;
- license: MIT;
- adaptation: cross-agent, text-only, no adopted executable;
- admission: `approve`, `validated=true`, native baseline compared, overlap
  reviewed.

Its admitted increment is an evidence-based GO/NO-GO, staged rollout,
monitoring, rollback, and post-launch verification contract. Its primary scope
is release readiness, rollout, monitoring, communication, rollback, and
post-change evidence.

Current CC body identity, candidate-specific exposure, loader invocation, and
instructions reaching a model remain explicitly `unknown`; behavior and value
are unproved.

## Overlap boundary

Both curated candidates add structured procedures beyond the portable native
baseline, but live added value is unproved. They overlap on staged delivery and
rollback language:

- `ci-cd-and-automation` owns pipeline design and automation;
- `shipping-and-launch` owns the final readiness decision, monitoring,
  communication, and post-change evidence.

The first attributable comparison must use separate candidate arms. Composition
would prevent treatment attribution and is not authorized by this preflight.

## Failure and fallback

- Digest, revision, license, admission, or overlap drift invalidates the
  preflight.
- Unproved current-host identity or exposure remains `unknown` and stops the
  candidate arm.
- A host without an independent loader event must retain loader attribution as
  `unknown`; behavior consistency cannot be promoted to causal Skill credit.
- A sufficient native result stops external comparison unless separate
  comparison authority exists.
- Failure to isolate the two curated candidates stops the first comparison
  rather than composing them.

## Authority and claim boundary

This is repository-only metadata validation. It performed no external
discovery, network access, model request, candidate materialization or
execution, CC Switch read or mutation, global configuration read or mutation,
or Git operation.

The validator requires the exact curated class
`approved-curated-third-party`, exact repository registry status `approved`,
and complete authority, execution, claim, and failure/fallback mappings.
Missing keys, empty maps, source-path removal, or relabeling approved metadata
as an installed/current-CC identity fail closed.

Before any live arm, a separate current-host preflight must bind exact
host/model/sandbox identity, exact execution-source bytes, task-scoped selected
and unselected exposure, and an independent loader event or explicit
loader-unknown attribution. A real release fixture and separate dispatch
authority remain mandatory.
