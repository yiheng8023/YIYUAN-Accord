# Human–AI collaboration semantic-authority continuity protocol — 2026-07-28

## Result

`SEM-03` now has a frozen three-treatment design, but no treatment has been run
under this protocol:

1. native reasoning with configurable user Skills disabled;
2. the exact CC-managed adapted `grill-with-docs` monolith;
3. the exact current Matt `grill-with-docs -> grilling + domain-modeling`
   composition at commit
   `ed37663cc5fbef691ddfecd080dff42f7e7e350d`.

The local adapted-monolith arm is pinned to the repository payload at
`skills/grill-with-docs/SKILL.md`. Its `cc.grill-with-docs` identity records the
historical CC-managed lineage; it does not depend on the mutable live CC Switch
installation remaining byte-identical after separately authorized replacement.

This is a protocol and direction-falsification gate, not candidate-value
evidence. It installs nothing, updates nothing in CC Switch, vendors no
third-party Skill body, dispatches no model, and authorizes no portfolio
preference.

## Why the old comparison is insufficient

The 2026-07-24 requirements trial already compared native Spark/low with the
local adapted monolith for three valid pairs. Both arms failed the hidden hard
contract, exact loader invocation remained unproved, and neither treatment
earned preference. Repeating the same one-turn ambiguity review would add
volume rather than evidence.

The unresolved question is cross-lifecycle continuity. A useful semantic
authority layer must preserve accepted meaning after the original grilling
turn, across fresh requirements, modeling, specification, independent review,
and handoff contexts. Source structure, a `CONTEXT.md` file, concise prose, or
one correct terminal answer cannot prove that.

## Fixed scenario and lifecycle sequence

The disposable scenario uses the user-bound `Pitch` example:

- `stand-alone video` initially has no canonical repository definition;
- Pitch-to-Video cardinality and deletion behavior are initially unresolved;
- a draft implementation uses cascade deletion before accountable acceptance.

The harness later injects the same frozen human decisions into every arm:

- a stand-alone Video has `lessonId == null`;
- one Pitch may be referenced by multiple stand-alone Videos;
- deletion of a referenced Pitch is restricted rather than cascaded;
- Pitch records packaging intent, not an implementation plan.

Each arm then crosses four fresh-thread phases:

1. ambiguity and contradiction elicitation;
2. semantic modeling and a sparse decision record;
3. specification consumption;
4. implementation-conflict review plus bounded handoff.

Fresh threads make repeated clarification and semantic loss observable instead
of letting conversational memory hide them. The portable authority carrier is
required, but the literal filename `CONTEXT.md` is not.

## Common hard gates

Every treatment receives identical facts, frozen human decisions, private
oracle, mutation boundary, and hard standards. It must:

- retain accepted term and decision identities across all four phases;
- keep unresolved material unresolved before human acceptance;
- separate domain language from implementation plans;
- create an ADR-equivalent record only for a hard-to-reverse, surprising,
  real-trade-off decision;
- detect the restrict-versus-cascade conflict before release or closure;
- prevent notes, projections, specifications, reviews, and handoffs from
  self-promoting into authority;
- avoid invented user decisions and writes outside the disposable root.

Terminal correctness cannot erase an earlier semantic contradiction,
unauthorized promotion, or lost decision.

## Direction falsification

The protocol can reject several attractive stories:

- If native meets the same hard gates without a material continuity or burden
  disadvantage, this scenario does not justify mandatory grilling.
- If the local monolith is more reliable than the current composition, current
  upstream structure does not justify a CC update.
- If the current composition is more reliable and cheaper under the fixed
  measurements, it becomes a portfolio-review candidate, not an automatic
  replacement.
- If all three fail the same boundary, first inspect the carrier, scenario,
  host delivery, and existing composition. Do not jump to a self-authored
  Skill.
- If loader or treatment delivery is unproved, report association only.

Directional measurements include clarification and repeated-clarification
counts, accepted-term and decision retention, contradiction detection,
unauthorized promotions, human burden, semantic-delta completeness, host
exposed token counts, latency, and treatment-fidelity evidence.

## Weak-Agent and execution boundary

The requested acceptance floor remains `gpt-5.3-codex-spark` with `low`
reasoning. The actual model and effort must be recorded. Silent substitution is
invalid. `gpt-5.6-terra/low` may diagnose an ambiguous failure but cannot count
as weak-Agent acceptance.

The disposable builder and parent-private oracle now exist. Eight offline
examples include one positive bundle and seven injected continuity, ADR,
review, handoff, status-promotion, and repeated-clarification faults. Packet
construction excludes the private oracle and withholds the frozen human
decisions until the harness performs the phase-boundary injection.

The
[current Matt static admission](HUMAN-AI-COLLABORATION-SEMANTIC-AUTHORITY-CURRENT-MATT-STATIC-ADMISSION-2026-07-28.md)
now binds all eight raw Git-blob files rather than only the three `SKILL.md`
entries. MIT license, provenance, static security, and portability review pass
for a disposable no-network trial. The review also records that Windows
`core.autocrlf=true` changes checkout hashes, so raw blob identity is required.

Before a live run, an isolated raw-byte projection, dependency-complete
task-scoped exposure, and private-oracle leakage checks must still pass. These
execution admission gates are currently open.

## Projection implementation checkpoint

The dedicated three-Skill projection builder and no-model exposure probe now
exist. Four builder tests include an injected transport failure and verify that
source retrieval is completed and hash-checked before any projection file is
written. Three exposure-probe tests cover the exact three-Skill identity set,
hash drift, and one missing dependency.

Two earlier live materialization attempts were stopped by intermittent GitHub
transport failures. The second exhausted bounded retries while reading
`skills/productivity/grilling/SKILL.md`; the repaired builder left that exact
temporary root empty. Those attempts remain transport-blocked preflight
incidents rather than candidate failures.

A later retry through the same exact-revision GitHub REST transport succeeded.
The builder retrieved and verified all eight package files plus the MIT license
before writing the isolated projection. The materialized manifest has SHA-256
`81570ce9ed247e5f346aa96f97fe30a89f3c15a333289f59192e2146751a3332`;
the projected-file tree has SHA-256
`295c4f5819f38e49cd4955d81294a5da1ce3197d78fc52c24bfecaf92027daa5`.

The subsequent Codex Desktop `0.145.0` no-turn probe observed exactly the three
required repository Skill identities. Its unselected control enabled zero
configurable Skills, while the selected arm enabled only
`domain-modeling`, `grill-with-docs`, and `grilling`. The identity set,
non-configurable states, projection bytes, global configuration, and repository
status remained stable. No thread, turn, or model request was started. The
durable report is
[`audits/human-ai-collaboration-semantic-authority-current-matt-no-model-exposure-2026-07-28/REPORT.json`](../../audits/human-ai-collaboration-semantic-authority-current-matt-no-model-exposure-2026-07-28/REPORT.json).

This closes dependency-complete task-scoped exposure only. Entry or dependency
loader invocation, slash-name resolution, relative-file delivery, instruction
delivery, behavioral causation, semantic continuity, candidate value, and
cross-host behavior remain unproved. All model-treatment runs remain at zero.

## 2026-07-31 current-host exposure refresh

Codex CLI and app-server drifted from the earlier `0.145.0` observation to
`0.146.0`, which triggered a no-model refresh rather than a behavioral rerun.
The first refresh attempt stopped before Skills enumeration because the old
probe extended each inherited MCP table with `enabled=false`; `0.146.0`
rejected the current `codegraph` table as having an invalid transport. No
candidate instruction ran and no report was written, so this is a probe-host
compatibility failure rather than a candidate failure.

The probe now uses a temporary empty `CODEX_HOME` beneath the disposable
projection and replaces the MCP table with an empty one for the no-turn
processes. Two TDD regressions require that isolation and cleanup behavior.
The repaired probe observed `Codex Desktop/0.146.0`, exactly the same three
required repository Skill paths and raw hashes, zero configurable Skills in
the unselected arm, and only the three composition Skills in the selected arm.
It started no thread or turn, sent no model request, retained no temporary
Codex home, and left global configuration, projection bytes, and repository
status stable. The current snapshot contains 50 Skills (`3` repo, `6` system,
`41` user), versus 114 in the older host snapshot; that dated inventory drift
is not deletion, quality, invocation, or value evidence.

The durable refresh report is
[`audits/human-ai-collaboration-semantic-authority-current-matt-no-model-exposure-2026-07-31/REPORT.json`](../../audits/human-ai-collaboration-semantic-authority-current-matt-no-model-exposure-2026-07-31/REPORT.json).
It refreshes dependency-complete exposure only. Native-disabled exposure,
local adapted-monolith selected exposure, public-packet private-oracle leakage
rejection, loader and instruction delivery, named composition, relative-file
delivery, behavior, value, and every weak-model run remain open.

The machine-readable authority is
[`registry/human-ai-collaboration-semantic-authority-continuity-protocol-2026-07-28.json`](../../registry/human-ai-collaboration-semantic-authority-continuity-protocol-2026-07-28.json).
