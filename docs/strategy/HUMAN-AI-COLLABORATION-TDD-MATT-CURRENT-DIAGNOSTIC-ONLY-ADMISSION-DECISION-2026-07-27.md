# Matt Current TDD Diagnostic-Only Admission Decision

Date: 2026-07-27

Status: `admit-diagnostic-only-current-dispatch-still-blocked`

## Decision

The exact `mattpocock/skills` TDD projection at
`ed37663cc5fbef691ddfecd080dff42f7e7e350d` is
`admit-diagnostic-only` for the single preregistered, non-comparative
`SE-TDD-MATT-CURRENT` diagnostic.

This is an identity-bound repository-governance decision. It is not an
approved release admission, does not add an entry to `registry/admissions.json`
or `release-manifest.json`, and does not make the candidate currently
dispatchable. No candidate bytes were materialized or executed, no model
request was sent, and no installation, global configuration, CC Switch,
portfolio, Git, or external-write action was performed.

## Exact identity and provenance

The decision binds the three source paths under
`skills/engineering/tdd/`, their projected paths, byte lengths, Git blob
identities, SHA-256 digests, the three-file projection-tree digest, and the
exact MIT license identity. The dated authenticated read-only observation
found current main equal to the pinned revision.

The attribution for this bounded decision is:

- upstream: `mattpocock/skills`;
- revision: `ed37663cc5fbef691ddfecd080dff42f7e7e350d`;
- license: MIT, SHA-256
  `0e7ac423bf2c6e223b7c5b156f8cf72da49d748e56a1641402c31f22ad07dbb5`;
- copyright: Copyright (c) 2026 Matt Pocock.

No source text or source asset is redistributed by this decision. The
repository's existing Matt notice binds an older curated revision; this record
does not relabel it as notice for the current revision. Signature and
distribution attestation remain unknown, and fresh source verification remains
mandatory at any future dispatch.

## Review result

The exact three-file projection is Markdown guidance with no bundled script,
concrete shell command, network or account call, dependency installation,
configuration mutation, permission expansion, credential access, Hook, MCP,
App, or external-system requirement. Future diagnostic writes and commands
remain task and runner effects rather than inherited executable assets.

The method is portable only to the frozen Python standard-library fixture. The
public seam is pre-agreed. The fixture does not require structural refactoring,
and refactor presence is not scored. This controls the current source's
decision to defer refactoring to `code-review`, while `code-review`,
`ask-matt`, optional project-context expansion, and the rest of the Matt
workflow remain excluded.

The source projection overlaps native TDD and the approved six-file adapted
`skill.curated.tdd` payload, but all three current logical files differ from
the approved payload and the approved payload has three additional files.
Therefore the existing approved entry is lineage and comparison evidence only;
it is not admission of these current bytes.

## Fixed diagnostic boundary

The exact Python command has priority over candidate examples or optional
workflow guidance:

```text
python -B -m unittest -v test_feature.py
```

Only these three files may be changed:

```text
feature.py
test_feature.py
PROCESS_EVIDENCE.json
```

The diagnostic excludes `code-review`, `ask-matt`, the full Matt workflow,
network, dependency installation, Git, configuration, MCP, Apps, Hooks,
browser, account or credential access, external systems, and external writes.
It permits at most one future dispatch, produces no comparative score or
ranking, and leaves every repository hard standard active and uncredited to
the candidate.

## Why dispatch remains blocked

`admit-diagnostic-only` closes only the exact candidate's repository-governance
admission gate. It does not authorize materialization, a task turn, or a model
request. A future dispatch still requires fresh source and toolchain
revalidation, source-snapshot-to-factory freshness, configured live ledger
authority, integration of the tested authorization adapter and append-only
identity ledger with the runner, and the remaining real app-server handle and
cleanup boundaries. Separate fresh dispatch authority is also required.

Until every downstream gate passes, the candidate remains metadata-only and
must not run.

## Claim boundary

Source or package identity is not behavior or value. Diagnostic admission is
not invocation, causation, comparative improvement, preference, superiority,
general TDD competence, production readiness, cross-host value, approved
release admission, portfolio authority, or proof of a self-authored residual
gap.

The machine-readable authority is
[`registry/human-ai-collaboration-tdd-matt-current-diagnostic-only-admission-decision-2026-07-27.json`](../../registry/human-ai-collaboration-tdd-matt-current-diagnostic-only-admission-decision-2026-07-27.json).
