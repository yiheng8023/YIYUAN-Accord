# Human-AI Collaboration Unknown-Quadrant Packet Overlay PoC

Date: 2026-07-27
Status: zero-model private overlays compatible; injected faults fail closed

## Result

Five private oracle overlays are byte-bound to concrete zero-model packets:
Known Knowns, Known Unknowns, Unknown Knowns, Unknown Unknowns, and the
method-attribution firewall. All five base overlays are compatible, and five
negative fault injections—private-oracle exposure, hard-standard drift, source-packet
mutation, unverified source identity, and live-authority expansion—are all
rejected.

The overlay does not rewrite the public packet, historical fixture, hard
standard, or live authority. Compatibility here means that the parent can bind
the private score contract without changing those surfaces. It does not prove
semantic equivalence, live Agent behavior, or candidate value.

## Unknown Knowns fixture gap

The repository already had an appropriate executable zero-model packet for
Known Knowns, Known Unknowns, Unknown Unknowns, and method attribution. The
creative `GEN-CREATIVE-01` scenario existed only as a planned matrix row, not an
executable Unknown Knowns packet.

A derived synthetic packet now supplies one positive example and five negative
controls. It requires three materially distinct, reversible directions while
preserving every brief fact and leaving information density, color temperature,
and motion level unresolved for human reaction. It rejects Agent-selected
winners, invented human preference, fact loss, convergent directions, rights
clearance claims, and unauthorized publication.

This closes a fixture-coverage gap, not a product capability residual gap.
Passing the packet does not prove creative quality, human preference, rights
clearance, or live Skill value.

## Decision boundary

The private overlay compatibility PoC is validated with zero model requests,
zero Skill invocations, zero asset generations, and no CC Switch, global
configuration, Git, or external-state mutation.

The next bounded action is to inspect one existing packet renderer or builder
for a parent-only oracle attachment seam. Reuse that seam if it already exists.
If it does not, record the exact adapter gap before implementation. This record
does not authorize oracle exposure, live weak-Agent dispatch, a new Skill, or
self-authoring.
