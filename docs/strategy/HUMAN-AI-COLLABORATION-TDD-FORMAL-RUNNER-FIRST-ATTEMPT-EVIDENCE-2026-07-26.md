# TDD Formal Runner First Attempt Evidence

The first native Spark/low attempt completed the integrated formal runner but
is invalid and does not count as a repetition.

The current-host raw stream normalized without loss under the bounded event
contract. The model, low effort, no-fallback route, disabled configurable
Skills, preserved system Skills, no-network workspace, global configuration,
and changed-file boundary were observed as requested.

The Agent's final implementation passed its three visible tests and the two
parent-owned hidden oracle classes. That final green did not satisfy the hard
standard. The event timeline did not observe a focused GREEN after the
production mutation, and the Agent-authored tests killed only five of seven
predeclared mutants. `nonpositive-delay-accepted` and
`string-schedule-accepted` survived.

This is precisely the distinction the process-loss risk requires: final
functional output can look correct while the process and independent test
quality lose required information. The hard gates therefore reject the run
instead of treating final green as a substitute.

The raw artifact and reanalysis report remain temporary cleanup debt. Their
digests are durable, but neither file is vendored and deletion is not
authorized by this evidence record.

The runner bytes used for the original capture were observed by SHA-256 but
were not retained after reanalysis support was added. This record therefore
separates that historical digest from the currently verifiable runner file.
The raw trace was subsequently reanalyzed without an Agent rerun under
`codex-app-server-tdd-normalizer-v2`.

This attempt proves the bounded formal-runner integration path on Codex
Desktop 0.145.0. It does not prove native weak-Agent TDD acceptance,
treatment delivery, Skill causation, candidate preference, general coding
superiority, production readiness, or cross-host value.

At most two more native attempts should run under the identical contract. If
the same hard-gate signature recurs three times, stop that arm rather than
manufacturing counted repetitions from invalid attempts.

That was the bounded next action at this checkpoint. The later three-attempt
batch reached the cap with heterogeneous signatures and zero valid
repetitions; its current decision is recorded in
`HUMAN-AI-COLLABORATION-TDD-NATIVE-FORMAL-ATTEMPT-BATCH-2026-07-26.md`.
