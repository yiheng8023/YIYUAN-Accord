# Process-Fidelity Chained-Trace Measurement Calibration

Date: 2026-07-27

Status: zero-Agent measurement calibration passed; live cohort not authorized

## Result

The controlled transcription calibration used zero Agent dispatches and zero
model calls. It reused the existing multi-hop metric evaluator and added the
missing trace adapter boundary: every material edge records the predecessor
output hash, current input hash, equality result, transform-contract hash, and
output hash.

Five frozen cases passed their preregistered outcomes:

- an exactly linked control remained clean;
- a predecessor-output/current-input mismatch failed closed;
- one local omission propagated and expanded to two downstream affected hops
  with amplification factor `2.8`;
- terminal restoration matched the source anchor but did not erase the
  recorded intermediate authority loss;
- an opaque material edge kept amplification and rollback metrics unknown and
  failed closed.

The absolute and process ledgers cannot cancel each other.

## Protocol relationship

The v2 information-equivalent protocol is now explicitly a delivery-topology
and terminal-task-fidelity subprotocol. Its existing source-backed smoke
remains one absolute-task-valid transport repetition, not a process-trace-valid
repetition. The smoke has no frozen process-hop ledger or cascade measurement.

This calibration does not inspect hidden model state. An acknowledgement is
not a semantic-retention snapshot. Only persisted carrier, transform, tool,
turn, and result artifacts are observable.

## Next evidence gate

The evidence stop remains active. A subsequent raw-event trace assessment
found that the durable existing smoke lacks the required material-edge
evidence: its process-hop ledger is absent and one material transform is
opaque. The smoke therefore remains a transport pilot and any later formal
comparative cohort starts from zero. The next bounded result is future protocol
design without dispatch.

No live cohort, additional topology, model call, candidate comparison, or
self-authored capability is authorized by this calibration.

Machine record:
`registry/human-ai-collaboration-process-fidelity-chained-trace-measurement-calibration-2026-07-27.json`.
