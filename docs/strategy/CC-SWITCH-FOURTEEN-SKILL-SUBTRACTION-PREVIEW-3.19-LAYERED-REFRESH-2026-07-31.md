# CC Switch Fourteen-Skill Subtraction Preview 3.19 Layered Refresh — 2026-07-31

Status: read-only transaction forecast refreshed.
No mutation has been authorized or executed.

## Result

The exact fourteen-item CC Switch 3.19 transaction, canary, backup rotation,
rollback, remote snapshot, common-link cleanup, and five-part authorization
boundary remain frozen in the 2026-07-30 manager refresh.
This document is not a new transaction design.

The only material delta is the source-verified layered `doc`/`pdf` state.
The Codex private root now has 57 entries because the two CC-owned aliases are
present. The fourteen subtraction targets remain unchanged, so the corrected
Codex post-state forecast is **57 → 43**, not 55 → 41.

## Preserved policy and sentinels

The two `doc`/`pdf` aliases resolve to the same CC-owned canonical identities
as the common-root aliases. Their two Codex configuration rows continue to
enforce canonical host-disable. Neither Skill belongs to the subtraction
cohort.

The following remain unchanged sentinels:

- `doc` and `pdf`;
- Matt's twenty-two promoted Skills;
- the three self-authored Skills;
- `diagnose`;
- Trae-owned and Plugin-owned roots;
- first-party physical Skill trees.

The other predicted counts remain database 41, CC 41, common agents 27, and
Claude 41. The point-in-time forecast is invalidated by any concurrent CC
Switch mutation and must be preceded by the live fail-closed preflight.

## Authority boundary

The pending five-part authorization boundary still covers:

1. uninstalling exactly the fourteen named Skills through the manager;
2. allowing the exact fourteen-oldest backup evictions if no concurrent backup
   appears;
3. removing only the fourteen resulting broken common-root links after full
   manager success;
4. writing and verifying one configured remote snapshot;
5. creating and then exactly cleaning the bounded recovery archive and
   temporary debug helper.

It does not authorize a `doc`/`pdf` toggle, Codex configuration change,
restart, separate app-server, model turn, Hook change, or any other portfolio
mutation.

## Claim boundary

This overlay makes the transaction prediction current against the layered
preflight. It does not prove execution, rollback, post-state, remote recovery,
remaining portfolio value, or program closeout.
