# TDD Non-Comparative Diagnostic Source And Governance Preflight

Observed: 2026-07-26T18:26:18+08:00
Status: dated source observation matches; execution governance remains blocked

This dated preflight rechecked only the source and governance surfaces required
by the preregistered non-comparative protocol. No projection, app-server
session, thread, turn, or model request was started. No candidate instructions
were sent to a model, and no installed Skill, global configuration, CC Switch
state, Git state, or portfolio state was changed.

## Source observations

Matt current main still matched the pinned revision
`ed37663cc5fbef691ddfecd080dff42f7e7e350d`. Read-only authenticated GitHub
contents responses matched the registered license and three TDD file lengths,
Git blob identities, and SHA-256 digests.

Superpowers 6.2.0 local package bytes still matched the registered plugin
manifest, license, `test-driven-development/SKILL.md`, and
`writing-good-tests.md` lengths and SHA-256 digests.

Codex remained at 0.145.0. The raw-item normalizer v2, source-pinned projection
builder, and no-turn selection preflight probe also matched their registered
digests.

## Governance observation

The Matt lineage has an approved and validated repository entry,
`skill.curated.tdd`, but the current three-file projection is not the approved
release payload: its `SKILL.md` hash differs from the released adapted payload,
all three shared logical file hashes differ, and the six-file release also
carries `deep-modules.md`, `interface-design.md`, and `refactoring.md`.

For Superpowers 6.2.0, no repository Skill, admission, or release entry exists
for the exact TDD projection. OpenAI-curated distribution is not silently
converted into this repository's curated execution admission.

Accordingly, neither exact candidate is currently execution-eligible under the
new protocol.

## Evidence limits

The repository retains exact identities and the dated observation, not the raw
GitHub responses or a copy of the local Plugin package. Its validator checks the
record against repository protocol and governance state and rehashes the three
repository-local toolchain files, but it does not replay the GitHub API
observation or reread the external Plugin cache. It validates a historical
observation; `freshForDispatch` remains false. These dynamic checks must be
repeated immediately before any later admitted dispatch.

This preflight proves no Skill-body delivery, independent loader event,
behavioral causation, candidate value, comparison, preference, formal weak-Agent
acceptance, production readiness, cross-host value, residual self-authored gap,
or portfolio decision.
