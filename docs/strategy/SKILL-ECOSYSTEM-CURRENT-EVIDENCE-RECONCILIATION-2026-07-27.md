# Skill Ecosystem Current Evidence Reconciliation — 2026-07-27

Status: **current evidence reconciled; no value, causation, preference, or
portfolio mutation claim**

This document is the current read-only reconciliation layer over dated Skill
source, runtime, exposure, live-fixture, TDD-admission, and self-authored
ablation evidence. It does not rewrite the 2026-07-23 overlap matrix. Every
input is bound by SHA-256 in
`registry/skill-ecosystem-current-evidence-reconciliation-2026-07-27.json`.

## Historical and current baselines

- The 2026-07-23 matrix records **Superpowers 6.1.1** and remains valid only as
  historical evidence.
- The current runtime-owned Superpowers source/package baseline is **6.2.0**:
  all 14 Skill entry files match the pinned release bytes. This proves source
  identity, not behavioral equivalence, invocation, execution admission, or
  value.
- The current Matt source baseline remains revision
  `ed37663cc5fbef691ddfecd080dff42f7e7e350d`. Its source pin is not an
  execution admission.
- The only observed Superpowers incident behavior in the bound live evidence
  is **6.1.1** behavior. It must not be relabeled as 6.2.0 behavior.
- CC Switch reports 251 database rows and 233 distinct names, while the
  observed body/projection layer has 75 physical and 75 resolvable bodies with
  176 unresolved Claude links. Database rows are not a loaded or invoked Skill
  count. Backup restore and cross-device content equality remain unproved.

## Reconciled evidence cells

| Cell | Bound result | Current boundary |
| --- | --- | --- |
| Requirements: native vs `grill-with-docs` | 3 pairs; visible 0/3 vs 1/3; hidden 0/3 vs 0/3 | Both arms fail the hidden contract |
| Implementation: native vs `disciplined-coding` | functional 3/3 vs 3/3; strict process 3/3 vs 2/3 | Bounded mixed result; no causation |
| Incident: native vs historical CC `diagnose` | functional 3/3 vs 3/3; strict process 0/3 vs 2/3 | Association only; not current Matt evidence |
| Incident: current Matt | visible 3/3; hidden 1/3; strict process 1/3 | Source-pinned mixed result |
| Incident: historical Superpowers 6.1.1 | visible 3/3; hidden 2/3; strict process 1/3 | Historical only; not 6.2.0 behavior |
| Maintenance: native vs adapted `deprecation-and-migration` | visible 3/3 vs 3/3; hidden 3/3 vs 1/3; clean process 3/3 vs 3/3 | Bounded native association; no general preference |
| Native TDD formal attempts | 3 attempts; 0 valid comparative repetitions | No valid comparative baseline |
| Current Matt TDD | 0 repetitions | Exact candidate execution admission blocked |
| Current Superpowers 6.2.0 TDD | 0 repetitions | Exact candidate execution admission blocked |
| Self-authored three-contract chain | deterministic packets only; 0 live cells | Live ablation arm is missing |

Task-scoped selected exposure is proved for the completed comparison cells.
Independent loader events and proof that candidate instructions reached the
model are not proved. Selected exposure must not be relabeled as loader
invocation.

## Fail-closed interpretation

The ledger rejects all of the following promotions:

- Superpowers 6.1.1 behavior relabeled as current 6.2.0 behavior;
- payload presence or selected exposure relabeled as loader invocation;
- repeated runs with hard-oracle failure relabeled as value;
- static content lineage relabeled as behavioral equivalence;
- the absent self-authored arm relabeled as zero performance or a residual gap;
- CC database rows relabeled as loaded or invoked Skills; and
- a current source pin relabeled as execution admission.

Across all cells, value, causation, general preference, cross-host value,
retirement eligibility, and portfolio-mutation eligibility remain false.
Missing-arm evidence is absence of evidence, not evidence of zero performance.
No deduplication, migration, retirement, deletion, installation, configuration
change, model dispatch, or portfolio mutation is authorized by this record.

## Next bounded gate

The prior gate—verifiable Spark/low routing plus task-scoped selected Skill
exposure—has already been observed and should not be repeated as though it were
still missing. Before another comparative run, bind:

1. exact current-candidate execution admission;
2. candidate-specific treatment fidelity, or an explicit unknown; and
3. a materially different scenario with a frozen hard oracle.

This is an evidence-reconciliation checkpoint, not a candidate-selection or
implementation decision.
