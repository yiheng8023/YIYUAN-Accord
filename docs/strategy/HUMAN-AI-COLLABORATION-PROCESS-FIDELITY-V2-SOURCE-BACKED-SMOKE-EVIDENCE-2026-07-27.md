# Process-Fidelity V2 Source-Backed Smoke Evidence

Date: 2026-07-27

Status: bounded source-backed smoke pass; one absolute-task-valid transport
repetition, no completed arm or topology comparison

## Result

One `gpt-5.3-codex-spark/low` source-backed repetition passed the frozen v2
absolute task oracle and the observed native read-only boundary. The Agent
received a root containing only `PUBLIC-SOURCE-BUNDLE.json`, called
`read_public_information_bundle` exactly once with the required locator, and
returned the fixed submission `armId`, all five expected claim states and
source sets, zero unsupported conclusions, and false write/external-access
declarations.

The parent observed no command execution, file change, tree delta, MCP tool
call, web search, provider fallback, or global configuration change. The
dynamic reader granted no general filesystem authority and returned the
required canonical public-bundle hash.

## Identity and calibration boundary

This was the replacement for the earlier measurement-invalid smoke. The
corrected renderer kept `fixtureId`, submission `armId`, and parent-side
`informationArmId` distinct. The earlier invalid result remains excluded and
is not retroactively counted.

This result counts as one absolute-task-valid source-backed transport
repetition. It is not a process-trace-valid repetition: no hop ledger or
cascade measurement exists. The preregistered arm requires three valid
transport repetitions, so no arm is complete. Complete-single-turn and
same-thread incremental topologies have not run under v2.

## Durable run record

The raw structured report, build manifest, trial packet, and exact public
source bundle are preserved under
`audits/process-fidelity-v2-source-backed-r2-2026-07-27/`. The machine
validator recomputes their byte hashes, the report canonical hash, response
hash, thread and turn identity, source fixture identity, and the at-dispatch
protocol hash. Independent verification therefore does not depend on `.tmp`.

The at-dispatch protocol file hash was
`7d5ff7a9e0b538204a6ba9ceeaecfaf8de09855ee42da0c7957a909491161357`.
Later result-ledger updates to the current protocol file must not be confused
with that frozen run identity.

## Claim limits

The evidence does not prove:

- a delivery-topology comparison or relative process-fidelity effect;
- automatic compression, automatic task creation, or `handoff` Skill
  invocation;
- lossless fresh-session recovery or universal private-oracle isolation;
- cross-host portability, general research quality, candidate superiority, or
  a residual self-authored gap;
- end-to-end process-fidelity acceptance.

The full private-oracle leakage absence is still not universally proved; the
bounded pre-dispatch carrier scan and observed scoped-read boundary passed.
A subsequent raw-event assessment found this smoke process-trace-ineligible
because a material transform is opaque and no process-hop ledger exists. It
remains a transport pilot, any later formal process cohort starts from zero,
and the required next action is an evidence stop rather than an automatic
wider cohort.

The machine record is
`registry/human-ai-collaboration-process-fidelity-v2-source-backed-smoke-evidence-2026-07-27.json`.
