# Skill Portfolio Current-55 Subtractive Triage

Date: 2026-07-30

Status: **live 55 partitioned; read-only subtraction preview open**

## Result first

The remaining 55 CC Switch rows are not one homogeneous quality tier. A
current source and host audit produces seven mutually exclusive cohorts:

| Cohort | Count | Current posture |
| --- | ---: | --- |
| Matt current promoted exact | 22 | Retain pinned; test incremental value |
| Self-authored falsifiable controls | 3 | Retain stable controls pending comparison |
| Explicit user or host exceptions | 2 | Retain without generalizing value |
| Shared entity, Codex shadow-disabled | 2 | Retain for common/Claude; disabled on Codex |
| Strong subtraction candidates | 15 | Preview removal; one reference-blocked |
| Addy source rebinding/current delta | 5 | Freeze; review before relink/update |
| Task-bound specialist value/overlap open | 6 | Test or host-disable before disposition |

The partition covers all 55 distinct database rows exactly once. It is not a
claim that the retained 40 all have behavioral value.

## Matt source truth

`mattpocock/skills` `main` still resolves to
`2ab958093e83e0ec752e6c1c5932da465bf23e0c`, exactly the installed pin. The
release authority remains the 22 paths in `.claude-plugin/plugin.json`, not
CC Switch's recursive discovery count. All 22 live CC trees match their raw
Git blob file sets and hashes. There is no promoted-suite snapshot staleness
to repair.

The name `setup-matt-pocock-skills` is misleading in this governance
discussion: the Skill scaffolds repository tracker, triage-label, and domain
context files. It is not Matt's one-click cross-Agent installer and does not
by itself bypass CC Switch. Its promoted status does not prove value, but
CC-manager policy is not a valid name-only reason to delete it.

## Strong subtraction evidence

Five legacy local identities already have exact current promoted replacements:

- `diagnose` -> `diagnosing-bugs`
- `review` -> `code-review`
- `setup-project-skills` -> `setup-matt-pocock-skills`
- `to-issues` -> `to-tickets`
- `to-prd` -> `to-spec`

Ten more items sit outside Matt's promoted release:

- deprecated: `design-an-interface`, `qa`, `request-refactor-plan`,
  `ubiquitous-language`;
- in progress: `writing-beats`, `writing-fragments`, `writing-shape`;
- personal: `edit-article`;
- misc plus an existing reject decision: `setup-pre-commit`;
- absent from the current source tree: `zoom-out`.

Fourteen items are ready for an exact manager-removal preview. `diagnose` is
not: the live `capability-router` and
`observability-and-instrumentation` bodies still name the legacy diagnosis
identity. Removing it first would turn subtraction into a broken route.

The promoted `setup-matt-pocock-skills` body also mentions deprecated `qa`
even though `qa` is absent from the promoted manifest. This is an upstream
release-consistency warning, not a reason to silently edit the exact upstream
package. It does not block a preview, but the dangling semantic reference must
be recorded.

## Addy source debt

`addyosmani/agent-skills` has advanced from the last reviewed revision
`06300e2...` to `7829ffd...`. Of the five selected upstream paths,
`performance-optimization` and `shipping-and-launch` changed across that
revision interval. All five live CC bodies differ from current upstream after
line-ending normalization, and their CC rows carry no source metadata.

These five remain frozen. The right next action is current-revision delta,
license/provenance, safety, and value review followed by a separately
authorized relink or update decision—not deletion and not silent overwrite.

## Specialist and control boundary

`disciplined-coding`, `migrate-to-shoehorn`,
`obsidian-open-format-knowledge-files`, `obsidian-vault`, `playwright`, and
`security-ownership-map` have task-specific or cross-host value hypotheses.
They also overlap native, runtime, official Plugin, or narrower installed
capabilities. They need task-bound probes or host-specific disabling decisions,
not count-based retention.

The three self-authored contracts remain falsifiable controls. Native implicit
Skill selection is now proved on Codex, but their incremental value, reduction,
or retirement still requires comparative evidence. `caveman` remains an
explicit user exception; its old repository `reject` classification is not
silently converted into deletion authority.

## Boundary and next action

This audit used the live CC database read-only, read installed bodies, refreshed
two public sources, and removed both exact temporary clone roots. It installed,
updated, disabled, or deleted no Skill and sent no model request.

Next, build a reversible CC Switch preview for the 14 ready candidates, repair
the two `diagnose` references before considering its removal, re-review the
five Addy bodies, and probe the six specialists. Any uninstall, toggle, source
update, or self-authored body change requires separate authorization.
