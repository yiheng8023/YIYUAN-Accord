# Codex App-Server Skill Treatment-Fidelity Evidence

Date: 2026-07-24
Status: synthetic body-only delivery proved on the bound host; independent loader event absent

## Result

The no-turn preflight discovered the disposable canary as a project (`repo`)
Skill. With the same inventory identity set, the control enabled zero
configurable Skills and the selected condition enabled only the canary. No
thread or model turn ran during preflight.

The live assay then completed three counterbalanced pairs on Codex app-server
`0.145.0` with `gpt-5.3-codex-spark/low`:

- all three unselected controls returned exactly `NO_TREATMENT`;
- all three structured selected arms returned exactly their distinct body-only
  tokens;
- the tokens were absent from Skill names, descriptions, paths, public prompts,
  structured inputs, and process-scoped config before each turn;
- all six thread and turn identities were distinct;
- no command, file change, MCP, App, dynamic-tool, collaboration-Agent, web, or
  other forbidden item appeared;
- canary bodies, global Codex config, and repository status remained stable.

The raw report is pinned by SHA-256 in the machine evidence. The durable record
also retains the three synthetic tokens, body hashes, response hashes, and
thread/turn identities so body and response digests remain reproducible after
temporary-root cleanup.

## Bounded Conclusion

This proves that body-only content from a project-scoped synthetic Skill
reached the model through the current structured Skill mechanism on the bound
host. It closes the host-mechanism gap that structured-input acceptance alone
could not close.

It does not create an independent loader notification and does not prove that
the installed historical `diagnose` body reached the model or caused its 2/3
strict-process association. It does not prove current Matt value, Superpowers
value, superiority, portfolio readiness, production competence, or cross-host
behavior.

The next comparison should use disposable, source-pinned candidate projections
rather than install, update, or global configuration changes. Exact installed
candidate delivery and causal value remain separate questions.

Machine evidence:
`registry/codex-app-server-skill-treatment-fidelity-evidence-2026-07-24.json`.
