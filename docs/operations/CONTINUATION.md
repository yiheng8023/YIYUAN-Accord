# 当前接续

更新：2026-09-15 · N33-20260909 / r25。[计划](PLAN-v3.3.md#当前推进顺序)拥有当前目标与路线，[基线](BASELINE-v3.3.md)和[验收](ACCEPTANCE-v3.3.md)展开F01–F08/A01–A08；机器投影为product/development.json。历史过程按[记录](PROCEDURE-v3.3.md)回查，不在本页重复整套试验经过。

## 目标、授权和用户最新纠正

完成3.3功能与系统收官，保持原main检出，本开发任务不开Goal。设计供应商中立，本版仅交付适用OpenAI入口，Claude后置；普通Chat不作为默认独立交付入口。Work/云端与跨项目衔接按真实职责和可行路径判断，不因缺Hook直接排除。

用户已允许自主提交/推送及检查；本公开仓库的标准GitHub托管运行器不受私仓2000分钟额度限制，按验证需要运行远端CI，不默认skip ci；用户2026-09-14已要求尽快完成并发布，功能和质量不打折；3.3.0在完整必要验收及精确候选核对后发布，当前尚未就绪。新账户、信任、数据、实质费用及无关共享/外部写入保持各自边界。云端环境、IDE和开发对话当前保留，归档另需明确授权；清理任务自身无复用价值的资源，保护唯一有效成果和必要恢复证据。

用户9月14日明确指出：22条开发任务及大量投入不应换来交接局部试验的无限循环。开源/同源机制应直接阅读和复用，复杂问题应拆到可交付结果；继承不要求全部历史无损。基本闭环为动态感知、在安全阈值前触发、必要时多轮补交、确认接管、释放源恢复职责。关键目标/权限/暂停/成果/未完责任必须保真，过程细节可以压缩或丢弃。既有指导已覆盖主要原则，当前纠正的是执行路线，不能再用增加同义指导或私有脚手架代替推进。

README本轮以后暂只保留已完成的Ubuntu→Linux改名，其余内容按用户要求留待发布前整体修订。知乎和Reddit已进入[传播候选](LAUNCH-v3.3.md)，尚未排期或发布；9月18日Product Hunt仅为可选机会，质量优先，不改变项目承诺或制造发布日期。

Plan/Goal不构成完整交付的前置条件；仅在用户明确选择或请求时使用，兼容其当前版本约束。普通继续/完成要求不触发自动开启，内部计划记录不是Plan模式。当前开发仍未开启Goal。

用户9月15日进一步指出随时介入是全程适应要求，不限于自动交接。既有变化处理设计保留，计划和验收补充执行前、在途、迟到回执及恢复后再次变化的风险组合；不穷举所有操作，也不因插话重排原工序。

观点和共识都需接受事实核对；不把开源同源推成各端接口相同、不把提前触发推成接管必成，也不把Plan/Goal一概判为固定流程或绝对不适合复杂工程。A01/A04已明确事实勘误与批准后状态同步反例，A08区分观察截断和产品归因；验收中重复要求发布新授权的旧措辞已按现有条件授权纠正。计划/接续变化同时检查判据、案例条件和证据效力，纯进度更新不机械重跑。

本轮关于整体上游、Skill/Hook协作、按任务情境隐式参与、简单需求轻量闭环、裸宿主生态发现/互补、上下文感知独立职责及八项自举的讨论均已获用户确认，已统一进入r25计划、基线、验收及机器投影。八项不作完备架构证明，自证指举证与可验证性，有限系统改进与强递归分开；五个关键连接落入既有A项，不制造新的固定流程或放宽发布底线。

用户进一步收准责任边界：热更新是Accord自身插件更新，安装/启用不接管介入前的无关用户历史。承接范围内既有任务、用户交付或当前目标实际采用的材料仍需核实适用性；保护他人既有成果不等于承担其历史债务。本项目已承接的开发历史及连续性保持，当前热更新案例只验证Accord已绑定的同一暂停任务。此边界已落到入口/恢复指导、Skill、基线、计划及验收。

用户再次强调基线、计划、验收变化须同时作用于受影响历史、当前与未来及整体依赖。计划和A04/A08已明确这一适用性检查；本次回溯修正基线仍将旧安装观察当当前、将全部SessionStart统称无状态的表述，并更新下表旧13文件候选数。旧包/旧标准下的原始结果保留，当前复用资格单独判断；不因此重演未受影响的试验。

## 当前事实

- 仓库以实时Git核对为准。当前工作候选为16文件3.3.0-dev.1，包SHA `164febb22bfd246b56547d2b1d5cf347dfa8728cc7433be9d2e5ade2f05037d3`；Skill入口6534字节，连同三份参考共24978字节。分层减少入口一次性读取量，未证明总上下文或费用下降。本次普通观察启动前重新核对：共享缓存已为16文件，与164febb2候选逐文件相同且保持启用；注册Git源仍停在1429f2a。缓存变化发生于本次模型运行前，具体触发者未知，不由目录相同推成当前会话已加载或市场已更新。此前13文件观察仍保留其原时间范围。已根据真实误删反例收准清理前的文件归属判断与完整原件核验，并明确未来案例措辞；清理指导已在一次后续两轮普通报告链中取得有界成功；不由单样本推断全面修复或因果增益。普通Hook保存当前transcript来源，新增按需observe-context和同次读取评估，正常用量增长不制造重试循环；路径/身份/窗口/压缩/换模/时效变化使旧条件失效。模式无关交付、暂停与恢复保护保留。源码与Codex包同步，本地接通不代替自主交接行为验收。
- 8d3e2f9/a5c93304新包已在公开无模型生命周期流程中完成一次受影响复验，外部observe/recheck及两位独立评审四轴输入通过；该a5c93304包的两个SDK子案例重新准入，原包覆盖为2/17，不重复累计旧包结果，也不自动平移到后续指导及回调时序修正包。证据及admission-report.json在私有accord-public-lifecycle-20260914-03目录；1879ae6/99ca350的02原件保留。03有四段forced=true，均在单一恢复期限后归零，14个保护对象保持、五个临时根回收。binding-review明确是事后来源核对，执行前字节/限额由派发及prepare/run-started绑定；首份评审时间格式被拒记录保留，规范化时间后准入无错误。A01–A08整项完成仍0/8，functionalCompletion与candidateEligible均false；默认CLI无外部observer时的空输入不抹去有界证据。
- c390fdf、1879ae6和8d3e2f9均已取得九组合CI全部通过；新增案例入口的[bb6cb95 CI](https://github.com/yiheng8023/YIYUAN-Accord/actions/runs/34861719241)也已通过；9a8e010/cc3243ea的[九组合CI](https://github.com/yiheng8023/YIYUAN-Accord/actions/runs/34871640585)本轮已全部通过。此前上下文接入的125项适配器/检查点、78项开发契约回归及产品/Codex包静态校验通过。bb6cb95另有52项聚焦回归，源码/检查器不新增执行循环。后续清理指导和回调时序修正已改变包身份，当前开发契约、产品及Codex包校验通过；既有结果不代验自主接管或发布资格。既有生命周期中断实例的一份原生资源回执forced=true，其余六份自然退出；七份均进程归零，六个CLI回执完整，不能写成全程自然退出。
- 历史：共享Accord曾按授权停用，已有对话仍可能包含旧指导；当前启用/安装状态见本页最新IDE准备记录。近期两次发布就绪核对使用任务作用域UserPromptSubmit源Hook注入职责及Skill路径；未见完整Skill正文读取证据。此前“完整当前Skill显式投影”的表述不能用于这两例，也不能把源回调当安装态或隐式加载证据。本轮已从原生plugin list与源码核对当前来源：注册Git市场停在cf13486/3.2.1，读取时唯一安装缓存为3.3.0-dev.1且disabled；首次公开观察结束后又发现缓存已变为注册源的3.2.1，停用配置未变。当前来源/缓存选择机制已定位，具体异步触发者尚未证实，不恢复旧缓存。随后9月14日再次读取时，共享缓存已回到13文件3.3.0-dev.1并与a5c93304候选逐文件相符，仍disabled，注册Git市场仍停cf13486；该后续变化不改写03观察窗口内14个保护对象保持的事实，也不能把旧保护文件集合当作当前集合。不反复改共享缓存来准备试验。实际执行前只刷新会影响下一动作的版本、配置和加载事实。
- 用户主动将上下文调至其当前可用上限，并非默认参数。配置值、原生生效窗口、当前占用、累计处理量和效率分别判断，不固化具体规格；观测不足时保守留余量并提前准备接续。
- 9月15日只读回看云环境详情和编辑表单：名称/仓库为YIYUAN-Accord，工作目录`/workspace/YIYUAN-Accord`，三个历史任务保留；universal、自动初始化、Agent网络关闭、缓存开启。未保存、更改、连接终端或启动任务；当前包云端运行仍未验证。IDE的真实普通交付保留当时版本身份。跨项目、远程主机和云端委派/回接分别绑定权限、状态与实际恢复路径。

本轮定位并修复迟到Stop回执占用新轮次续作标记的问题，按原生hostObservation的当前执行轮次核对，既有用户授权epoch不因宿主自动续作而伪造更新。新增三项回归覆盖新用户轮次、同一授权下宿主续作换轮、检查过程中换轮；前两项先复现失败后通过，相关119项检查点和9项上下文回归共128项通过。包内运行文件同步；缺少turn_id仍保持原有处理，不声称已识别未知迟到事件。Interrupt保持保守失效，未把自动续作当新授权。该本地修复不代验真实宿主并发、全程用户介入或自动交接闭环；未新增模型试验。

## 发布前剩余工作（2026-09-15，核对5e225fc）

9月14日c390fdf的35–50%是当时的低置信工程估计，保留在Git历史，不作为当前实时进度。此后已完成三父范围定义、CLI两轮交付、当前安装包的IDE普通交付、市场/Hook就绪与环境名称刷新，并修复Windows输出及迟到Stop问题。已有实现和有效观察继续复用；九个范围已定义不等于九个通过，两个SDK子范围仍绑定原包，A01–A08整项完成仍0/8。

剩余工作分为以下四类；这是现有W01–W08的执行归并，不是新增要求、四次试验或四个串行阶段：

| 剩余工作 | 已有基础与明确缺口 | 收敛方式 |
|---|---|---|
| 入口适用性定案与差异接通 | 三父范围已定义；4项纳入开发、7项待判，最终集合未成立 | 用已核官方来源和本机事实确定具体职责/执行路线，再补实际差异；不能为省额度排除入口 |
| 关键行为与生命周期验收，修正实际缺陷 | 已有普通交付、原生恢复、上下文取数和SDK机制；自主择时交接/接管失败回退、环境变化、模型选路、资源压力及退出仍未完整验证 | 8个未绑定必要范围需补充可反证条件；同一有价值的连贯案例可覆盖相互关联的职责，不按范围数机械增加模型试验 |
| 系统组合与净影响 | 局部效果及失败原件已保留；A01–A07依赖和单一完整组合案例未闭合 | 复用未受影响证据，验证组合与实际负担；不把分散成功拼成同一episode或因果增益 |
| 精确候选与发布收尾 | 当前候选16文件，用户共享安装另有身份；010e5228包的九组合回归及两条原生生命周期CI全部通过；当前164febb2包已补本机热切换机制观察；各入口及宿主升级仍分别核验 | 必要验收成立后完成README/发布说明、精确版本/包及独立复核、发布和公开后态检查 |

这仍是集成、必要验收及可能返工的工作量，不能按只剩打包发布估算。入口集合和关键行为的未知使工时及总完成百分比暂不可靠；没有证据证明核心目标不可行，也没有证据保证剩余范围无需开发即可通过。下一关键动作是闭合可靠上游到普通任务交付，入口适用性与必要差异交叉推进；不重演已完成的小链。

本次全账户普通Codex周额度回读剩7%，重置时间为2026-09-19 16:11（Asia/Shanghai）。这是本次查询快照，非项目专属预算；不能由token或试验数量推算额度消耗，也不能保证在达限前发布。按风险复用现成来源、已有证据和合适的子代理，不以额度压力降低判据、重置用户限制或擅自切换其固定模型。

## 当前路线与下一实际动作

用户补充上游载体不限Skill，关键是整条闭环和不可抗力中断后的状态恢复。已收准计划/基线/验收：可恢复的状态与受控转换是必要职责，具体存储及执行可复用宿主；增加动作效果未知、掉线不等于远端停止、核对后续作及实际恢复唤起的判据。当前代码已具备持久检查点、epoch/revision、暂停、输入丢失与恢复核对保护；这些部分连接不证明断电、远端副作用或全链自动恢复。沿既有主线接通并验行为，不因术语新增统一控制面或重演既有机制试验。

2026-09-15本次回读又发现共享缓存已变回13文件，逐文件等于注册Git源1429f2a；具体触发者仍未知，本轮只读，未替换缓存或改信任。上一轮开始前及清理后16文件的原观察仍成立，不可把任何一个快照写成永久状态。当前会话的旧本地receipt为unbound、无保存checkpoint，缺少可用原输入且待replay/恢复核对；未据此伪造状态或续作许可。当前用户请求、main检出、已有提交和本Root写入职责由当前会话/Git核对，独立文档对齐不依赖该helper的就绪判断；后续插件行为试验必须重新绑定实际加载来源。5eec5d3的CI34959859202目前七项通过、四项运行，结果后续回读，不复测已通过的本地入口切片。

最新普通入口观察未计为Accord通过。公开`observe_codex_entry.py`新增显式`--native-package-hooks`，逐文件绑定完整16文件包及全部6条源Node Hook注册，在持久exec/resume进程中隔离共享配置、插件与Apps载入，保留现有认证位置；默认checkpoint包装路径不变，也不把源投影称作市场安装或Skill隐式注册。每阶段重新核对源文件、原案例请求、执行命令与唯一原生任务身份。

第一次实际五轮使用CLI0.154.0、Terra/medium及原coordination案例，194.595秒完成5/5结构检查，累计211200 tokens、输出4901；五个进程均自然退出归零。但原生记录没有Accord核心指导，源Hook全部失败；另有阶段2在批准后仍保留“待同意后生成”的旧plan.md语义缺陷。因此原result的caseComplete不构成Accord或业务全验通过，未改写失败或修补被验产物。原件、阶段快照、实际执行源码和Root review.json保留在accord-ordinary-entry-20260915-01，不作独立正式准入。

根因在观察器引用命令：固定官方[core调用方](https://github.com/openai/codex/blob/rust-v0.154.0/codex-rs/core/src/session/mod.rs#L4706)传入实际用户shell，本机为PowerShell；先前依据命令引擎默认cmd回退构造带引号绝对Node命令的判断失效。已按实际shell补PowerShell调用符，并绑定shell来源。现有本地固定响应机制分别保留失败/修复后的App Server记录及一次修复后的原生CLI记录，证实完整当前输入指导先于模型动作送达；均零真实模型调用。CLI本身自然退出0，复用的单JSON辅助读取器随后误读exec JSONL的错误原件保留，无重复执行。新增当前轮指导来源检查，缺失时不再继续下一业务阶段；它不证明行为采用。本轮不再重跑真实模型，后续重验须新绑修复后的条件。56项入口回归、21项生命周期回归及开发/产品/Codex包静态检查通过，包字节仍为164febb2；本轮只修改公开观察器及相应反例，代码/测试上限1840000→1860000，文件数及余量要求保持。

原生workspace-write启动只追加了本案例工作区的共享信任项，已核实仅此差异后独占文件回写原始字节；17份共享保护文件恢复到原哈希，原观察的unchanged=false保留。业务工作区完整留存于阶段快照，四份诊断原生历史复制核对后，18个任务临时根和私有配置恢复副本已回收，所属进程归零；共享原生对话未删除或归档。诊断原件位于accord-hook-loading-diagnosis-20260915-01/02/03，保留修复范围与反例。

用户进一步明确原AGENTS.md驱动三Skill的动态设计；本次核对历史b13edc3，确认简单直达、多能力组合和事件驱动重选，并区分常驻意图底线与按需完整Skill。计划、基线、验收及nextBoundary已对齐：Accord自身和宿主连接须在不要求修改用户AGENTS.md的条件下承接整体入口，组件数量不预锁，具体组合按需；共同规范一致性、实际采用与失效恢复沿现有主线推进，不把原设计误写成固定SOP或将通用设计宣称为全面验收。

主线调序后的关联视图已复核：基线页头修正为r25，F01/F02/F03/F06区分历史准入、现有入口实现与当前未完行为；验收明确普通任务链中既有A项的关联，以及默认生命周期4响应/6CLI与可选热更新6响应/8CLI的边界。基线、验收的“工序”链接已指向计划，PROCEDURE仅为历史观察；架构说明补当前startup/clear入口及条件参考，旧无AGENTS探针仍绑定原变体。机器投影nextBoundary已在调序时更新，effectChain/验收边界与未完成标志本轮核对一致；未改功能底线、包/运行字节、案例定义或准入状态，也不因文档同步重跑已验机制。

本轮按用户对工序/优先级/交叉作业的要求重新核对依赖：可靠上游应先闭合，Skill每次必然隐式触发不是前提。当前源码已有startup/普通输入核心职责、一个协调Skill的分层、transcript取数及恢复提示，剩余重点是当前包在真实普通任务中的采用与交付，而非继续堆积局部Hook/生命周期演示。计划已撤掉“先实现transcript接入”等过时下一步，将“可靠上游→必要细节/能力→执行纠偏→交付/连续性”置为主线；入口差异、机制验证、CI及证据回读交叉推进，条件足够的主线不等待全入口穷尽。实际安装候选、共享缓存与源投影仍须分开；必要入口或指导不可用时先修连接，已注入却未遵循时追查行为/反馈断点。

本轮公开观察器新增显式 --hot-reload 可选案例；默认v1仍四响应/六CLI，可选案例六固定响应/八CLI，均七个App资源。升级构建在prepare时冻结为3.3.0-dev.1+codex.hot-reload-observation，本次只改manifest.version，未增加产品字段或测试Hook。实际Windows CLI0.154.0观察48.806秒完成，在同一App Server和同一Accord已绑定暂停任务中，两次外部CLI安装之后直接turn/start，未经额外reload或重新信任；独立inspect从当前轮注入路径、原生epoch/turn、精确安装副本、Hook定义/信任及状态原件确认新根/回退根生效，原暂停checkpoint与历史输入前缀保持。六个本地固定响应、零真实模型调用；七App及八CLI全部自然退出归零，五临时根回收、14个共享保护文件保持，原件保留在accord-hot-reload-20260915-01，inspection.json为pass。该case仅验证已承接任务，不接管其它用户历史，不证明代码/状态格式迁移、同版本原地替换、信任变化或真实宿主升级。

127项运行回归与21项生命周期回归通过；独立源码评审未见阻断问题。Root补证据根先校验再读取变体路径的边界回归。代码/测试上限由1820000调整至1840000以容纳现有公开观察器的可选案例与反例，文件数147和5%余量要求保持，不新增执行服务。19d8f78的CI34947485855中Linux/macOS原生热更新均已通过，独立下载并回读两份artifact：16文件canonical包身份、版本变体、同任务轮次/当前注入、状态/信任及15份资源回执关联成立，POSIX只声明主进程/同进程组释放，计数等未知保留null。readback.json与原件位于accord-hot-reload-hosted-20260915；随后回读该运行的九组合常规回归及两项原生案例均成功，合计11项全部通过。该机制切片可退出主线；未重复运行模型试验。

184bdba3/010e5228的[CI34940336723](https://github.com/yiheng8023/YIYUAN-Accord/actions/runs/34940336723)已回读11项全部通过。用户随后要求检查热更新及宿主升级后的持续生效机制；W02/W05/W06/W07已纳入总体职责，现按固定0.154.0官方源码核实有效插件变化及同任务下一轮的刷新连接。已更正“无跨进程广播”等于“不会自动刷新”的误推：下一轮会发现活动安装根变化并按差异重建Hook；同版本原地修改仍有缓存边界。当前包补宿主重启/升级的Skill触发与生命周期操作指导，运行时字节未改；计划、A06及版本政策同步分开目录、实际采用、信任、原状态和恢复者。

原默认公开生命周期案例仅观察坏候选拒绝、同包重试和重启/卸载后的状态保留；本轮已另绑并完成版本标识变体的同任务热切换与回退，尚未实际切换宿主版本。官方已有同任务外部升级/回退测试，但跳过Windows和remote；其源码/固定版本CI不代验本机、全入口或真实跨版本升级。故结论为机制与职责部分覆盖、必要行为验收未完整。下一生命周期切片优先复用官方版本切换样例及现有公开观察器，补有效新版本的旧任务续作/回退和实际宿主升级恢复；先绑定具体宿主版本与必要差异，不先制造故障或重复安装演示。该热更新观察未升级用户宿主、共享安装缓存、市场来源或信任，也未新建常驻监控器。

用户要求整体影响判断内化为产品能力，并允许借宿主实现。本轮复用同一changeImpactGuidance接入startup/clear、普通输入、resume及compact恢复；Skill入口与验证参考补影响范围、间接依赖和历史结论适用性处置。语义发现由宿主承担，重大未决影响复用原有状态/unresolved完成保护，不另建扫描器或宣称自动发现全部依赖。127项检查点/工具批次和30项开发/分发回归通过；新增回归核对跨入口职责可见、压缩不改状态、局部文件已通过但关联缺口仍不能退役。独立只读评审未见具体缺陷，实际宿主影响判断及关联修正仍需A04/A08行为证据；原988ac64e包运行不改写为其后续包结果。

此前一次预绑的普通只读源码审查已自然完成：eeac355/988ac64e包、CLI0.154.0、Terra/medium、只给一份原始请求，使用临时body_after_prefix=8192条件，478.981秒内经历7次原生压缩和恢复后输出最终报告。原生记录逐次显示恢复Hook保留完整原始请求，8个窗口均为default，无模型调用Plan/Goal或人工续作；11次只读命令未执行被审代码。148份输入及共享配置保持、所属进程归零且无强制退出；协议状态在原生退出后已空，父级初次status因状态已无而失败，未重建或强行退役。原报告无明确职责丢失发现，其“闭环”措辞仅按静态职责覆盖理解，不作行为全验。原生累计241101 tokens（输出3372），有宽泛及重复读取；不据此证明省费、阈值最优、默认环境、裸宿主或跨任务接管。原件与recheck.json保留在accord-auto-continuity-review-20260915-01；147份源码逐一确认可从Git恢复后回收临时工作区，原patch、请求、报告、原生历史和回执保留，未归档任何任务。该有界事实已补同任务连续性证据，未提交整项A05准入；不会为重复这一成功链再跑模型。

2db6291的CI34933499720两条原生生命周期通过，九组合在前置包身份校验失败、未进入常规测试：Root生成adapter.json时工作副本为CRLF，而Git按.gitattributes提交为LF，4d653a73因此只对应当时工作字节。已将工作副本对齐LF，并逐一读取HEAD中16份Git原件确认与工作副本相同后重算当前包身份；原CI失败与4d653a73显式Skill样本保留原身份，不改写为新包执行。无需据此重做模型试验。前一988ac64e包修正后cc7c234的[CI34933986220](https://github.com/yiheng8023/YIYUAN-Accord/actions/runs/34933986220)九组合常规回归及两条原生生命周期已全部通过；Root下载artifact核对16个源文件、安装文件及保护/回收后态，回执保留在accord-entry-layering-native-20260915/readback.json。整项行为验收与发布资格仍未完成。

本轮从已有代码修复一处上游依赖：复用同一个纯entryGuidance，SessionStart/startup与clear在不读取或写入任务状态存储的情况下提供整体职责及同包Skill/helper路径；UserPromptSubmit继续保留原输入失败、水印、暂停和隔离逻辑。第一Hook去掉旧700限制，复用宿主默认输出阈值，单位按官方说明为近似token；未改变模型上下文规格。启动路径在状态位置为普通文件、EACCES及已有失败/暂停条件下得到局部反证核验，126项运行回归通过。它不解决Node缺失、Hook未信任、原生条件下全部失败恢复或自动交接，需要继续按实际入口补接与验收。

一个协调Skill已按明确情境分成入口与三份参考，旧详细段落保留；Root补整体正向协作职责并收窄过宽指针，manifest介绍与实际分工一致。新增referenceFiles和显式startupEntry声明绑定当前变体，冻结历史默认不被重写；参考缺失、未声明、被篡改及无效内容均影响包校验，30项当前开发/分发回归通过。一次预绑的显式Skill只读交接审查正确识别70/100目标漂移、暂停丢失、未回读/未证接管及父任务释放风险，读取入口和两个相关参考；输入保持。此为合成情境的独立使用核对，不代验普通隐式触发、实际宿主操作或收益。源映射及回执保留在accord-skill-layering-20260915。 两个实现工作树及临时分支已在确认cherry-pick等价、无未提交修改后回收；源对照已逐文件核对并移入该证据目录，不留散落的开发副本。

资源调查确认官方已修复部分MCP/异常Hook后代遗留并有条件懒启动，当前0.154源码有原生后台及子任务生命周期控制；正常保留与孤儿/泄漏不同。W06/W07和A06/A07/A08已覆盖更广资源分配与后态，继续复用原生能力并补普通入口持续负载/取消/退出证据，不新增重复常驻执行器。当前源码与IDE/桌面全部构建、实验控制可用性和低配长期曲线分别核验，不能把已有修复称为所有卡顿问题解决。

用户明确接受跨额度重置周期继续，额度不改变功能和质量底线；没有本地Mac不作为搁置macOS/Xcode适配的理由。本轮实际补上公开生命周期观察器的POSIX进程控制，复用原有prepare/run/inspect及固定本地响应，未新增准入层或调度器。已合入7a550b8；Windows相关61项、WSL/Linux65项通过且无skip，四项仅POSIX适用的实进程测试按平台发现，原Windows测试和最低数量不变。开发/产品/Codex静态校验与actionlint通过，插件13文件cc3243ea不变。

CI34927715779的macOS和Linux原生生命周期两job均通过，分别取得7段App Server记录、6条原生命令、4次固定响应；真实模型调用0。安装文件散列与当前13文件完全相同，加载/信任、SessionEnd对照、Interrupt/resume、失败更新保护、卸载及未完状态保存均在既定有界episode中回读。每段直接进程退出0、同进程组已不存在、五个owned root回收、保护文件保持；POSIX的逃逸后代和CPU/RAM不可由此证明，activeProcesses保持unknown，不能平移为Windows Job完整子树或Xcode GUI证据。原始artifact和独立本地回读保存在accord-posix-lifecycle-20260915；上传器默认遗漏source-package/.codex-plugin/plugin.json已如实记录，原artifact未改写，精确Git原件与预绑/安装散列相符；CI已补include-hidden-files以保全后续完整快照。修正后bf00a77的CI34928079618两条原生job也已通过，Root下载并逐文件核对两份artifact均完整保留13文件、与预绑及安装散列一致；独立回读在accord-posix-lifecycle-20260915/corrected/readback.json。另有126项开发/准入回归通过；子代理临时资源、工作树和分支已回收。bf00a77的CI34928079618随后十一job全部通过，含九组合常规回归及两条原生链；这是cc3243ea包的既有结果，不平移为本轮4d653a73候选或发布通过。

下一实际工作先核对修正后的CI回执与资源后态，继续沿官方Xcode专用配置/插件通道和已有macOS原生执行基础补集成差异，兼顾原有连续性及其余范围；没有缺口前不再造执行器。自动连续性scope仍未绑定：现有固定阶段exec/resume不能证明主Agent自主择时交权与失败回退，本轮没有为凑定义数量新增一层事前绑定框架，也没有把主持者推动升级为自主行为。

用户指出“继续至”仍显示harness；已查到VSCode globalStorage中persisted-atom-state/environment保留旧对象。当前客户端仅在选中ID失效时自动替换，因此环境改名但ID未变会留旧名称。GitHub旧/新地址实读均返回仓库1304939826；经用户在现有列表重选Accord并切回本地，环境ID6a870b3542cc819199cff184a2e06f37不变，label、repo name/full_name及clone_url均已回读为Accord。未改数据库、重建云环境、删除历史或请求云任务，具体回执在accord-entry-applicability-20260915/environment-name-refresh.json。此问题已关闭，不再重演；历史harness身份仍按原边界保留。

用户已纠正详情页可打开，当时的旧版本与Hook就绪问题已处理。已查明市场固定在v3.2.1标签，native upgrade无变化符合该固定引用；按用户要求通过原生marketplace remove/add切到精确1429f2a，再plugin add安装当前cc3243ea。13个缓存文件逐项匹配、显示名YIYUAN Accord、版本3.3.0-dev.1。执行前已读到用户将全局选择改为enabled=true，保留此最新选择，不恢复早前停用。IDE后端0.154.0-alpha.6.2回读6Hook，原5项trusted、新增resume/compact项untrusted；仅补该项当前hash后新进程确认6项enabled/trusted，Skill亦enabled。宿主配置API拒绝项目文件写入；模型/权限使用标准项目配置文件，Hook信任按原生规则写用户配置，未扩大到其它插件。用户配置变更仅为本市场ref、测试目录trust和新增Hook信任。

IDE实际小链已完成（01a0a306-22c5-73c0-989e-1f14564eaa3d）：原生source/originator为vscode/codex_vscode、0.154.0-alpha.6.2、Terra/medium、default、on-request/workspace-write，64.487秒自然task_complete。两报告正确且一致，3个原文件SHA保持；实际4次exec只读/哈希/新增两报告/复核。6403字节Accord developer注入与对应输入回执证明UserPromptSubmit实际参与；其它Hook执行及整项资格不据此通过。用户从界面提交的消息多了2个下划线转义及末尾LF，语义相同但不称逐字匹配，原binding不改。独立子代理语义复核通过，精确Goal表0行，累计198953 token（未缓存输入36160、输出2281）。证据在accord-ide-validation-20260915-01/evidence/observed-result.json与冻结原件；Root已通过helper退役无未完checkpoint的该输入回执，并另记维护者收尾，不冒充模型自主清理。用户仍查看的测试目录/项目配置/信任暂保留，待退出该工作区后退役；原生对话不归档。

Windows界面控制入口实际存在于node_repl/@oai/sky，先前从统一浏览器接口限制外推整个会话无桌面控制能力需要撤回。经用户授权VSCode窗口可读可操作；用户已停止自动界面操作并改为手动配合，此后不自动接管。IDE确有插件/Hook界面。两后端导出的426份含实验声明的协议文件逐字节相同，可复用接口代码而不声称行为等价；冗余导出已回收，单份schema与摘要在accord-entry-applicability-20260915/backend-schema。

514cf2e的CI在两个Windows组合的首项报告输出失败，Linux/macOS七组合通过；原因是CP1252无法编码新入口说明中的中文，尚未进入Windows测试阶段。已在CLI入口统一stdout为UTF-8，并以强制CP1252的实际命令复现失败后验证修复：紧凑/缩进报告均保留中文并可解析，8项当前开发回归及另外两条CLI命令通过。未改CI的系统编码或安装环境，运行插件包身份不变；29762ce的新CI34907918984已通过九组合前置产品/投影校验（含两个Windows），九组合现已全部通过。继续入口实际差异与待判条件，既有定义不作运行通过。9月15日只读刷新：Desktop 26.908.4834.0实际带起npm后端0.154.0；VS Code扩展26.908.40401自带0.154.0-alpha.6.2，该次身份观察时Code未运行、共享Accord为disabled、配置为9cd9f34d。其后用户启用并完成上述IDE验证；早前配置不是当前恢复目标，不跨构建借用入口证据。原始身份摘要保存在accord-entry-applicability-20260915/entry-runtime-identity.json。

现有准入的单一entry只表示观察/执行载体，不能证明目标入口集合完整。本轮在v5的三个父范围增加subjectEntries和逐入口事实映射，严格核对候选全集、选定集合、待判状态及来源依赖，保持旧scope/case语义。它是验收漏项保护，不是新执行调度器；12项当前准入回归通过，开发/产品/Codex静态校验通过。Root另与998120a旧实现核对六个旧案例的定义及复用散列，均不变。结构化openCoverage明确输出被验集合，3.3人工文档同步；未保留旧渲染器中对v5不可达的分支。未运行新的业务模型观察。官方来源与本机云设置的限定复核在计划和entrySurfaces中，私有界面回读摘要为accord-entry-applicability-20260915/cloud-settings-readonly.json。

原工序的普通入口核对已执行一次（accord-release-readiness-20260914-01，bb6cb95/a5c93304）：Terra/medium首轮自然completed、exit0、无强制、进程0，耗时130.601秒，累计262068 token（未缓存输入34853、输出3983），在预绑360秒工作/180秒单轮/15秒恢复、400000累计/12000输出内。两份候选报告语义正确，但Agent删除了预存且下一轮仍需的cache.json，自检只核验当前分析的三个输入；检查器正确失败并阻止第二轮派发，原caseComplete=false保持。完整原案例字节及被删输入原文已按原散列另行保全；失败工作区不被改写。原生创建的本任务信任行已在排除其它变化并核对当前版本后移除，配置恢复9cd9f34d；其它保护对象保持，临时state/temp根回收。三次额外记忆查询存在，其中错误的Raw+Select-String管道输出约359KB完整记忆库，成本问题保留，不假定它导致了删除，也不把继承共享指导的本案当严格四文件隔离。当时只修正原件保全指导和未来提示歧义；该失败案不再派发，后续另绑修正后观察，见下段。试验前的全账户周额度查询为剩26%，不归因为本案成本。Goal交接兼容按既有工序处理，未因讨论开启Goal；该原生轮模式default、Goal事件/工具及精确thread的Goal记录均未见。

修正后另绑的一次两轮观察（accord-release-readiness-20260915-02，9a8e010/cc3243ea）已完成：两轮自然completed/exit0、同任务resume、进程0；155.781秒，累计272971、未缓存输入32643、输出5064，均在原限额内。四个输入逐阶段保持，两份报告从candidate-only正确更新到candidate-and-local-installation，旧待办随事实更新；独立语义复核通过。报告评估的仍是8d3e2f9/a5c93304业务快照，不混为执行包。两轮实际default，精确任务Goal表0行；未启用Plan/Goal。任务信任行精确恢复，15个保护对象保持，空state/temp根回收。原始流、两阶段文件、原case与独立复核/后态回执保留。它补上本两轮范围更新和输入保全的实际证据，不增加17项正式通过数，不代验安装态、自主交接或因果增益；不再重复这条小链。

最新校准区分工作分工、子代理执行线程与用户侧独立任务：现有子代理足以承担的并行先复用现有入口和编排，不默认增加顶层任务，也不声称线程多更省。确需独立生命周期/隔离或载体更新时再比较总成本及用户负担；命名、状态跟踪由Agent在授权和宿主能力内处理，创建与归档边界保持。具体参考及验收已同步，不固定并行数或汇总轮数。CLI/App Server/桌面已有执行、子代理、压缩及适用转场操作，不能把“尚未观察到自主采用”写成“没有执行者”；没有具体操作缺口前不另造泛用转场控制器。原生0.154源码和官方文档确认SessionStart/compact在自动压缩后的下一模型请求前投递恢复信息，原连接保留。开发中第一次直接读取了同任务旧转录分支并得到过期；随后从活跃带rollout后缀的分支取得新鲜观察（最近响应边界282595、窗口828400，均为当时值）。当前Hook会保存实际transcript_path，不能仅凭thread id选旧文件或放宽过期条件。该次直接模块观察时共享插件停用，不代验安装态自主行为；后续启用和IDE小链按本页最新事实另记。继续有实益的实现、集成与验收，按实际可用信号及必要状态选择保持、分拆或转交，避免重复已完成的基础演示。

自动交接保持必要交付。本次先完成普通入口的感知连接：复用0.154原生Hook的transcript_path，仅有界读取其当前任务记录，使用最近响应量加来源明确的尾部估计，接入既有容量及恢复余量评估。独立检查未发现新的接线缺陷；9项适配器测试与116项检查点测试通过。历史真实rollout的只读回放仅确认格式适配，没有新模型调用；不将其认定为实时占用、普通自主择时或完整接管。下一步验证普通Agent实际采用信号、提前选择适用宿主转场/续接、多轮补交和安全接管，同时推进剩余入口/父级生命周期/系统收官。新包改变了指导及运行模块，1879ae6的两个准入结果保留原包范围，受影响范围另行核对，不能自动平移为新包通过。

既有运行修复、两次五阶段观察与SDK生命周期过程按PROCEDURE的原日期回查；公开执行器已集成、相关工作树已回收。当前不重做这些基础链，保留其失败、包身份与资源归属限制。

1. **主线：可靠上游与真实普通交付。** 先确认当前候选实际入口、必要运行条件和授权，再完成一项有价值的普通任务，检查核心职责与必要Skill细节在依赖动作前被采用，以及反馈、纠偏、核验和连续性。已有实现不重做；源投影成功不自动代表当前共享安装已更新，固定响应不代验Agent行为。
2. **交叉：入口差异、机制与验收连接。** 三父范围与九项已定义/八项未绑定的实际状态保持，入口最终集合仍未成立。复用既有官方来源解决会影响当前切片的差异；其它适配、必要宿主升级/资源条件和证据回读独立推进，不阻止具备条件的主线，也不凭支线通过宣称主线完成。
3. **汇合：连续工作及系统收官。** 自主交接仍是必要责任，放进真实连续工作的触发、接管、回退与交付中验证。只在上游条件实际成立后合并相关行为证据，完成必要范围、系统净影响和已授权的发布收尾。热更新小链已取得当前包的本机和托管机制证据，不重复试验；原失败、已验结果及未覆盖差异继续保留。

两次观察的原始结果、分阶段快照、业务文件与必要恢复状态均保留。第一次在第二轮核验时被180秒截止，原日志另见总量越界；第二次历时464.182秒，第五轮输出14475超过14000，被进行中监测停止，未产生final或业务修订。五轮均观测到default，限定本thread的Goal数据库查询无记录；这只说明观察条件，不证明完整无模式闭环。两次任务新增信任行均已精确恢复到配置SHA 9cd9f34daa484589141a43c9194d829eeaae3a50e7e4b169a1c2e60ab530f62b，第二次五个Job均归零，临时运行目录及冗余配置副本已回收；原始sharedConfigurationPreserved=false不改写。详见[第二次观察](PROCEDURE-v3.3.md#持久协作第二次观察与当前状态一致性缺口2026-09-14)。

早前拟追加的私有只读收尾模型试验已停止。只做了零模型发现：旧ephemeral线程的thread/read/resume分别返回not loaded/no rollout found；原生readOnly命令可以读取保留回执。未使用标注为云端专用的history恢复参数，未创建新模型任务。不能由这个临时来源不可恢复推断正常持久会话缺少resume。

## 可复用证据及限制

| 已有结果 | 当前使用边界 |
|---|---|
| [原生命令实际恢复](PROCEDURE-v3.3.md#普通提示下的原生文件占用恢复2026-09-14)：包装命令exit0仍识别程序失败，原生补齐、结果复用、八项核验及清理，成品正确 | 90秒截止前无final，原生interrupted；原案failed，不重写文件、不加时追认；通用指导在场，非安装态或完整资源验收 |
| [真实源/目标接管](PROCEDURE-v3.3.md#真实主载体接管与源退出后修订2026-09-14)：源自备状态、目标实读并生成结果、源核验退出后同一目标继续修订 | 末轮超累计处理预算后中断；保留正确效果与原失败，控制器择时不代验自主择时 |
| [调用者修正与新工作区复用](PROCEDURE-v3.3.md#调用者预算修正与新工作区资料承接2026-09-14)：12项离线回归、一次正确的新工作区清单及自然完成 | 私有调用者/更小任务，不证明最小子集、成本因果增益或主载体交权；这些任务不再复做 |
| [真实IDE交付](PROCEDURE-v3.3.md#真实ide普通交付与云端环境更名2026-09-12)、[普通协作与暂停恢复](PROCEDURE-v3.3.md#暂停任务的控制器故障与剩余阶段恢复2026-09-13) | 保留原包、入口、输入与辅助条件；不把API的vscode标签当GUI，也不把控制器恢复当产品自主性 |
| [压力后续做](PROCEDURE-v3.3.md#真实资源失败后的普通接续与成熟能力复用2026-09-13)、[择时失败与竞态修复](PROCEDURE-v3.3.md#普通审阅的择时失败与恢复竞态修复2026-09-13) | 正确缓存、修复及验证可复用；普通自主压力发现/择时仍有缺口，不能只补提示或增预算 |
| [被错过时序的指导修正](PROCEDURE-v3.3.md#真实语义承接与失败时序指导修正2026-09-14)、[前身限定回查](PROCEDURE-v3.3.md#前身交接机制的限定回查2026-09-14) | 当前恢复不能补成已错过的历史条件；已补定位真实自主fork、目标核验及续作链，本轮定向历史追溯结束；撤回成功链未定位的判断，保留因果归属/源卸载限制，见[回溯更正](PROCEDURE-v3.3.md#既有交接判断的回溯更正2026-09-14) |

## 保留的运行边界

开发接管已确认，当前main集成写者为01a09602-a44d-79f2-8ad6-d104863ca7d1；native-continuity与acceptance-driver两条任务工作树已完成集成并删除，子代理已停止写入；源任务已停止写入并释放恢复职责，原main和对话保持。用户IDE工作区及未绑定输入回执仍保留，旧事务不重放。历史已关闭案例不自动作为新运行工作区。

早前源进程关闭沙箱的单次授权已消费，不能再用。现有CODEX_HOME应配适用的已有SQLite状态，避免空数据库触发无关历史回填；宿主正常数据库/缓存活动不等于全局零写。Windows CLI参数、命令API限制及角色权限按实际版本核对；不把请求字段或完成文本当实际效果。原件、唯一必要证据及恢复职责保护后，退役无用副本与任务资源；归档/删除用户对话仍无授权。

需要检查点接口、公开观察器/RPC、输入修订、子代理父转录警告或更早普通任务时，沿历史记录与product/development.json精确来源回查。缺失信息保持未知，先用已有能力建立可行条件；不重新读取或搬运整个历史。
