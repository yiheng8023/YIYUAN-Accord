# Continuation — 2026-07-18

## Repository truth anchor

- Repository: `yiheng8023/agent-autonomy-harness`
- Local workspace: `C:\Projects\agent-autonomy-harness`
- Visibility at bootstrap: private
- Imported source history: `yiheng8023/agent-skills-curated`
- Imported source commit: `a5f4451dcdaf897dc8c17c4a9f5620addcf29181`

Verify branch, status, HEAD, upstream, remote repository identity, and current
files before relying on this handoff.

## Accepted direction

The product authority now covers an agent-neutral autonomy and collaboration
harness spanning instruction carriers, Skills, Hooks, MCPs, Plugins, Apps,
context lifecycle, Git topology, handoff, capability routing, verification, and
resource release. CC Switch remains the operational source/install/update
manager where it is already suitable; the harness should not rebuild it.

## Preserved inheritance

The complete Git history and current capability-curation implementation were
copied into this repository. Existing registry, evidence, Skills, tests, and
verification remain valuable input. Their old repository identity is historical
and does not define the new product boundary.

## First next action

Review `docs/strategy/PRODUCT-NORTH-STAR.md` and
`docs/strategy/RESEARCH-AND-POC-PLAN.md`, then create a scenario/evidence matrix
for the three PoCs before broad external discovery or implementation.

## Current checkpoint — 2026-07-23

The three-PoC scenario/evidence matrix and initial context, Git, MCP, Skill
portfolio, and ablation contracts now exist as uncommitted in-flight work.
The repository is still on `main` at
`55659f30091990f7c589932e0379880de30dc403`, equal to the observed
`origin/main`, with the in-flight files intentionally preserved.

Use these current navigation surfaces before relying on the July 18 first-next
action:

- `docs/strategy/POC-SCENARIO-EVIDENCE-MATRIX.md`;
- `docs/strategy/SKILL-PORTFOLIO-REBASELINE-AND-CLOSEOUT-GATES.md`;
- `docs/strategy/SKILL-ECOSYSTEM-OVERLAP-AND-ABLATION-MATRIX-2026-07-23.md`;
- `docs/cc-switch-3.18-live-drift-and-transaction-gate-2026-07-23.md`.

CC Switch is live at `3.18.0`. The dated 251-row/75-body/176-broken-link
snapshot remains valid historical evidence, but the later
[backend reconciliation event](../strategy/CC-SWITCH-3.18-STALE-ROW-BACKEND-RECONCILIATION-EVENT-2026-07-27.md)
supersedes it for live counts. The user-authorized repair invoked CC Switch's
own `uninstall_skill_unified` Tauri backend for exactly the 176 missing-body
local rows. Current verification is 75 database rows, 75 SSOT bodies, zero
missing-body rows, and zero broken Claude or Codex symlinks. CC Switch returned
to ordinary startup, and its WebDAV status recorded equal local and remote
manifest hashes. This proves current-host reconciliation, not cross-device
restore equality, loader invocation, behavioral value, or portfolio quality.

The repository-authored contracts remain active as provisional comparison
baselines, not as proven permanent value. Their Agents/Codex trees and
CC/Claude trees currently diverge, so do not uninstall or import them until
both versions are backed up and one canonical repository revision is bound.
The admitted long-term self-authored payload count may be zero. Treat all 27
Lark Skills as an atomic dependency cohort.

The local Matt-like pool is a mixed historical snapshot; only the CC `handoff`
payload is currently source-attributed and byte-matched to the verified
official main body. Superpowers `6.1.1` is the historical tested comparison
baseline. The
[2026-07-27 runtime and CC count drift snapshot](../strategy/SKILL-RUNTIME-AND-CC-COUNT-DRIFT-SNAPSHOT-2026-07-27.md)
now establishes the current local Codex plugin cache `6.2.0` as a dated,
runtime-owned source/package baseline: all 14 `SKILL.md` entries match the official
release bytes, while 17 of 72 package files are OpenAI packaging additions or
metadata and 55 match upstream blobs. Keep Superpowers selective rather than
copying it into CC for Codex. Exact bytes, installation, database enablement,
and projection presence still do not prove invocation or behavioral value, and
no first-party head-to-head benchmark supports a general superiority claim.

The
[current Skill evidence reconciliation](../strategy/SKILL-ECOSYSTEM-CURRENT-EVIDENCE-RECONCILIATION-2026-07-27.md)
binds the dated source, runtime, exposure, live-fixture, TDD-admission, and
self-authored-ablation inputs by SHA-256. The only bound Superpowers behavioral
evidence remains `6.1.1`; `6.2.0` is current source/package identity only.
Current Matt and Superpowers `6.2.0` TDD arms were blocked by exact-candidate
execution admission at the dated reconciliation boundary. Newer
[Matt current diagnostic-only admission decision](../strategy/HUMAN-AI-COLLABORATION-TDD-MATT-CURRENT-DIAGNOSTIC-ONLY-ADMISSION-DECISION-2026-07-27.md)
and
[Superpowers 6.2.0 diagnostic-only admission decision](../strategy/HUMAN-AI-COLLABORATION-TDD-SUPERPOWERS-620-DIAGNOSTIC-ONLY-ADMISSION-DECISION-2026-07-27.md)
records now resolve that static repository-governance gate for each exact
candidate and exactly one possible future non-comparative diagnostic. Both
current dispatches remain blocked, neither candidate enters the approved
release inventory, and the self-authored chain has zero live cells. Missing
arms are not zero scores, net-value evidence, or residual-gap proof.

The user-supplied 2026-07-24
[human-AI SDLC research intake](../strategy/USER-SUPPLIED-HUMAN-AI-SDLC-RESEARCH-INTAKE-2026-07-24.md)
is retained as a substantive software-engineering specialization expansion,
not as accepted evidence. Repository truth does not prove that an earlier
retained report was Kimi-produced. The new report's body contains repeated
inline-target mismatches even though its 292-entry bibliography preserves many
useful primary-source candidates. Repair high-impact claims before matrix or
standard use; do not change the mother framework, hard standards, paused
CALIBRATION, or the self-authored Skill chain from this intake alone.
Five high-impact claim groups have now received bounded primary-source repair
in the
[2026-07-27 claim ledger](../strategy/HUMAN-AI-COLLABORATION-HIGH-IMPACT-PRIMARY-SOURCE-CLAIM-LEDGER-2026-07-27.md):
DORA organizational amplification, Stack Overflow trust and rework
self-reports, METR's narrow experienced-open-source-developer productivity
trial, the controlled multi-turn degradation study, and the short-term
AI-assisted skill-formation experiment. They are design inputs for nine
scenario rows only. They are not independent reproductions, causal or
cross-host proof, evidence-state promotion, hard-standard thresholds,
candidate admission, or authority to alter the self-authored chain.
The same bounded intake now records that the report substantively covers
multi-step error propagation, collaboration conversion loss, lifecycle
amplification, and delivery-chain attenuation, but does not provide one
hop-by-hop end-to-end fidelity model. The coverage rebaseline, scenario matrix,
master plan, and acceptance map therefore carry
`acceptance.end-to-end-process-fidelity` as partial. Compression, handoff,
delegation, routing, review, and lifecycle transitions still need falsifiable
per-hop and cumulative-loss evidence; no lossless-chain capability is claimed.
All 14 declared software-lifecycle slices are now named or sampled, but the
machine evidence-grade partition is eight bounded synthetic Agent slices, four
zero-model-calibrated slices, two planned-only slices, and zero live-domain
slices. Coding is a priority and deep experiment surface, not the whole
lifecycle or the whole collaboration problem. Release/change and engineering
management remain planned-only; the requirements-to-maintenance thin slice
remains calibration-only.

Latest bounded checkpoint on 2026-07-27: the additive cumulative-loss
accounting mechanism remains intact, and the current Skill evidence
reconciliation, Matt current diagnostic-only admission, shared Context/Git
snapshot projection, additive receiver delta ledger, MCP observer acquisition
gate, and auto-attach v2 offline amendment are now linked through the program
plan, scenario matrix, acceptance projection, and fail-closed validators. The
MCP protocol has not executed a live paired run. One invalid pre-window
calibration is retained as an authority/prerequisite incident; the v2 injected
offline path removes the unsupported observer `thread/resume` prerequisite
without authorizing a live rerun. All affected program assessments remain
`partial`. After these integrations,
`python -B scripts/verify.py` and `git diff --check` pass; the latter reports
only generated-file line-ending conversion warnings. The latest focused
release/change, rebaseline, scenario-matrix, TDD-readiness, other-CC,
three-lane, cleanup, and program-map set reports 149 passing tests in 3.240
seconds; the release/change protocol contributes 30 tests, including 29
negative rejection cases. The final top-level verifier integration set reports
372 passing tests in 322.027 seconds, and the foreground complete
`python -B -m unittest discover` run reports 1699 passing tests in 465.389
seconds. The earlier retained 1,667-test redirected run remains an invalid
inventory-drift incident and is not reclassified by this later valid run.
This is local in-flight evidence only: HEAD remains
`55659f30091990f7c589932e0379880de30dc403`, equal to observed
local `origin/main` without a network refresh, while the intentionally dirty
worktree remains uncommitted.

The next no-new-authority lifecycle step has now passed its mechanism-only
gate. The
[seven-stage zero-model calibration](../strategy/HUMAN-AI-COLLABORATION-SOFTWARE-LIFECYCLE-THIN-SLICE-ZERO-MODEL-CALIBRATION-2026-07-27.md)
reopens persisted requirements-to-maintenance artifacts, validates multi-input
bindings, independent review identity, G0–G7 non-inheriting synthetic receipts,
and eight cumulative accepted-invariant ledger snapshots. Existing TDD,
release/rollback, incident/migration runner-contract, and cumulative-loss
classifiers are recomputed with positive and negative controls. Incident and
migration now execute exact focused assertion RED, bounded mutation, focused
GREEN, final private oracles, and S0–S3 receipts. Architecture and independent
security review now bind constraint/source/dependency/unknown controls plus
three seeded faults, two benign canaries, exact locators, severity floors,
distinct reviewer role/process, and artifact immutability. The focused
calibration/validator set passes 56 tests; the top-level integration set and
complete repository suite also pass as recorded above.

The capture still has 0 Agent dispatches, 0 model calls, and no observed route.
Its `networkAccessUsed=false`, `gitMutationUsed=false`, and
`externalWriteUsed=false` fields are parent reports, not system-instrumented
absence proofs. It remains `valid-calibration-only`; it does not prove a live
lifecycle, architecture correctness, security assurance, cognitive reviewer
independence, human authority, end-to-end competence, candidate Skill value,
or a broad residual self-authored gap.

The zero-model surface now justifies a separate request for one exact native
`gpt-5.3-codex-spark` low disposable run. The calibration record does not
authorize the paid account-scoped dispatch. Before any run, bind the exact
packet, model route and usage boundary, effective sandbox, parent-observed
event trace, private-oracle isolation, and stop conditions. A native baseline
must precede candidate Skill comparison. Do not infer real Git mutation,
deployment, or MCP lifecycle capability from this evidence.

The Git PoC now has 32 deterministic `GIT-01` through `GIT-05` fixtures plus
one Windows/Git `2.55.0.windows.3` disposable native suite. Exact-base
worktree creation, one fast-forward merge, non-force clean cleanup, and three
dirty/unmerged refusal boundaries were observed only inside Python temporary
repositories. The same host now also reports both source and target paths for
repository-local copy-detection porcelain. A real content-conflict run also
observed `UU tracked.txt`, `HEAD`, `MERGE_HEAD`, the unmerged index, conflict
bytes, worktree registration, and unrelated bytes unchanged across
reobservation. It did not resolve or abort the merge. This proves bounded
logical/content containment, not filesystem zero-write observation; `git
status` may refresh Git metadata. No current-repository topology mutation or
general Git manager is authorized or justified.

## Current open claims

- The Git host trial is now split into independent preflight, create, merge,
  cleanup, and recovery authorization phases. No bound user-repository
  create/denial run has executed. Create permission does not carry merge or
  cleanup authority; failure does not carry recovery-write authority.
- Disposable `GIT-07` evidence now detects one real content-conflict state
  without mutating it. This proves neither conflict resolution/abort safety nor
  interruption, crash, user-repository, remote, or cross-host recovery.
- A 23-fixture Git host-preflight contract now binds the absolute
  repository/worktree locator, dirty-path ownership, parent-observed approval,
  exact attempted command, denial, and later independent re-observation.
  It executes no Git command and proves no bound-repository or native-dialog
  safety.
- A nine-fixture `ABL-GIT-INTERCEPT-01` decision contract now rejects the
  current `git-guardrails` payload's universal native-hook claim,
  `pre-auto-gc` substitution, packaged JSON-script/`pre-push` protocol
  conflation, and one-push-canary scope upgrade. It authorizes no Hook write or
  dangerous command. The repository release transaction is now suspended:
  admission is `recipe-only` / `validated=false`, the approved inventory,
  route, and payload are removed, and projections are rebuilt. The live CC
  copy is unchanged and no command interception has been proved.
- A separate Git read-only preflight envelope now samples a bound repository
  exactly twice with run-bound event identities, ordered timestamps, raw
  porcelain evidence, canonical digests, and local-ref-only freshness. Every
  dirty path remains `unknown` ownership, so the current in-flight worktree
  stops before mutation. This envelope cannot prove approval, creation safety,
  remote freshness, recovery, or cross-host behavior.
- Dynamic on-demand MCP activation and task-end release remain unproven.
- The accepted operating policy is default-off and task-scoped: activate only
  the MCP set needed for the current bounded task or phase, then request release
  at its end. Where a host cannot prove that lifecycle, use a startup/new-thread
  profile or documented idle timeout as a visible fallback rather than claiming
  same-session switching. Treat reduced host responsiveness from an oversized
  active set as a measurable failure mode: compare bounded task/tool latency
  under the same workload instead of inferring benefit from process count alone.
- Codex app-server `0.145.0` now has isolated direct-call and new-thread
  disable/re-enable evidence. A newly created thread reflected the current
  disabled or restored config state. A later no-model-turn attribution trial
  used three independent isolated app-servers and five-second observation
  windows after disable plus reload. All 33 exact process samples retained the
  baseline Sentinel identity, no stop event occurred, and the original thread
  still returned the same PID and instance while status exposed no callable
  tools. On this Windows host, Codex version, Sentinel, and window, reload was
  therefore not an observed immediate-release mechanism for an already-loaded
  runtime. This does not prove that reload never releases, that a later active
  turn or longer window behaves the same, or that arbitrary MCPs are equivalent.
  A separate no-model-turn paired attribution trial used six more isolated
  app-servers across three concurrent repetitions. Each unsubscribe arm sent
  only `thread/unsubscribe`, while its subscribed control sent no host request.
  All three responses were `unsubscribed`; all 66 exact process samples retained
  their six baseline Sentinel identities, no stop event occurred, and every
  post-window call returned the same PID and instance. For this bounded
  five-second host/version/Sentinel window, unsubscribe was therefore not an
  observed immediate-release mechanism. It was not task-end evidence and does
  not prove longer-window, overlapping-owner, lease, or arbitrary-MCP behavior.
  A separate single-Sentinel run observed native idle unload after about 30
  minutes and new-thread recovery. Same-thread on-demand actuation, task-end
  release, overlapping ownership and final-release semantics, leases, stable
  resource benefit, generic crash recovery, and cross-host parity remain
  unproven. Each formal attribution runtime also logged an unauthenticated
  external network attempt; no host probe model turn or request was sent, but
  zero network traffic is not claimed.
- A two-repetition startup-profile comparison observed full tool access,
  allow-then-deny filtering, and full server disable across six isolated runs.
  Filtering retained one Sentinel; disable started none. Startup/new-thread
  profiles are the current bounded fallback, not automatic task-scoped
  switching.
- Two isolated child-exit runs observed one local stdio MCP fail while the
  app-server and separate control alias survived. Same-thread victim recovery
  failed twice; a new thread recovered twice through new exact instances and
  also initialized another control instance. This is a bounded fallback, not
  proactive restart, duplicate freedom, prior-state restoration, or evidence
  for a new supervisor/controller.
- A 22-fixture offline MCP task-lifecycle contract now rejects missing task
  identity, event-order, terminal-zero, concurrent-instance, same-thread
  recovery, control-workload, and bounded-sample evidence. Its concrete
  multi-task trace also rejects duplicate acquisition, cross-task/double
  release, premature server release, leaked leases, missing final release, and
  non-overlapping pseudo-concurrency. It does not start a host or MCP and
  proves no live lease, task-end release, crash recovery, duplicate freedom,
  or resource benefit.
- A separate 27-fixture MCP task-selection contract now refines an upstream
  MCP-class routing decision to the smallest admitted declared candidate set,
  keeps activation authority separate, requires task-or-phase-only activation
  by default, leaves unselected MCPs inactive, and requires a host-bound
  release request plus state-compatible fallback. Persistent activation exits
  this default path for separate evidence and authorization. This is offline selection evidence:
  it neither switches nor releases any MCP and does not prove that the declared
  candidate universe is complete.
- An 18-fixture selection-to-lifecycle skeleton now binds the exact task
  contract, host/version/adapter, selected MCP payloads, release fallback, and
  planned lifecycle-dimension subset while leaving all observations empty and
  all proof counters false. The existing lifecycle scorer remains offline-only.
- A separate 12-fixture
  [same-thread refresh parent-event adapter](../mcp-same-thread-refresh-evidence-contract-2026-07-24.md)
  now binds `sameSessionSwitching` to exact host/schema/adapter identity,
  one-key config digests, ordered parent-captured bytes, an active turn, and a
  direct-call pair on one thread. It rejects status-only and reload-accepted
  overclaims, but has not executed a live host transition. The separate
  reload-attribution trial started no `turn/start`, so it does not satisfy or
  upgrade this active-turn adapter.
- Context telemetry and automatic thread creation vary by host.
- An 11-fixture
  [context-pressure provenance envelope](../context-pressure-provenance-evidence-envelope-2026-07-24.md)
  now requires exact host/version/profile/run/time binding and SHA-256-backed
  host evidence before CTX-03 may consume a signal. It remains offline and
  proves no live telemetry, threshold, compression, or thread creation.
- A twelve-fixture context-pressure advisory now separates direct counters,
  host events, heuristics, user observations, and unknown evidence. It has no
  universal percentage and stops at `WAIT` without explicit thread authority
  plus a prepared CTX-04/05 packet. It does not prove live counter delivery,
  compression quality, optimal timing, or automatic thread creation.
- A seven-case CTX-04/05 pre-dispatch freshness gate now binds the exact trial
  contract, rebuilds the canonical
  public prompt and stale assertions and resamples the packet's declared source
  hashes plus local Git truth. One newly built baseline packet matched an
  immediate current-worktree recheck. This is a read-only instant-in-time
  source-byte/builder-consistency check, not source-semantic freshness, an
  atomic creation transaction, receiver recovery, model, loader,
  automatic-thread, or live-remote proof.
- A
  [Context/Git snapshot projection contract](../context-git-snapshot-projection-contract-2026-07-27.md)
  now makes the continuation builder consume the shared Git observer exactly
  once instead of maintaining a weaker second parser. Disposable tests preserve
  rename/copy paths, detached and no-upstream states, multiple worktrees, path
  spaces, and local-ref-only freshness, and reject live-remote or unknown
  ahead/behind promotion. This proves local projection parity only, not an
  atomic thread transaction, receiver recovery, filesystem zero-write behavior,
  or live remote freshness.
- An additive
  [receiver delta ledger](../context-handoff-receiver-delta-ledger-evidence-2026-07-27.md)
  now re-runs the unchanged canonical `ABL-CTX-HANDOFF-01` scorer across sixteen
  deterministic falsification cases and records exact omission, change,
  provenance, stale-fact, unsupported-claim, repository-drift, opacity, and
  failure-code deltas. It binds the packet, artifact, raw response, private
  oracle, source manifest, and shared-Git before/after digests and rejects
  private-oracle leakage. This remains parent-recomputed offline accounting; it
  does not prove receiver recovery, a fresh session, Skill invocation,
  losslessness, atomic creation, AGENTS adherence, weak-Agent behavior, or
  cross-host parity.
- AGENTS/rules compliance must be tested across Agents.
- A fourteen-fixture CTX-07 instruction-carrier contract now separates file
  visibility, host discovery, exact task-bound loading, private-oracle
  adherence, hard-standard outcome, and host approval. Synthetic evidence and
  a single-host pass cannot prove cross-Agent parity or universal AGENTS/rules
  adherence. The formal weak-Agent gate remains actual Spark/low; Terra/low is
  diagnostic only.
- The CTX-07 live-run preflight now has a read-only public/private packet
  builder. It binds the exact carrier identity and digest, withholds the
  canonical private oracle, and blocks when an exact task-bound loader event
  cannot be captured. No host task, loader observation, weak-Agent run, or
  cross-host comparison has executed.
- `HND-FRESH-01` now has its own source-backed `handoff` loader preflight bound
  to the fixed canonical Batch-01 protocol and exact payload hashes. Its
  capture-capability allowlist is now repository-resolved and empty: a
  caller-supplied reference or shape-valid digest cannot promote `available`.
  A real host/parent capture interface must be observed and separately admitted;
  even admission would not prove a loader event, fresh-session invocation,
  artifact production, receiver recovery, or weak-Agent acceptance.
- A 2026-07-28 Codex CLI 0.145.0 explicit-cue/control probe observed the
  source-backed contract's `Suggested Skills` section only under `$handoff`.
  This is behavior association only: the host emitted no task-bound loader
  identity/digest event and reported Skills context-budget omission. The
  formal preflight remains
  `blocked-missing-handoff-loader-observability`; fresh-session invocation,
  implicit activation, and canonical Arm C remain open.
- `HND-FRESH-01` is now explicitly mapped to the stronger existing
  `ABL-CTX-HANDOFF-01` producer/receiver contract rather than receiving a
  duplicate scorer. Its five axes are producer integrity, host exposure,
  loader invocation, receiver repository truth, and creation mode. This mapping
  is local design evidence; no fresh-session loader invocation has executed.
- Cross-device equality after CC Switch WebDAV sync remains open.
- CC Switch `3.18.0` isolated uninstall/restore fixtures passed, and source
  review established per-item disable/uninstall semantics. Live GUI execution,
  stale-row reversible recovery, batch cleanup, hash refresh, and projection
  rebuild remain unproved on this host.
- The full five-arm weak-Agent Matt/Superpowers/repository-chain comparison
  remains unexecuted, but bounded subset live evidence now exists. Batch 01 contains three
  native versus historical Matt `disciplined-coding` runs per arm with a mixed
  process result. Batch 02 contains three native versus installed historical
  `diagnose` synthetic-incident pairs: both arms passed the functional oracle
  3/3, while strict process passed native 0/3 and `diagnose` 2/3. The latter is
  an association only; loader invocation and instruction delivery remain
  unproved for the exact installed candidate, and no preference, portfolio
  mutation, residual-gap, self-authored-change, production, or cross-host claim
  is allowed. A separate
  three-pair synthetic canary assay now proves body-only project-Skill content
  reached the model through the bound Codex app-server mechanism. It is not an
  independent loader event or exact installed-`diagnose` delivery proof.
  Identical Batch 02 and canary repetitions stopped. Batch 03 then ran three
  valid disposable source-pinned current-Matt versus historical Superpowers
  `6.1.1` pairs after
  retaining one classifier-invalid guard pair unchanged. Both candidates
  passed visible tests 3/3. Full hidden contract passed current Matt 1/3 and
  historical Superpowers `6.1.1` 2/3; complete strict process passed 1/3 each.
  Matt twice missed its own 3-5-hypothesis claim; historical Superpowers
  `6.1.1` once violated the shared evidence
  schema and once left no observable red-before-green command. The mixed result
  favors neither candidate. Exact project selection is proved, but independent
  loader events, candidate-specific body delivery, causation, full-Superpowers
  orchestration, preference, portfolio, production, and cross-host claims
  remain blocked. Identical repetitions stop; the next step must use a
  materially different falsifiable software-lifecycle scenario or a
  treatment-fidelity probe.
- The requirements/domain challenge has now completed three valid Spark/low
  native-disabled versus exact-selected CC `grill-with-docs` pairs. Visible
  tests passed `0/3` versus `1/3`, but the independent hidden contract passed
  `0/3` in both arms. Hidden failure-code occurrences were 22 versus 24, and
  each arm produced one forbidden status-promotion run. Exact exposure and
  structured Skill input acceptance are proved; loader invocation,
  candidate-specific delivery, added value, harm, preference, product
  discovery, requirements completeness, cross-host value, and a self-authored
  residual gap remain unproved. Nine invalid environment or measurement runs
  remain recorded and excluded. Stop identical repetitions and move to a
  materially different requirements or discovery scenario.
- The 2026-07-28
  [semantic authority reconciliation](../strategy/HUMAN-AI-COLLABORATION-SEMANTIC-AUTHORITY-LAYER-RECONCILIATION-2026-07-28.md)
  accepts durable domain language and sparse decision records as a
  cross-lifecycle state plane, while keeping grilling selective rather than
  mandatory. Current Matt upstream is a `grilling + domain-modeling`
  composition behind a thin `grill-with-docs` entry; the exact CC treatment
  remains a historical adapted monolith with neither primitive installed
  separately. The existing requirements protocol remains byte-bound; no CC
  update, replacement, superiority, lifecycle-continuity, or self-authored-gap
  claim is made.
- The 2026-07-28
  [semantic-authority continuity protocol](../strategy/HUMAN-AI-COLLABORATION-SEMANTIC-AUTHORITY-CONTINUITY-PROTOCOL-2026-07-28.md)
  freezes the next `SEM-03` comparison as native versus exact local adapted
  monolith versus exact current Matt composition. It uses four fresh-thread
  lifecycle phases and common hard gates so conversational memory cannot hide
  semantic loss. The disposable fixture and parent-private oracle now cover one
  positive bundle and seven injected faults. Current-composition execution
  admission remains partial: all eight raw Git-blob files, MIT license,
  dependency inventory, static security, portability limits, and Windows EOL
  conversion are now bound, but isolated dependency-complete exposure is not.
  Model dispatch, CC mutation, portfolio preference, and a self-authored arm
  remain unauthorized by that record. The first continuation action is the
  isolated raw-byte exposure preflight.
- The next distinct lifecycle slice is pre-registered as
  [maintenance/migration Batch 01](../strategy/HUMAN-AI-COLLABORATION-MAINTENANCE-MIGRATION-PROTOCOL-BATCH-01-2026-07-24.md).
  It proposes a synthetic v1-to-v2 compatibility fixture with four distinct
  affected-consumer states and a Spark/low disabled-versus-selected comparison
  for CC `deprecation-and-migration`. The local 12,510-byte Skill is an adapted
  derivative, not the exact 9,000-byte pinned upstream blob; the adaptation
  diff, fixture, private oracle, classifier, and exact exposure were resolved
  before live execution. Three valid Spark/low pairs now pass visible tests in
  both arms; the hidden contract passes native 3/3 and the exact-selected CC
  candidate 1/3. Five earlier measurement-invalid runs remain hash-pinned but
  excluded. The durable
  [live comparison](../human-ai-collaboration-maintenance-migration-live-comparison-batch-01-2026-07-24.md)
  proves only a bounded native association on this synthetic fixture: there is
  no independent loader event, candidate causation, general preference,
  production migration, removal readiness, or portfolio action. Identical
  repetitions stop. The security/verification slice remains
  deferred because the suitable Codex Security capability is a proprietary
  always-enabled system Skill and the current host has not proved a clean
  disabled control; it must not be vendored into a project projection.
- A 2026-07-24 authenticated CLI recheck now has two successful ephemeral,
  read-only, no-tool route probes: `gpt-5.3-codex-spark/low` and
  `gpt-5.4-mini/low`. This supersedes the dated current-login block without
  rewriting the historical 2026-07-23 preflight. It does not execute a formal
  arm: the event stream exposed no independent model/reasoning fields, and the
  two-percent Skills context budget omitted 130 additional Skills for Spark
  while shortening Mini's descriptions. Task-scoped selected/unselected Skill
  exposure was not proved by those CLI runs.
- A subsequent 2026-07-24
  [app-server exposure probe](../codex-app-server-task-scoped-skill-exposure-evidence-2026-07-24.md)
  now proves the narrower self-authored-disabled control condition on Codex
  `0.145.0`: the same 111 identities were observed while all 105 user Skills
  changed from enabled to disabled and all 6 system Skills remained enabled
  through one-off process configuration. An ephemeral thread independently
  reported exact `gpt-5.3-codex-spark/low`, both instruction sources,
  read-only/no-network sandbox, and approval `never`, then returned one exact
  marker with no command, file, MCP, dynamic-tool, collaboration-agent, or web
  item. Global config and repository-status digests were unchanged. This is
  exposure/control-plane evidence, not a five-arm result. The later
  selected-Skill exposure preflight closed that narrower setup condition but
  supplied no behavioral credit.
- That selected-exposure preflight has now also passed for the exact CC
  Switch-managed [`grill-me`](../codex-app-server-selected-skill-exposure-evidence-2026-07-24.md)
  path. It was the only enabled user Skill; the other 104 user Skills were
  disabled and the 6 system Skills retained their state. Spark/low thread
  metadata was exact, but no `turn/start` or model request was sent. Therefore
  loader invocation, instructions reaching the model, trigger behavior, value,
  and superiority all remain open. A behavior-bearing comparison still
  requires exact current-candidate execution admission and candidate-specific
  treatment fidelity or an explicit unknown; it is not a five-arm launch. The
  generated `0.145.0` v2 protocol has a
  typed Skill input but no dedicated Skill-loader-completed notification, so
  accepted input and behavior-consistent output must remain distinct from
  independently observed loader proof.
- The same [CLI/MCP diagnostic](../codex-cli-model-route-and-mcp-startup-diagnostic-2026-07-24.md)
  records two yellow lines for one visible `sites-design-picker` failure, plus
  a separate background remote Plugin-service MCP transport failure. The
  injected picker is absent from static `codex mcp list`; no config, Plugin,
  MCP, account, credential, or restart mutation was made. A shared root cause,
  persistence across clean restarts, and a verified fix remain unproved.
- A separate nineteen-case attribution fixture now rejects crediting hard
  standards to Skills, treating installed files or Agent self-report as
  invocation, conflating handoff producer integrity with receiver quality,
  expanding `ask-matt` into a top-level router, upgrading local verification
  into cross-domain closure, and treating Terra/low as Spark/low acceptance.
  All four five-arm rows plus one offline resume-correction composition probe
  now have nine shared deterministic packets, private-oracle scoring, and
  nineteen positive/negative examples. The engineering pair separates
  visible-test success from a parent-observed hidden regression and rejects
  main/unrelated mutation. The composition probe preserves completed progress
  while invalidating an undispatched stale Superpowers SDD brief after a newer
  user correction; it is not a sixth live arm and does not prove runtime
  subagent interruption or net value. None is a live weak-Agent result.
  Spark/low routing and task-scoped selected-Skill exposure are already
  observed and must not be repeated as missing gates. Another comparative run
  requires exact current-candidate execution admission, candidate-specific
  treatment fidelity or an explicit unknown, and a materially different
  scenario with a frozen hard oracle.
- A separate fifteen-fixture parent evidence envelope now requires exact
  packet/payload/response digests, task-scoped selected and unselected Skill
  exposure, loader events, private-oracle results, repository before/after
  truth, and three distinct run/thread/task identities. It remains synthetic
  contract evidence; no five-arm live comparison or weak-Agent acceptance has
  executed.
- A 2026-07-24 [source-lineage and collision index](../skill-source-lineage-collision-index-2026-07-24.md)
  derives eight groups and 56 logical Skill identifiers from seven existing
  dated records without reading live CC Switch, Agent Home, or Skill bodies.
  It keeps the source-backed `handoff`, historical Matt-like mappings, contract
  root collisions, Lark cohort, the reconciled three-sample CC cohort, Matt
  samples, Superpowers local hashes, and runtime/plugin aliases distinct. The
  selected cohort proves bounded historical content ancestry for all three
  samples and exact CC-to-repository adapted-payload equality for two; it does
  not prove the producing CC source row or installation transaction. It proves
  no current loader state,
  behavioral equivalence, superiority, residual gap, migration, or deletion
  eligibility.
- A 2026-07-24 [read-only host-transaction revalidation](../skill-ablation-host-transaction-revalidation-2026-07-24.md)
  found that the prepared global Codex config SHA-256 has drifted while its
  length and `skills.config` count, all six target Skill hashes, and the exact
  backup-absence precondition still match. The 2026-07-19 transaction is
  blocked for re-intake; no config content was recorded and no config, backup,
  restart, task, loader, or model action occurred.
- That historical global transaction is no longer the preferred route for this
  Codex host. The process-scoped app-server probe supplies the baseline
  exposure condition without silently updating or executing the old
  transaction.
- A 2026-07-24 [closeout cleanup-debt preview](../closeout-cleanup-debt-preview-2026-07-24.md)
  retained its original identity and was re-observed on 2026-07-27. It now
  inventories 35 exact repository-local `.tmp` roots, 2,459 files, 811
  directories, and 60,346,279 bytes, including the six
  bounded TDD schema/pilot/formal evidence roots and four later
  initial process-fidelity schema, v1 calibration, v2 zero-dispatch, and
  summary entries plus the first invalid v2 smoke and corrected zero-dispatch
  pairs, the valid replacement smoke pair, and four paired
  thread-unsubscribe calibration/formal host roots plus five excluded
  multi-connection subscription-preflight calibration roots and the exact
  invalid pre-window creator-connection-close calibration host/workspace
  roots, without reading retained runtime or research-archive content. The
  added unresolved process root retains the failed 1,667-test full-suite log
  that exposed exact-root inventory drift; it is not a valid full-suite result.
  The
  retention classes are 16 authoritative-evidence roots, 11 invalid/excluded
  attempt roots, 7 authority-unresolved process roots, and one user-source
  preservation root. Fifteen contain SQLite-like state; the user-restored Kimi
  research ZIP preservation root is retained. The legacy
  `C:/Projects/agent-skills-curated` workspace is a protected, unscanned,
  aggregate-external boundary with no archive, move, or deletion authority.
  All remain cleanup debt. The preview does not
  authorize deletion, archival, migration, commit, push, or broad recursive
  cleanup and is not program closeout evidence.
- A 2026-07-26
  [dedicated read-only claim trial](../human-ai-collaboration-read-only-claim-live-comparison-2026-07-26.md)
  now records three formal native `gpt-5.3-codex-spark/low` runs for
  `GEN-RESEARCH-01`. All 105 configurable user/repository Skills were disabled;
  the observed model, low effort, no-fallback, read-only/no-network,
  configuration, tree, and zero-tool-item boundaries passed. All three runs
  returned C3 as `contradicted` while the v1 private oracle required `unknown`,
  and `unsupportedConclusionCount` was 1, 3, and 3. A later v1 calibration
  dispatch produced the same C3 pattern and count 3. The
  [calibration-abort record](../strategy/HUMAN-AI-COLLABORATION-PROCESS-FIDELITY-V1-CALIBRATION-ABORT-2026-07-27.md)
  now establishes that v1 did not uniquely define state entailment,
  unsupported-conclusion counting, or exact source-set completeness. All four
  v1 outputs are therefore measurement-ambiguous and ineligible for Agent
  comparison; they remain bounded host, route, tree, configuration, and
  observed tool-item diagnostics only. The newest cohort stopped after one
  dispatch and the remaining v1 tasks were cancelled.
- A 2026-07-26
  [new-feature TDD preregistration](../strategy/HUMAN-AI-COLLABORATION-NEW-FEATURE-TDD-PROTOCOL-2026-07-26.md)
  binds three primary weak-Agent arms: native, Matt `tdd`, and selective
  Superpowers 6.2.0 `test-driven-development`. The local Matt checkout at
  `9603c1c…` is navigation only; read-only GitHub evidence pins current remote
  `main` at `ed37663…` and exact file hashes. The protocol pre-agrees the
  public seam, excludes both full development frameworks, neutralizes their
  refactor-stage conflict, and requires a parent-observed valid RED before
  production plus mutation-sensitive Agent tests. A dependency-free fixture
  builder and seven normalized offline timeline cases now pass. Exact Matt and
  Superpowers project projections and their current-host configurable metadata
  selection also pass a no-thread/no-turn
  [exposure preflight](../strategy/HUMAN-AI-COLLABORATION-NEW-FEATURE-TDD-EXPOSURE-PREFLIGHT-2026-07-26.md).
  This does not prove Skill-body delivery, an independent loader event, or
  value. A subsequent non-scored
  [raw-item pilot](../strategy/HUMAN-AI-COLLABORATION-TDD-RAW-ITEM-PILOT-EVIDENCE-2026-07-26.md)
  separated measurement validity from Agent performance: one sealed Codex
  0.145.0 trace normalized under the parent classifier while the Agent's TDD
  process was rejected, and a second trace with a potentially opaque failed
  write command failed closed. This proves bounded current-host raw-event
  normalization only. The parent-owned hidden oracle and seven mutants are
  executable, and the integrated
  [first formal-runner attempt](../strategy/HUMAN-AI-COLLABORATION-TDD-FORMAL-RUNNER-FIRST-ATTEMPT-EVIDENCE-2026-07-26.md)
  completed on the native arm. It is invalid and not counted: final visible
  and hidden behavior passed, but a focused GREEN after production was not
  observed and only 5/7 mutants were killed. The subsequent
  [capped native batch](../strategy/HUMAN-AI-COLLABORATION-TDD-NATIVE-FORMAL-ATTEMPT-BATCH-2026-07-26.md)
  froze three native Spark/low attempts. Every visible and hidden suite was
  green, but every ordered TDD process was rejected; r2 was additionally
  measurement-invalid, and only r3 killed 7/7 mutants. Complete failure
  signatures were heterogeneous and valid repetitions remain zero. The native
  arm is paused at its attempt cap; without a valid native baseline, Matt and
  Superpowers are not eligible as formal comparative arms. At most one
  separately preregistered, non-scored and non-comparative feasibility and
  trace-observability diagnostic per exact treatment projection may follow.
  It cannot establish delivery, causation, preference, acceptance, or
  superiority.
- That bounded route is now explicitly preregistered in the
  [TDD non-comparative treatment diagnostic protocol](../strategy/HUMAN-AI-COLLABORATION-TDD-NONCOMPARATIVE-TREATMENT-DIAGNOSTIC-PROTOCOL-2026-07-26.md).
  It permits at most one independent dispatch per exact candidate and produces
  no score, ranking, winner, or portfolio decision. A newly surfaced governance
  gate currently blocks both task turns: the current Matt `ed37663…`
  three-file projection is not the byte-distinct approved adapted TDD release
  payload, while Superpowers 6.2.0 has no repository-approved release entry.
  A dated
  [source/governance preflight](../strategy/HUMAN-AI-COLLABORATION-TDD-NONCOMPARATIVE-TREATMENT-DIAGNOSTIC-SOURCE-GOVERNANCE-PREFLIGHT-2026-07-26.md)
  revalidated Matt current-main bytes, local Superpowers bytes, Codex 0.145.0,
  and projection/normalization tool hashes without starting a thread or turn.
  The
  [exact-candidate admission-gap audit](../strategy/HUMAN-AI-COLLABORATION-TDD-EXACT-CANDIDATE-ADMISSION-GAP-AUDIT-2026-07-26.md)
  now records pass, partial, blockers, and minimum remaining gates for both
  exact candidates; it admits or rejects neither candidate. The later
  [Matt current diagnostic-only decision](../strategy/HUMAN-AI-COLLABORATION-TDD-MATT-CURRENT-DIAGNOSTIC-ONLY-ADMISSION-DECISION-2026-07-27.md)
  admits only the exact pinned Matt projection as governance input to one
  future diagnostic. It does not authorize materialization, execution, a
  model request, or dispatch and does not add a release entry. The parallel
  [Superpowers 6.2.0 diagnostic-only decision](../strategy/HUMAN-AI-COLLABORATION-TDD-SUPERPOWERS-620-DIAGNOSTIC-ONLY-ADMISSION-DECISION-2026-07-27.md)
  now admits its exact two-file local projection at the same governance level.
  Both remain blocked on fresh source/toolchain, materialization, ledger,
  runner, app-server, and current-task authority gates. A separate
  [current self-authored TDD treatment audit](../strategy/HUMAN-AI-COLLABORATION-TDD-CURRENT-SELF-AUTHORED-TREATMENT-GAP-AUDIT-2026-07-27.md)
  found no symmetric repository-authored treatment: the three-contract chain
  is phase control, the approved adapted TDD payload is Matt third-party
  lineage, and runner/ledger components are infrastructure. This is an
  experiment-identity/attribution gap, not a residual functional gap,
  self-authoring need, or creation authority. Metadata selection
  and structured-input feasibility must not be promoted to body delivery,
  independent loader invocation, causation, or value. The one-dispatch and
  no-replacement rules are not live runtime enforcement. An
  [offline dispatch identity ledger PoC](../strategy/HUMAN-AI-COLLABORATION-TDD-NONCOMPARATIVE-DISPATCH-IDENTITY-LEDGER-POC-2026-07-26.md)
  validates the isolated hash chain, identity state machine, torn-tail failure,
  same-process contention, missing-construction-outcome classification, manual
  retain-consumed reconciliation, and a two-authority same-candidate
  counterexample. It does not consume the protocol, validate an admission
  record, or integrate with the formal runner at that evidence stage.
  The subsequent
  [dispatch authorization adapter PoC](../strategy/HUMAN-AI-COLLABORATION-TDD-NONCOMPARATIVE-DISPATCH-AUTHORIZATION-ADAPTER-POC-2026-07-26.md)
  now makes the ledger's intended public entry document-bound. A synthetic
  valid bundle passes, while the actual current protocol and dated preflight
  fail before reservation. No real admission or fresh dispatch preflight was
  created. A subsequent
  [no-model runner preflight PoC](../strategy/HUMAN-AI-COLLABORATION-TDD-NONCOMPARATIVE-RUNNER-PREFLIGHT-POC-2026-07-26.md)
  now records and file-fsyncs a reservation before calling an injected fake
  factory, resolves the ledger only through a digest-bound synthetic authority
  document, freezes the five authorization documents in one immutable envelope,
  and exposes no caller-selected ledger path. Post-envelope document-path drift
  cannot change reservation authorization. Successful construction appends
  `construction-succeeded` before returning the handle, and thread binding
  rejects before that event. A required injected structured handle validator
  checks once and explicitly rejects falsey synthetic handles. Factory or
  validation failure cleans same-process registered resources once in LIFO
  order. If failure append and cleanup both fail, even a hostile factory
  exception remains primary and secondary errors are attached best-effort.
  Ambiguous success append uses durable ledger readback; no durable outcome is
  exposed as `reserved-without-construction-outcome`. Manual reconciliation is
  limited to retaining the reservation as consumed with no release or retry;
  the next dispatch still rejects before factory construction. Two
  protocol-selected ledger authorities can each reserve the same candidate
  once, so the current cap is `protocol-selected-ledger-local`, not
  system-global.
  The actual current document bundle rejects at the protocol-eligibility gate
  before authority resolution or callback, and the later preflight-freshness
  gate is not dynamically reached. The wrapper is not integrated with the
  formal runner or a real app-server factory, and no live ledger authority is
  configured. The unit-tested authorization-envelope-to-reservation
  document-drift window is closed, but live source-snapshot-to-factory
  materialization freshness, a real app-server handle adapter, real
  child-process or socket cleanup, cleanup after process crash, cross-process
  exactly-once cleanup, cross-process exclusion, crash recovery, and power-loss
  durability remain unproved. Live transition therefore remains blocked even
  if a later governance review admits a candidate.
- A
  [current TDD execution-readiness reconciliation](../strategy/HUMAN-AI-COLLABORATION-TDD-CURRENT-EXECUTION-READINESS-RECONCILIATION-2026-07-27.md)
  now confirms that both exact identity-bound diagnostic admissions satisfy
  only the static repository-governance gate. The current protocol still has
  no eligible candidate or live ledger authority, the dated source/toolchain
  preflight is not fresh for dispatch, and the static decisions are not the
  adapter's short-lived dispatch envelopes. The formal runner is not integrated
  with the adapter, ledger, or runner preflight and materializes before those
  controls. Atomic source-snapshot materialization, real app-server resource
  ownership and cleanup, cross-process exclusion, and crash recovery remain
  unproved. Both candidates stay metadata-only; no weak-Agent diagnostic,
  candidate materialization, app-server start, or model request is authorized.
- The
  [TDD non-comparative dispatch successor contract v2](../strategy/HUMAN-AI-COLLABORATION-TDD-NONCOMPARATIVE-DISPATCH-SUCCESSOR-CONTRACT-V2-2026-07-27.md)
  now preregisters a pure offline fail-closed successor without rewriting the
  historical protocol or evidence. Its current decision remains `NO-GO`; the
  builder authenticates neither authority, clock, nor toolchain and does not
  authorize candidate materialization, app-server start, model dispatch,
  formal acceptance credit, or portfolio mutation. A future separately
  authorized slice must establish one shared ledger authority before fresh
  snapshot and preflight capture, then receive a separate independent grant.
- The
  [CC Switch Lark seven-Skill update event](../strategy/CC-SWITCH-LARK-SEVEN-SKILL-UPDATE-EVENT-2026-07-27.md)
  records the later user-authorized live update of `lark-apps`, `lark-base`,
  `lark-calendar`, `lark-doc`, `lark-drive`, `lark-slides`, and `lark-task`.
  All seven current `SKILL.md` entrypoints matched `larksuite/cli` `HEAD` at
  the observed commit, and both Claude and Codex projections still target the
  CC SSOT. This does not prove whole-tree equality, invocation, or value. The
  The later
  [stale-row backend reconciliation event](../strategy/CC-SWITCH-3.18-STALE-ROW-BACKEND-RECONCILIATION-EVENT-2026-07-27.md)
  resolves the 176-entry current-host projection debt. Composio discovery 404,
  cross-device restore equality, deduplication, and portfolio-quality review
  remain separate open gaps.
- The
  [release/change zero-model protocol](../strategy/HUMAN-AI-COLLABORATION-RELEASE-CHANGE-ZERO-MODEL-PROTOCOL-2026-07-27.md)
  and
  [candidate preflight](../strategy/HUMAN-AI-COLLABORATION-RELEASE-CHANGE-CANDIDATE-PREFLIGHT-2026-07-27.md)
  now convert `SE-RELEASE-CHANGE-01` from an unstructured planned gap into a
  frozen repository-only negative control. The native arm and the approved
  `ci-cd-and-automation` and `shipping-and-launch` candidates have exact
  repository source, license, admission, and overlap checks, but current CC
  presence, exposure, loader delivery, and behavior remain unknown. Fifteen
  release evidence codes are deliberately missing; therefore the only valid
  decision is `preparation-only-no-go / NO-GO`. No model, candidate, CC
  Switch, network, Git, release, or rollback action ran, and no competence,
  value, preference, release-readiness, cross-host, or residual-gap conclusion
  is authorized.
- Process loss is now a machine-enforced cross-cut for all thirteen
  human-AI collaboration scenarios. Each row references
  `XCR-01-process-fidelity-and-loss`, and the matrix defines measurable
  invariant survival, omission, added-assumption, provenance, authority,
  latency, amplification, recovery, and rollback terms. This remains
  governance and protocol structure, not end-to-end evidence; the first live
  process-fidelity PoC must preregister its edges, private oracle, thresholds,
  stop condition, and last trusted recovery anchor.
- A 2026-07-26 deterministic multi-hop injection PoC now exercises that
  contract with a preserved control, one-hop detection and source-backed
  recovery, undetected `13/9` amplification, and an opaque-edge stop. It proves
  only the synthetic evaluator and fail-closed boundary; live Agent,
  automatic-compression, fresh-thread, cross-host, and Skill/Hook superiority
  claims remain open.
- The corrected
  [v2 information-equivalent three-arm weak-Agent protocol](../strategy/HUMAN-AI-COLLABORATION-PROCESS-FIDELITY-INFORMATION-EQUIVALENT-TRIAL-PROTOCOL-V2-2026-07-27.md)
  is now preregistered for complete single-turn input, same-thread incremental
  input, and source-backed fresh-session recovery. It freezes the same
  v2 synthetic research task, unambiguous public semantic contract, parent-only
  private oracle, Spark/low route, authority and tool boundary, output contract,
  three repetitions per arm, and two non-cancelling ledgers for absolute task
  correctness and relative process loss. Its offline validator must fail before
  dispatch on information, oracle, model, authority, history, source-artifact,
  input-binding, scoped-reader, or carrier-isolation drift. This protocol does
  not assume that the host exposes
  compression telemetry, automatically creates a new thread, or invokes the
  source-backed handoff in a fresh session; those remain separately unproved.
  The v2 builder and preflight now use separate parent-evidence and
  Agent-visible roots. Direct and incremental arms expose no files; the
  source-backed arm exposes only `PUBLIC-SOURCE-BUNDLE.json` and must use the
  parent-scoped `read_public_information_bundle` dynamic tool. Shell execution
  and unrestricted file reads remain forbidden. The parameterized read-only
  runner records the actual Agent-visible input, reports detailed v2 oracle
  failures, and enforces the scoped read boundary. A source-backed
  zero-dispatch preflight passed with zero trial calls and zero scored arms.
  One later live source-backed calibration called the scoped dynamic tool
  exactly once with the required locator and hash, matched every non-identity
  oracle field, and preserved the observed read-only boundary. It is still
  measurement-invalid: the source-backed renderer omitted the fixed submission
  `armId`, the Agent returned the visible `fixtureId`, and the sole historical
  failure was `unknown-arm`. It counts as zero valid arms. The correction
  separates fixture, treatment, and delivery-topology identities and requires
  every renderer to state the same treatment arm. Focused, repository-wide,
  and zero-dispatch revalidation then passed, and one separately authorized
  replacement source-backed Spark/low smoke ran before the evidence stop.
  That replacement smoke passed the frozen v2 absolute oracle, used
  exactly one scoped dynamic read, and preserved the observed read-only tree
  and global configuration boundary. The normalized
  [source-backed smoke evidence](../strategy/HUMAN-AI-COLLABORATION-PROCESS-FIDELITY-V2-SOURCE-BACKED-SMOKE-EVIDENCE-2026-07-27.md)
  counts it as one absolute-task-valid transport repetition out of three
  required for the source-backed arm. Its durable raw report and at-dispatch
  protocol identity are preserved outside `.tmp`. It has no process-hop
  ledger or cascade measurement, does not complete an arm, compare
  topologies, prove automatic fresh-session handoff, or promote the
  cross-cutting end-to-end evidence state. An evidence stop is active before
  any additional topology or repetition. The zero-Agent chained-trace
  calibration then passed without model calls. The raw-event trace assessment
  found the durable smoke process-trace-ineligible: its material
  source-read-to-response edge is opaque and no process-hop ledger exists.
  It remains a transport pilot, and any later formal process cohort starts
  from zero. The
  [zero-dispatch chained-transform protocol](../strategy/HUMAN-AI-COLLABORATION-PROCESS-FIDELITY-CHAINED-TRANSFORM-TRIAL-PROTOCOL-2026-07-27.md)
  now freezes a real output-to-input chain, one identity-control and one
  authority-omission arm, a parent-bound recovery envelope, four
  position-balanced repetitions per arm, exact Spark/low routing, separate
  absolute and process ledgers, and a strong diagnostic that cannot rescue a
  weak-Agent failure. It records zero formal process runs and authorizes no
  dispatch. The
  [zero-dispatch packet preflight](../strategy/HUMAN-AI-COLLABORATION-PROCESS-FIDELITY-CHAINED-TRANSFORM-PACKET-PREFLIGHT-2026-07-27.md)
  now binds the exact protocol and trace-schema hashes and materializes only
  Hop 1 with one dynamic input, two Agent-visible files, zero private scoring
  leaks, and no later Agent stage. It deliberately leaves the actual route
  unobserved, the raw-evidence destination unbound, and live readiness false.
  The subsequent
  [zero-dispatch sequential artifact adapter and trace evaluator PoC](../strategy/HUMAN-AI-COLLABORATION-PROCESS-FIDELITY-CHAINED-TRANSFORM-ADAPTER-EVALUATOR-POC-EVIDENCE-2026-07-27.md)
  now covers ordered persistence, parent hash recomputation, conditional `S0`
  unsealing, two valid scripted mechanisms, and six fail-closed fault classes.
  Its raw captures are retained in a non-temporary repository-local audit
  directory. It still made zero Agent/model calls, did not observe Spark, did
  not authorize live dispatch, and leaves the formal process cohort at zero.
  The subsequent
  [additive cumulative-loss accounting PoC](../strategy/HUMAN-AI-COLLABORATION-PROCESS-FIDELITY-CUMULATIVE-LOSS-ACCOUNTING-POC-2026-07-27.md)
  now parent-recomputes new, carried, recovered, first-seen, reintroduced,
  deduplicated unique, peak-active, and first strict budget-breach measures
  across the four comparable semantic states. The injected calibration stays
  at cumulative unique weight 6 while carried and after recovery, rather than
  being double-counted or erased; the control remains zero. This is an
  additive zero-Agent mechanism only: the frozen evaluator, schema, protocol,
  and retained audit bytes remain unchanged, the budget cannot change
  `processAcceptancePass`, and the cross-boundary/cumulative subgate stays
  open.
  The subsequent
  [offline dispatch-gate contract](../strategy/HUMAN-AI-COLLABORATION-PROCESS-FIDELITY-CHAINED-TRANSFORM-DISPATCH-GATE-CONTRACT-2026-07-27.md)
  now validates one exact run-cell authority envelope, three single-use hop
  authorizations, parent-derived host-wire route and identity receipts,
  artifact linkage, terminal evidence, and ten fail-closed cases. It keeps
  provider-backend execution route telemetry unknown and live readiness false.
  The subsequent
  [same-host one-shot dispatch-ledger contract](../strategy/HUMAN-AI-COLLABORATION-PROCESS-FIDELITY-CHAINED-TRANSFORM-DISPATCH-LEDGER-CONTRACT-2026-07-27.md)
  reuses the existing file-lock and hash-chain primitives. Ten ledger tests,
  including two independent process contenders, produced exactly one accepted
  reservation and one rejection; retry, replacement, ambiguity release,
  corruption, and partial tails fail closed. Its zero-dispatch preflight is
  advisory and non-mutating, no model was called, and live readiness remains
  false. The next bounded gate is one exact current-task authority, a fresh
  host-reported Spark/low route observation, atomic reservation, and at most
  three no-tool hops; formal weak-Agent repetitions still start from zero.
  The program-level acceptance remains `partial` behind two explicit open
  subgates: the full eight-run no-retry/no-replacement chained cohort, and
  cross-boundary/cumulative coverage spanning accountable human edges, real
  compression/handoff/delegation/tool/review/lifecycle edges, a general plus a
  software-lifecycle scenario, deduplicated loss accounting, and human review
  burden. One authorized run could raise the candidate repetition count only
  to one and cannot close either subgate.
  The open `GEN-RESEARCH-01` treatment comparison resumes after this bounded
  protocol rather than being silently replaced by it.
- The
  [three-lane program-acceptance reconciliation](../strategy/HARNESS-THREE-LANE-PROGRAM-ACCEPTANCE-RECONCILIATION-2026-07-27.md)
  now binds the newer Context, Git, and MCP records back to
  `acceptance.dynamic-runtime-control-gap-research`,
  `acceptance.native-task-orchestration-boundary`, and
  `acceptance.residual-gap-proof`. All three remain `partial`. The mapping
  explicitly prevents parent-created transport from becoming automatic
  context lifecycle proof, disposable Git evidence from becoming user-
  repository safety, and startup/new-thread/idle/reload/unsubscribe-retention
  MCP observations from becoming same-thread task-scoped lifecycle control. It
  incorporates the shared Context/Git projection contract, additive receiver
  delta ledger, observer acquisition gate, auto-attach v2 offline amendment,
  and the bounded three-repetition reload, paired-unsubscribe, and
  multi-connection host observations above. The new evidence is linked only to
  its existing partial acceptance criterion, adds no broader host proof, and
  does not justify a self-authored runtime controller. The
  [multi-connection subscription preflight](../mcp-app-server-0.145.0-multi-connection-subscription-preflight-evidence-2026-07-27.md)
  used one App Server and two distinct loopback WebSocket connections in three
  protocol-valid zero-turn repetitions. Both connections called the same
  loaded Sentinel, but the second connection returned `notSubscribed` twice
  while the first returned `unsubscribed` then `notSubscribed`. The required
  second independently releasable subscription was therefore not observed,
  so the final-owner release trial is stopped rather than misclassifying
  callability as ownership. Re-open that path only if a real second
  subscription acquisition path appears or a separate decision authorizes a
  persisted-rollout model-turn trial. Longer-window behavior and resource
  benefit remain separate experiments.
  `MCP-THREAD-CREATOR-CONNECTION-CLOSE-ATTRIBUTION-01` is now prepared as a
  narrower paired, no-model-turn protocol that closes only the thread-creating
  connection while an independent observer and the exact Sentinel remain
  bound. One
  [calibration attempt](../mcp-thread-creator-connection-close-calibration-attempt-2026-07-27.md)
  started only the isolated control arm and failed before either paired window
  because the runner incorrectly required a zero-turn thread rollout file to
  materialize. This is invalid prerequisite evidence, not a release or
  retention result. It also records an authority conflict: loopback execution
  was still unauthorized by the protocol. The offline probe now treats rollout
  absence as an observation and has a regression test, but no live rerun
  followed. The formal paired protocol remains unexecuted; a fresh loopback
  calibration or formal run requires separate explicit user authorization and
  fresh roots. The two retained roots enter cleanup debt only and are not
  authorized for deletion. The later
  [observer acquisition-path admission](../mcp-thread-creator-close-observer-acquisition-path-admission-2026-07-27.md)
  verifies three reproducible zero-model-turn `thread-created-auto-attach`
  callability reports, but the current protocol/probe still requires an
  unvalidated `thread/resume` path. The pair is therefore blocked pending a
  new offline amendment. Auto-attach is not a second subscription, owner,
  lease, task-end signal, release result, or live authorization. The
  [auto-attach v2 offline amendment](../mcp-thread-creator-connection-close-auto-attach-offline-amendment-v2-2026-07-27.md)
  now removes that unsupported observer `thread/resume` prerequisite in a new
  preserved revision. Sixteen injected in-memory cases enforce B's
  initialize/config barrier before A starts the non-ephemeral read-only thread,
  direct B use of the same thread and exact Sentinel, pre-post-window evidence
  sealing, and bounded cleanup on ordinary and cleanup failures. Formal live
  runs and paired reports remain zero and all live authorities remain false.
  This is offline protocol evidence, not a creator-close retention or release
  result. A future positive association would still not prove
  task end, final-owner release, leases/reference counts, arbitrary-MCP
  behavior, resource benefit, controller need, or cross-host/version parity.
  Future work must target one named remaining live gap rather than add another
  generic offline simulator.
- The old GitHub repository has not been deleted by this migration task.

## Host and remote handoff limits observed at bootstrap

At bootstrap time, the Codex project list did not yet include
`C:\Projects\agent-autonomy-harness`, and no project-registration action was
available through the observed app tools. The user later registered the project
manually and authorized creation of this project-scoped continuation thread.
That resolves the historical bootstrap prerequisite only; it is not evidence
that the host can automatically register projects or create continuation
threads.

GitHub Actions for bootstrap commit `280a2080e167b32eadafa2cd672a64b63e24b8d4`
did not start on Ubuntu, Windows, or macOS because GitHub reported failed account
payments or an insufficient Actions spending limit. Local bootstrap validation,
the inherited verifier, 366 tests, 105 routing scenarios, manifest checks, and
topology checks passed. Remote CI remains billing-blocked rather than code-red.

## Current unknown-quadrant mapping checkpoint

The
[unknown-quadrant process-fidelity mapping](../strategy/HUMAN-AI-COLLABORATION-UNKNOWN-QUADRANT-PROCESS-FIDELITY-MAPPING-2026-07-27.md)
now records the four-unknowns method as a bounded research lens for explicit
fact retention, visible uncertainty, tacit human preference, blind-spot
discovery, and chained process loss. It preserves the existing separation:
hard standards are mandatory controls; Matt and Superpowers are selective
external method candidates; the self-authored three-Skill chain is a set of
phase controls; runner, ledger, and CC Switch are infrastructure or management.
It does not authorize a new Skill, remove the existing chain, dispatch a weak
model, or claim cross-host value. The next bounded use is to refine fixtures
and attribution oracles before any separately authorized live comparison.

Current validation for this slice:

- the mapping validator and its 10 focused tests pass;
- 78 related Skill/TDD/CC tests pass;
- 103 process-fidelity tests pass;
- `python -B scripts/verify.py` passes;
- `git diff --check` reports no whitespace error.

These results do not replace a full-suite run. The monolithic integration test
module was attempted earlier with 120-second and 300-second limits and produced
no failure stack before timing out, so its current result remains unknown rather
than green or red. The program remains active, the inherited worktree remains
intentionally large and dirty, and retained audit roots or temporary process
artifacts remain cleanup debt for the final program cleanup gate; this
checkpoint authorizes no deletion.

The subsequent
[unknown-quadrant attribution-oracle PoC](../strategy/HUMAN-AI-COLLABORATION-UNKNOWN-QUADRANT-ATTRIBUTION-ORACLE-POC-EVIDENCE-2026-07-27.md)
implements the next bounded zero-model step. All 22 fixtures match their
predeclared classification. Historical 2026-07-23 overlap fixtures remain
unchanged. The oracle rejects hard-standard credit, unisolated Matt plus
Superpowers attribution, self-authored phase controls relabelled as a domain
method, runner or ledger value credit, and terminal correctness used to erase
process loss. Spark/low remains the weak acceptance route and Luna/low remains
diagnostic-only. This is deterministic mechanism evidence, not a live run,
candidate-value result, residual-gap proof, or self-authoring authority.

The later
[unknown-quadrant packet-overlay PoC](../strategy/HUMAN-AI-COLLABORATION-UNKNOWN-QUADRANT-PACKET-OVERLAY-POC-EVIDENCE-2026-07-27.md)
binds five parent-private overlays to concrete zero-model packets and rejects
five injected boundary faults. Four lanes reuse existing packets. The
Unknown Knowns creative lane needed one derived packet because the matrix row
had no prior executable fixture. Its one positive and five negative examples
preserve the human preference boundary, brief facts, direction diversity,
rights limits, and no-publication authority. This is a fixture-coverage repair,
not live creative evidence or a residual capability gap. The next read-only
question is whether an existing renderer already exposes a parent-only oracle
attachment seam.

That question is now answered by the
[parent-oracle seam reuse decision](../strategy/HUMAN-AI-COLLABORATION-UNKNOWN-QUADRANT-PARENT-ORACLE-SEAM-REUSE-DECISION-2026-07-27.md).
The weak-Agent builder already supports protocol-owned private payload hashes
without writing their contents into `TASK.json`; the process-fidelity builder
already separates `agentVisibleProjection` and `privateOracle`; and the context
packet defaults to prompt-only output unless a parent packet is explicitly
requested. Existing regressions cover the core leakage boundary. No new
generic adapter is justified. Live integration remains unimplemented and must
wait for one exact authorized protocol arm.

Current verification after the oracle, packet-overlay, creative fixture, and
seam-reuse additions:

- all 52 `test_human_ai_collaboration_unknown*.py` tests pass;
- 24 existing weak-Agent, process-fidelity packet, and context packet builder
  regressions pass;
- `python -B scripts/verify.py` passes;
- `git diff --check` reports no whitespace error.

These are zero-model mechanism and compatibility checks. They add no valid live
weak-Agent repetition, no candidate treatment delivery, and no residual-gap or
portfolio preference evidence. The next program slice should return to one
named unresolved live Harness gap or perform another bounded zero-model
preflight that directly enables such a trial; it should not add another generic
oracle or adapter layer.

## 2026-07-28 semantic-authority continuation handoff

The direction remains provisionally aligned with the north star, but alignment
is now treated as a falsifiable proposition rather than a settled premise. The
self-authored `intent-contract`, `capability-router`, and `closure-contract`
Skills are upstream candidates under evaluation. Their current implicit
loading is not design validation, maturity evidence, or proof that they do not
conflict with official or reviewed third-party Skills. The same separation
applies to `AGENTS.md` or other rules, Hooks, host loaders, compression, and
thread handoff: each carrier needs independent adherence, overlap, failure, and
fallback evidence.

The bounded `SEM-03` slice now contains:

- a frozen native versus local adapted monolith versus exact current Matt
  composition protocol;
- a disposable Pitch lifecycle fixture with a parent-private oracle;
- exact raw Git object identities, MIT provenance, and static security and
  portability review for the current eight-file Matt treatment package at
  `ed37663cc5fbef691ddfecd080dff42f7e7e350d`;
- a dedicated three-Skill projection builder and no-model exposure probe;
- a fail-closed builder repair that retrieves and verifies the complete package
  before writing any projected file.

Four builder tests and three exposure tests pass. Two live materialization
attempts were blocked by intermittent GitHub transport failures; the second
exhausted bounded retries on
`skills/productivity/grilling/SKILL.md`. The repaired builder left
`C:\Users\15521\AppData\Local\Temp\aah-sem03-composition-projection-20260728-v1`
empty; that exact empty temporary root was then removed. No host exposure probe
or model turn was run, and no CC Switch, installed-Skill, global
configuration, commit, or push mutation occurred.

The next task should begin with repository truth, not this narrative:

1. reread `AGENTS.md`, this continuation, `PRODUCT-NORTH-STAR.md`,
   `RESEARCH-AND-POC-PLAN.md`, the `SEM-03` protocol, and current Matt static
   admission;
2. verify branch, status, HEAD, upstream, and ahead/behind while preserving the
   intentionally large inherited dirty worktree;
3. rerun the 16 focused projection, exposure, and protocol tests;
4. obtain the exact eight-file package through one stable, source-pinned
   transport and materialize it only through the atomic builder;
5. run the no-model dependency-complete exposure probe before any weak-model
   turn;
6. then audit the self-authored three-Skill chain against native, official, and
   reviewed third-party alternatives across overlap, conflicts, authority,
   lifecycle coverage, process loss, host adherence, Hook interaction, and
   compression or handoff failure. Do not retire, replace, install, or update a
   Skill from comparison metadata alone.

Current repository navigation truth at handoff: branch `main`,
HEAD `55659f30091990f7c589932e0379880de30dc403`, and `origin/main` at the same
commit. The worktree is intentionally large and dirty. These values must be
reverified in the receiving task. The program remains active and no closeout,
portfolio preference, self-authored residual gap, dynamic MCP lifecycle, or
automatic context handoff claim is justified.

## 2026-07-28 SEM-03 exposure and carrier-audit checkpoint

The receiving task reverified `main`, HEAD and `origin/main` at
`55659f30091990f7c589932e0379880de30dc403`, upstream `origin/main`, and
ahead/behind `0/0` while preserving the intentionally large dirty worktree.
The 16 projection/exposure/protocol tests and the pre-change top-level verifier
passed.

The exact current Matt package then materialized successfully from revision
`ed37663cc5fbef691ddfecd080dff42f7e7e350d`. All eight raw package files and
the MIT license matched the recorded byte counts, SHA-256 digests, and Git blob
identities. The Codex Desktop `0.145.0` no-turn exposure report passed with
exactly three required repo Skills selected, zero configurable Skills in the
control, stable identity and non-configurable states, and no thread, turn, or
model request. The durable report is
`audits/human-ai-collaboration-semantic-authority-current-matt-no-model-exposure-2026-07-28/REPORT.json`.
This closes dependency-complete task-scoped exposure only; loader invocation,
slash composition, relative-file delivery, behavior, value, and cross-host
claims remain open.

The
`HUMAN-AI-COLLABORATION-SELF-AUTHORED-CONTROL-CHAIN-CARRIER-AUDIT-2026-07-28`
record found that the current `.agents` and `.codex` copies of
`intent-contract`, `capability-router`, and `closure-contract` are byte-equal,
while all three CC Switch bodies are byte-distinct older variants. The startup
catalog exposes two entries per logical name without an independent loader
event. The capability-router Hook is currently registered in candidate
`auto` mode and a bounded synthetic check observed advisory injection plus
non-zero prompt-path latency. Global rules, Skills, the Hook, host exposure,
compression, and handoff must therefore be treated as distinct ablation
variables rather than one proven chain.

The initial projection and temporary report were created under exact new
repository-local `.tmp` paths. Their appearance drifted the governed cleanup
inventory; that is cleanup-debt evidence, not a code-stack failure. After the
durable report and protocol references are verified, those exact temporary
paths are authorized for precise removal. No other `.tmp` root is eligible.
The two exact paths were removed after their report bytes matched the durable
audit copy. The updated 23-test SEM-03 projection, exposure, protocol, and
carrier-audit set then passed, and `python -B scripts/verify.py` passed after
the cleanup-debt inventory returned to its governed state. No Skill or CC
Switch mutation, model dispatch, commit, or push is authorized by this
checkpoint.

## 2026-07-28 CC Switch current Matt promoted-suite checkpoint

The user explicitly authorized replacing the nine stale Matt-derived local
copies and installing Matt's current promoted suite through CC Switch for both
Claude and Codex. CC Switch 3.18.0 was the mutation surface; the SQLite
database was read only for verification.

The release authority was the 22-path `skills` array in
`.claude-plugin/plugin.json` at
`2ab958093e83e0ec752e6c1c5932da465bf23e0c`, not the unfiltered discovery
cardinality. The repository contains 41 `SKILL.md` files: 22 promoted, 4
deprecated, 9 in-progress, 4 misc, and 2 personal. CC Switch recursively scans
all directories containing `SKILL.md` and does not filter those lifecycle
classes, so the user's observed count of 38 is neither the current source-tree
count nor the promoted release count.

After the exact nine-copy replacement and 22-path installation, CC Switch
contained 88 rows, with all 88 enabled for Claude and Codex. All 22 Matt rows
recorded `mattpocock/skills@main`. The 66 promoted files in the CC Switch
central store, Claude projection, and Codex projection matched the raw Git
blobs at the pinned revision with zero file-set or SHA-256 mismatch. This
proves installation and filesystem projection only; loader invocation,
instructions reaching a model, behavioral value, cross-host value, and
replacement of the self-authored control chain remain open.

The durable host event is
`audits/cc-switch-matt-promoted-suite-installation-2026-07-28/REPORT.json`.
The second all-repository discovery refresh remained in a GitHub-transport
loading state, so it was not used as count evidence.

After the durable event and raw-blob whole-tree verification were written, the
exact repository backup, recovery snapshot, temporary source roots, and nine
CC Switch auto-backup directories created by this replacement were removed.
No historical backup or unrelated temporary root was included.

## 2026-07-28 CC Switch and Codex Skill-overlap checkpoint

A follow-up read-only overlap audit found that duplicate path counts must not be
treated as duplicate capabilities. CC Switch has 88 physical central Skill
directories. Codex has the same 88 names plus `.system` and
`codex-primary-runtime`; 84 of the 88 are expected links back to the CC Switch
central store.

Four same-name Codex entries are physical directories rather than CC Switch
links: `intent-contract`, `capability-router`, `closure-contract`, and
`kimi-webbridge`. `kimi-webbridge` is content-identical across the two
surfaces. The three self-authored control-chain Skills are not identical:
Codex matches `.agents`, while the CC Switch central copies are older divergent
bodies. This is an authority and lifecycle conflict, not merely duplicate
storage.

The `.agents` and Codex roots also expose 73 same-name directories. All 43
`.agents` links point to CC Switch. Of the 30 physical `.agents` directories,
18 are semantically identical to the CC Switch copies after line-ending
normalization and 12 still differ: the three self-authored control-chain Skills
plus `lark-apps`, `lark-base`, `lark-calendar`, `lark-doc`, `lark-drive`,
`lark-im`, `lark-slides`, `lark-task`, and `lark-wiki`.

No Skill was deleted or replaced during this audit. Loader precedence and a
single effective instruction body are not yet proved. The durable read-only
event is
`audits/cc-switch-codex-skill-overlap-audit-2026-07-28/REPORT.json`. A future
mutation must first choose one canonical authority and projection lifecycle for
each divergent same-name Skill.

The authorized CC replacement also exposed a protocol-design defect: the
historical 5,340-byte adapted-monolith arm had been bound to the mutable live
CC path. After replacement that path correctly contained Matt's current
245-byte composition entry, so the frozen protocol failed closed. The
historical arm and its builders now bind the existing repository payload at
`skills/grill-with-docs/SKILL.md`, preserving SHA-256
`e1078020c41b954638ba94acda95a3340739908bd68b1db9bc2af129d3936035`.
The current live Matt entry was not substituted for the historical arm.

Post-repair verification passed: 18 current projection/exposure/protocol tests,
24 extended SEM-03 tests including current Matt static admission, 27 affected
historical protocol and builder tests, and the top-level `scripts/verify.py`.

## 2026-07-28 control-chain factorial ablation preregistration

The carrier audit's next bounded experiment is now frozen in
`HUMAN-AI-COLLABORATION-SELF-AUTHORED-CONTROL-CHAIN-FACTORIAL-ABLATION-PROTOCOL-2026-07-28`.
It reuses `INT-AMB-01`, `ROUTE-MIN-01`, and `CLOSE-PRESS-01` and separates two
independent variables: hard-only versus the exact current three-Skill chain,
and Hook off versus Hook auto. Four cells prevent a hard-standard pass, a
scenario-relevant Skill loader event, or Hook advisory output from being
credited to another layer.

The current protocol is offline preregistration only. The user's live Hook
configuration remains untouched. A future off/auto preflight must use a
disposable isolated profile or parent-controlled equivalent. The existing
live-run evaluator predates the Hook factor and is explicitly not eligible to
upgrade this protocol to live readiness. The next gate is the smallest
zero-model evidence adapter plus isolated Hook-mode and injected-failure
preflight. No Skill or CC Switch mutation, weak-model dispatch, cleanup,
commit, push, portfolio decision, residual-gap claim, or program closeout is
authorized by this record.

The isolated direct-handler preflight now passes without editing `hooks.json`.
All three off-mode observations emitted zero bytes. Auto mode emitted a
428-byte advisory for `INT-AMB-01` and `ROUTE-MIN-01`, but not for
`CLOSE-PRESS-01`; registration and injection are therefore separate evidence.
Invalid JSON returned success with zero advisory bytes and parent-visible
debug evidence. The live registration digest was unchanged. The durable
report is under
`audits/human-ai-collaboration-self-authored-control-chain-hook-mode-preflight-2026-07-28/REPORT.json`.
This advances only the isolated Hook-mode and failure-fallback gates. The next
zero-model gate was the factorial run-evidence adapter. That side-effect-free
adapter now passes all twelve `3 scenarios x 4 cells` synthetic contracts and
rejects factor mismatch, Skill exposure or loader drift, Hook-mode drift,
silent model substitution, hard-standard credit, authority errors, and
repository drift. Synthetic evidence remains ineligible for live or
weak-Agent credit. The next gate is now task-scoped no-model four-cell
exposure only.

Preflight intake then found a dependency-completeness omission: the three body
hashes do not cover `intent-contract/references/intake-contract.md` or
`capability-router/references/routing-contract.md`. The current `.agents` and
`.codex` reference copies are byte-equal; both CC Switch references are
byte-distinct. These two files are now pinned in the carrier audit and
factorial protocol. The exact-chain projection must contain all five files
before the no-model four-cell exposure can count.

The corrected five-file atomic projection and four-cell no-turn preflight now
pass on Codex Desktop `0.145.0`. Hard/off, hard/auto, exact/off, and exact/auto
exposed `0`, `0`, `3`, and `3` configurable Skills. Direct Hook evidence
emitted `0`, `428`, `0`, and `428` bytes. Default config, live Hook
registration, projection bytes, and repository status remained stable. The
system temporary projection and isolated `CODEX_HOME` were removed after the
durable report was captured under
`audits/human-ai-collaboration-self-authored-control-chain-four-cell-exposure-2026-07-28/REPORT.json`.

Two pre-result startup failures were probe defects, not candidate outcomes:
empty isolated config cannot accept incomplete MCP disable tables, and atomic
staging manifests must bind final paths. Both are repaired and fault-tested.
The exposure report still proves no loader invocation, host Hook consumption,
instruction delivery, behavior, value, or weak-Agent acceptance. The next
gate is an independent scenario-relevant loader event and host
Hook-consumption evidence surface. Do not dispatch a weak model merely because
inventory exposure now passes.

The current-host observability intake is now durable in
`HUMAN-AI-COLLABORATION-SELF-AUTHORED-CONTROL-CHAIN-LOADER-HOOK-OBSERVABILITY-ADMISSION-2026-07-28`.
Local app-server `0.145.0` schemas and the official app-server surface agree
that `skills/changed` is inventory invalidation, while `hook/started` and
`hook/completed` are task-associated Hook run notifications. No independent
task-bound Skill loader identity or digest event is exposed. A model turn would
therefore close only the Hook-consumption half of the gate. Model dispatch
remains zero, and the live factorial remains unadmitted. Record this as a
current-host observability constraint, not a candidate or code-stack failure.
The next thread must either recheck a newer host for an exact loader event or
ask the owner whether to amend the frozen attribution acceptance boundary; it
must not silently substitute behavior association.

The static comparison is now reconciled in
`HUMAN-AI-COLLABORATION-SELF-AUTHORED-CONTROL-CHAIN-SUBTRACTIVE-CLOSEOUT-RECONCILIATION-2026-07-28`.
The three Skills materially overlap global hard gates and remain provisional
ablation carriers, not admitted final architecture. Official host mechanisms
provide inventory, explicit input, Hook, thread, and compaction surfaces but
do not prove the chain's semantic value. Reviewed Matt and Superpowers
workflows cover bounded subflows and should be reused before more authoring,
but do not currently prove full replacement. The chain covers intake, routing,
and closeout checkpoints only; it is not a complete software lifecycle.

Do not author another control Skill, promote the candidate Hook, bulk-enable an
external suite, deduplicate the two startup-visible projections, migrate CC
Switch ownership, or retire any current payload from this evidence. Provisional
retention preserves the frozen experiment only, and the final admitted
self-authored payload count may be zero. Program closeout remains unsupported
because live value attribution, canonical consumer convergence, all three
program-lane acceptances, and separately authorized final cleanup remain open.

The program-level closeout audit is now durable in
`PROGRAM-FINAL-CLOSEOUT-READINESS-RECONCILIATION-2026-07-28`. The canonical
acceptance map currently has 61 criteria: 44 verified, 15 partial, and 2
planned. Seventeen open criteria affect eight objectives and group into six
independent evidence clusters: semantic/lifecycle, residual-gap/authoring,
consumer/source governance, standard lifecycle, runtime/Hook/orchestration,
and final cleanup.

This falsifies the premise that the exact-loader trust decision is the only
remaining closeout gate. It is one independent input to the runtime and
self-authored-value work. A favorable decision would not close consumer
mapping, source convergence, standard-candidate lifecycle, domain/process
evidence, or final cleanup. Continue advancing those clusters independently;
do not use a passing verifier, static overlap, or cleanup inventory conformance
as a status upgrade.

## 2026-07-28 Skill-portfolio execution recalibration

The owner rechecked the practical portfolio boundary before further mutation.
The master plan now makes the next phase subtraction-first, CC
Switch-manager-backed, exact-upstream-reconciled, dependency-aware, and
foreign-root-preserving. Dated catalog or installed counts are inventory
observations rather than quality or progress targets. CC Switch remains the
shared third-party operational manager, so an upstream one-click installer
must not create a parallel unmanaged path; CC repository metadata still
requires exact current-upstream and dependency-closure verification.

Current read-only host evidence records `~/.trae-cn/skills` as 78 physical
directories with zero `lark-*` entries and zero CC Switch links. Trae's
versioned Feishu Plugin retains its own 26-item Lark Skill dependency set.
Those two roots are foreign-managed and excluded from CC/common cleanup. The
separately observed 27-item CC/common Lark cohort is not a Trae dependency and
must not be copied or rehomed into Trae. Its later retention or removal is a
separate previewable, backed-up, reversible manager transaction with database,
projection, link, sync, rollback, foreign-sentinel, and cleanup reobservation.

The current Matt suite, CC-recommended sources, the user's public candidate
lists, earlier governed discovery, and materially relevant sources beyond
those lists remain candidate inputs rather than an exhaustive shortlist.
Deduplication is by capability, authority, dependency, invocation, and
verified behavior rather than directory name alone. The self-authored
`intent-contract`, `capability-router`, and `closure-contract` chain remains a
falsifiable candidate/control set: narrower external superiority does not
automatically disprove the broader router role, while static overlap does not
prove permanent value. Any retirement or replacement must jointly disposition
the affected global `AGENTS.md`, Skill references, Hook, loader, compression,
handoff, branch/worktree, MCP, consumer, fallback, backup, and rollback
carriers.

The master plan and existing canonical acceptance criteria were updated
without adding a count-growth acceptance item. The tightened gates require
exact pre/post inventory, upstream pin and dependency closure, atomic manager
transactions, foreign-root isolation, separation of listing/install/exposure/
invocation/delivery/behavior/value evidence, visible
`gpt-5.3-codex-spark`/`low` routing before a formal weak-Agent turn, viable
alternative comparison before residual-gap authoring, and explicit
disposition of every temporary or process artifact. This plan maintenance
does not authorize any Skill, CC Switch, Trae, Hook, model, AGENTS/rules,
cleanup, commit, or push mutation.

## 2026-07-28 CC Switch shared Lark cohort removal checkpoint

The owner subsequently authorized removal of the exact 27-item shared Lark
cohort while preserving Trae's independently managed common and versioned
Plugin roots. CC Switch 3.18.0 was the manager mutation surface: a one-item
canary and 26-item batch succeeded, the database moved from 88 rows with 27
Lark rows to 61 rows with zero Lark rows, and the CC Switch SSOT plus Codex and
Claude projections now contain zero Lark entries and zero broken links.

The independent schema-3-locked `.agents` copies were removed as the same exact
cohort and the lock was atomically replaced with an empty Skill map. This
applies the recovered directory consensus: CC Switch is the shared third-party
entity store, Codex and Claude are managed projections, and `.agents` is at
most a compatibility or unsupported-host adapter rather than a second entity
store. `skillStorageLocation` remains `cc_switch`.

Trae's common root remained at zero Lark entries and its versioned Feishu
Plugin retained 26; pre/post names and hashes were stable. CC Switch retained
20 managed backups and an explicit remote sync returned `uploaded`. The exact
temporary source clone, snapshot helper, agent-created recovery bundle, and
debug listener were removed after verification. CC Switch was restored to one
normal process.

The durable record is
`CC-SWITCH-LARK-COHORT-REMOVAL-EVENT-2026-07-28`. The current 18-test
projection/exposure/protocol suite and `python -B scripts/verify.py` passed.
This task's startup Skill catalog predates the removal, so fresh-task catalog
disappearance remains unobserved. No Hook or model dispatch, storage-mode
change, unrelated `.agents` cleanup, commit, or push occurred.

## 2026-07-29 common Skill-root and causality recalibration

The owner corrected two assumptions. First, `~/.agents/skills` is a retained
common compatibility root for present or future Agents; cleanup may remove or
replace governed contents but must not clear or remove the root itself. Second,
Codex and Claude are different hosts, so byte-distinct effective Skill packages
are not inherently a lifecycle error and cross-host byte parity is not an
acceptance criterion.

The current Codex turn exhibits behavior consistent with implicit Skill
selection, but the cause is unresolved. Global rules, the self-authored Skill
chain, startup inventory, native/runtime behavior, and their composition are
live candidates. A 2026-07-29 recheck found `hooks.json={}`, zero Hook files,
and only a historical `hooks.state` entry, so Hook recall is not a current
candidate cause. Do not treat this mixed-carrier turn as proof that Codex now
has a native replacement for the chain, or as proof that the chain caused the
behavior. Keep discovery, selection, body loading, instruction delivery,
adherence, causation, and value as separate evidence states.

The narrower native-capability question was then isolated successfully. Current
official Codex documentation explicitly supports description-matched implicit
invocation with default `allow_implicit_invocation=true` and full `SKILL.md`
reading after selection. A same-prompt control/treatment trial on Codex
`0.145.0` used a temporary `CODEX_HOME`, no global `AGENTS.md`, the live empty
Hook surface, no
Codex-private Skill root, and temporary removal of the three self-authored
`.agents` payloads while retaining the common directory. The control returned
a normal violet-compass receipt and not the private token; the sentinel
treatment visibly read the full Skill body and returned exactly
`PURPLE_COMPASS_RECEIPT_7D29`. Both fresh ephemeral tasks ran
`gpt-5.6-sol`/`none` under a read-only sandbox.

This proves current-host native implicit Skill discovery, selection, and
full-body loading. It does not attribute the earlier mixed-carrier turn, prove
universal reliability, or decide the incremental value of the three
self-authored Skills. The live Skill trees were restored by digest; the
sentinel and temporary authentication copy were removed. Update, reduction, or
retirement review must now compare the chain against this verified native
baseline rather than an obsolete assumption that Codex lacks implicit
invocation.

## 2026-07-29 CC Switch subtraction and Codex shadow-disable checkpoint

The owner authorized one exact manager transaction after the portfolio
rebaseline. CC Switch 3.18.0 removed `-21risk-automation`, both
`git-guardrails` directories, `scaffold-exercises`, `write-a-skill`, and
`sora` through `uninstall_skill_unified`. A one-item canary preceded the
remaining five removals. Five resulting broken compatibility links under
`.agents` were removed by exact path while the common root remained intact.

`doc` and `pdf` were retained in the CC entity store, common root, and Claude
projection. `toggle_skill_app` disabled only their Codex projections because
the current Codex runtime already exposes broader runtime-owned document and
PDF capabilities. The database moved from 61 rows/60 distinct names to 55/55.
Claude has 55 CC-enabled rows; Codex has 53 CC-enabled rows plus `.system` and
`codex-primary-runtime`. All four inspected Skill surfaces have zero broken
links.

Matt's 22 promoted rows, the three self-authored candidates, `caveman`, and
`kimi-webbridge` remain enabled for both hosts. Trae roots were excluded from
all mutation targets. The current foreign-root counts were reobserved, but no
pre/post Trae content-hash sentinel was captured, so the evidence claims
target exclusion rather than byte equality.

A secret-screened, source-consistent pre-transaction recovery archive passed
verification without settings, credentials, account data, the raw database,
or detected secrets. CC Switch retained six exact rollback backups among its
20 managed backups and uploaded a compatible WebDAV snapshot. After
verification, the temporary recovery archive, manifest, and CDP helper were
removed. The debug listener was closed and one ordinary CC Switch process was
restored.

The durable record is
`CC-SWITCH-SUBTRACTION-COHORT-AND-CODEX-SHADOW-DISABLE-EVENT-2026-07-29`.
The extended 24-test SEM-03 set, event validator tests, and top-level verifier
passed after cleanup. No Hook, `AGENTS.md`/rules, model, storage-mode, commit,
or push mutation occurred. Fresh-task catalog refresh, loader invocation,
behavioral value, remaining-portfolio quality, self-authored disposition, and
program closeout remain open.

## 2026-07-30 Codex common-root shadow-disable correction preflight

The next fresh task falsified one interpretation of the 2026-07-29
transaction. All six uninstalled Skills are absent from the startup catalog,
but `doc` and `pdf` remain visible alongside Codex's runtime-owned document and
PDF capabilities. Their private `~/.codex/skills` projections are absent, while
the retained `~/.agents/skills` links still resolve to the CC entity store.

The correct boundary is now explicit: CC's Codex toggle proved database and
private-projection disablement, not Codex host-exposure disablement. Codex
officially discovers the common root, so another live carrier bypassed the
private projection decision.

An isolated no-model preflight on Codex Desktop 0.146.0 copied the exact current
`doc` and `pdf` bodies into a disposable project `.agents/skills` root. Control
listed both exact paths enabled; the treatment retained the same identity paths
with `enabled=false` through two exact `skills.config` entries. No thread, turn,
model, global config, CC Switch, common live root, Claude, or Trae mutation
occurred. The temporary projection and isolated `CODEX_HOME` were removed.

The durable record is
`CODEX-COMMON-ROOT-DOC-PDF-SHADOW-DISABLE-EXPOSURE-RECONCILIATION-2026-07-30`.
Deleting the two common links is not preferred because it would express a
Codex-specific policy by removing shared compatibility for every Agent. The
preferred live correction is two official Codex path-disable entries while
retaining the common root and Claude projection. That next step requires new
authorization for the global Codex config transaction, one Desktop restart,
and exact rollback-backup cleanup. It requires no CC Switch, common-root,
Claude, Trae, model, commit, or push mutation.

## 2026-07-30 Codex common-root doc/pdf live host-disable checkpoint

The owner authorized the exact live transaction. The preflight config hash and
zero-entry state were unchanged. A same-directory atomic replacement added
only the two exact common-root `doc`/`pdf` paths with `enabled=false`, retained
an exact-byte rollback backup, and passed TOML parsing before a real Codex
Desktop restart.

Fresh post-restart app-server processes on Codex Desktop 0.146.0 provide the
host-exposure evidence. With Plugin features disabled, the CC-resolved
`doc`/`pdf` rows remain discoverable but are both disabled. With Plugin
features enabled, those two rows remain disabled while runtime-owned
`documents:documents` and `pdf:pdf` are enabled. The live config was stable
across both probes. No thread, turn, model, CC Switch, common-root, Claude,
Trae, commit, or push action occurred.

CC Switch still owns the physical bodies; the common and Claude links remain;
the private Codex links remain absent. This implements a Codex host-adapter
policy without deleting the shared entity or compatibility carrier. The
durable record is
`CODEX-COMMON-ROOT-DOC-PDF-HOST-DISABLE-TRANSACTION-2026-07-30`.
The exact rollback backup was retained through the first dedicated-test and
top-level-verifier pass, then hash-checked and removed by exact path. The final
evidence state and cleanup inventory were reverified. The global adapter rows
remain; no transaction backup or same-directory write temporary remains.

## 2026-07-30 current-55 subtractive portfolio checkpoint

The live 55-row CC Switch portfolio is now partitioned by current source,
lifecycle, exposure, overlap, and dependency evidence instead of count. The
seven mutually exclusive cohorts contain 22 current Matt promoted exact
packages, three self-authored falsifiable controls, two explicit user/host
exceptions, two shared entities shadow-disabled on Codex, 15 strong
subtraction candidates, five Addy source-rebinding debts, and six task-bound
specialists. The partition covers all 55 rows exactly once.

Matt `main` remains the installed pin
`2ab958093e83e0ec752e6c1c5932da465bf23e0c`; all 22 live promoted trees still
match raw Git blobs. `setup-matt-pocock-skills` is a promoted repository
context/tracker scaffold, not the one-click installer, so CC Switch management
is not a name-only retirement reason.

Five legacy names have current promoted replacements, while four installed
items are deprecated upstream, three are in progress, one is personal, one is
misc plus previously rejected, and `zoom-out` is absent from the current Matt
tree. Fourteen are ready for an exact manager-removal preview. `diagnose` is
not: both `capability-router` and `observability-and-instrumentation` still
name its legacy identity. The promoted setup Skill also mentions deprecated
`qa` even though `qa` is omitted from the promoted release manifest; this is
an upstream consistency warning, not permission to edit the exact package.

Addy's current `main` advanced to
`7829ffd90d973b6325f5f12f1b1226dcace74443`. All five selected live bodies
differ from current upstream after line-ending normalization and their CC rows
carry no source metadata. They remain frozen pending current delta and source
rebinding review.

The durable record is
`SKILL-PORTFOLIO-CURRENT-55-SUBTRACTIVE-TRIAGE-2026-07-30`. Both exact
temporary source clones were removed. This checkpoint performs no CC Switch,
Skill, config, Hook, rules, model, commit, or push mutation. A 14-item manager
preview, diagnose-reference repair, Addy delta review, specialist probes, and
separate mutation authorization remain open.

## 2026-07-30 fourteen-Skill CC Switch subtraction preview checkpoint

The 14 ready candidates are now bound to live bodies, database rows, all three
shared projections, fresh no-model Codex exposure, exact CC Switch 3.18.0
backend commands, backup rotation, rollback, expected counts, and cleanup. The
payload is 19 files and 58,356 bytes; all fourteen are currently enabled for
Claude and Codex and disabled for the other recorded hosts.

The exact tag source confirms that `uninstall_skill_unified` creates a body and
metadata backup before deleting projections, SSOT, or the database row.
`restore_skill_backup` restores exactly one requested host, so dual-host
rollback requires a second `toggle_skill_app`. CC Switch does not manage the
common `.agents` links, so those links must remain until all manager
uninstalls succeed and only then be removed as the exact broken cohort.

`edit-article` is the proposed canary. `diagnose` remains excluded due to live
references. The promoted setup Skill's mention of deprecated `qa` is recorded
as an upstream consistency warning rather than a hard dependency or permission
to edit source.

The manager already holds 20 backups. Fourteen successful uninstalls would,
absent concurrent backup creation, evict the fourteen oldest Lark-removal
backups under the manager's source-proved retention policy while retaining the
six 2026-07-29 backups. That automatic recovery-data deletion is explicitly
inside the pending authorization boundary.

The durable read-only record is
`CC-SWITCH-FOURTEEN-SKILL-SUBTRACTION-PREVIEW-2026-07-30`. The incomplete
source checkout used only for raw Git inspection was removed. No uninstall,
restore, remote sync, `.agents` cleanup, debug listener, model, config, Hook,
rules, commit, or push action occurred. One exact mutation authorization is
now required.

## 2026-07-30 Addy-five and six-specialist static-triage checkpoint

Read-only current-source review rejects a bulk Addy update. Addy `main` remains
`7829ffd90d973b6325f5f12f1b1226dcace74443`; only the selected
`performance-optimization` and `shipping-and-launch` paths changed since the
last reviewed revision. All five live bodies are byte-equal to their governed
Harness adaptations, while their CC rows carry no source metadata. Exact
current upstream would lose local authority/privacy controls and introduce
missing root references or sibling-Skill dependencies into the one-file CC
projections.

The bounded Addy dispositions are: keep `ci-cd-and-automation` frozen pending
a non-Vercel value test; prioritize a materially different
`deprecation-and-migration` ablation because the prior hidden oracle favored
native; repair the legacy `diagnose` reference and PII example in
`observability-and-instrumentation` before later source or removal work;
consider only the keep-or-revert measurement loop from current
`performance-optimization`; and consider only ecosystem-neutral dependency
audit wording from current `shipping-and-launch`. The research note is
`docs/strategy/addy-five-delta-audit-2026-07-30.md`.

The separate six-specialist triage retains exactly
`obsidian-open-format-knowledge-files`: its live body is byte-equal to the
approved repository payload, its declared Kepano pin is still current `main`,
and it remains dependency-free and agent-neutral. Five active CC copies are
eligible for an exact manager-removal preview:
`disciplined-coding`, `migrate-to-shoehorn`, `obsidian-vault`, `playwright`,
and `security-ownership-map`.

`disciplined-coding` has no bound source authority and no current Matt path;
bounded historical behavior did not prove incremental value.
`migrate-to-shoehorn` remains recoverable as an approved on-demand payload but
is narrow and dependency-bearing. `obsidian-vault` remains a local derivative
of a personal Skill with no configured vault. The current Playwright and
security-ownership trees are exact OpenAI source copies after line-ending
normalization, but the former duplicates live browser/Playwright runtime
surfaces and the latter lacks `networkx` and crosses a broader Git-history data
boundary. Official and approved source metadata remain even if later active CC
copies are removed.

The six-specialist durable record is
`SKILL-PORTFOLIO-SIX-SPECIALIST-STATIC-TRIAGE-2026-07-30`. This checkpoint
installed nothing, executed no Skill body, sent no model request, and changed
no CC row, projection, common-root link, config, Hook, rule, Plugin, MCP, App,
Trae root, commit, or remote. The five-item removal preview and every Addy
adaptation remain separately authorization-gated.

## 2026-07-30 self-authored authority and CC ownership-collision checkpoint

The three self-authored identities now have a live authority reconciliation.
`C:\Projects\codex-user-config` remains source authority at
`0c93458d48cb1ebaa6d0d289e3a21f46d2f61f65`, equal to its `origin/main`.
Its three Skill trees are clean at that revision. For each of
`intent-contract`, `capability-router`, and `closure-contract`, the source,
`.agents`, and `.codex` package trees are byte-identical physical directories.
The CC SSOT packages are smaller divergent snapshots, and all three Claude
paths are symbolic links to those older CC trees.

A fresh no-model Codex app-server `skills/list` with `forceReload=true`
exposed two enabled current entries per logical name, one from `.agents` and
one from `.codex`. That proves duplicate Codex discovery only; invocation,
instruction delivery, behavior, value, and fresh Claude exposure remain
unproved.

The live CC Switch binary has advanced from the 3.18.0 source used by the
fourteen-item preview to `3.19.0`. The official `v3.19.0` tag resolves to
commit `c0ff89b9b208c092d6ef40b155403dcf290e5767`. Its current source still
iterates every app on uninstall, resolves Codex to `~/.codex/skills`, removes
physical targets recursively, calls the same removal path on disable, and
under the live `symlink` setting removes an existing target before creating a
CC link. The normal uninstall backup protects the CC body and metadata, not
the newer externally owned `.codex` tree.

Therefore ordinary CC uninstall, toggle, or sync is unsafe for these three
identities and must not be used. A later collision-safe transaction must
preserve the current source-owned `.agents` and `.codex` trees and requires a
host-policy choice: quarantine the unproved older Claude links, or first define
and test a source-owned Claude adapter. Codex dual-root simplification is a
separate source-repository gate. The authoritative `capability-router`
reference migration belongs in `codex-user-config`, not the old CC copy.

The durable record is
`SELF-AUTHORED-THREE-LIVE-AUTHORITY-AND-CC-COLLISION-RECONCILIATION-2026-07-30`.
It authorizes no mutation. The fourteen-item preview must also refresh its
manager, backup, rollback, projection, and sentinel assumptions against live
3.19.0 before any deletion authorization can be consumed. No temporary source
root, recovery archive, database copy, model turn, Skill execution, CC change,
host change, config, Hook, rule, commit, or push occurred.

## 2026-07-30 diagnose reference-migration and fifteenth-admission checkpoint

`diagnose` is now bound as a conditional fifteenth subtraction candidate, not
as currently removable. Its local unattributed package is 2 files and 8,328
bytes. The current promoted exact `diagnosing-bugs` replacement is 3 files and
9,803 bytes at the current Matt pin
`2ab958093e83e0ec752e6c1c5932da465bf23e0c`. Both are currently enabled for
Claude and Codex and exposed on a fresh no-model Codex list.

Three exact legacy identity references remain. The authoritative
`codex-user-config` router should eventually replace its hard-coded
`diagnose` stage with the neutral `diagnosis capability`. The divergent CC
router must not be patched in place; it leaves only through the collision-safe
first-party carrier transaction. The governed observability adaptation should
replace “canonical `diagnose` Skill” with the active task-bound diagnosis
workflow and simultaneously remove `${userId}` from its logging example before
a separately authorized CC projection update.

Admission depends on five unsatisfied gates: first-party carrier policy and
safe legacy-router retirement; authoritative router patch/projection;
observability patch/projection; a zero exact retained-reference plus fresh
host-exposure check; and a live CC Switch 3.19.0 manager/backup/rollback/
projection/sentinel refresh. Only then can the ready cohort grow from 14 to
15, from 19 to 21 files, and from 58,356 to 66,684 bytes. The current arithmetic
`55 - 15 = 40` is not a forecast because the carrier transaction may change
the database baseline first.

The durable record is
`DIAGNOSE-REFERENCE-MIGRATION-AND-FIFTEENTH-SUBTRACTION-ADMISSION-PREVIEW-2026-07-30`.
No proposed source patch was applied, no legacy CC copy was edited, and no
Skill, row, link, host, model, config, Hook, rule, commit, or remote changed.

## 2026-07-30 fourteen-Skill CC Switch 3.19 refresh checkpoint

The fourteen-item manager preview is execution-current again at the read-only
level. The running binary reports CC Switch 3.19.0; official tag `v3.19.0`
resolves through `09ccf3280c779c6cf7023cd2c3fc3faa21af8b73` to commit
`c0ff89b9b208c092d6ef40b155403dcf290e5767`. The tag is unsigned and exact
binary-to-tag supply-chain identity is not claimed.

The current source preserves the required transaction behavior: safe
single-segment directory validation, backup before deletion, all-app projection
cleanup, link-only removal for symlink targets, one-host restore, collision
refusal, and a 20-backup modified-time retention policy. Unlike the three
self-authored collision rows, all fourteen candidates have CC-owned SSOT trees
and symbolic-link Claude/Codex projections; none occupies an externally owned
physical `.codex` directory.

All fourteen database content hashes, row flags, file counts, payload bytes,
and three projection links remain current. A fresh no-model app-server list
returned exactly the fourteen enabled direct-CC identities. Current tree
digests were frozen under a newly explicit path/byte-count/file-hash algorithm
rather than assuming the prior preview's digest construction.

The backup inventory remains 20. Fourteen successful uninstalls would evict the
current fourteen oldest named Lark backups and retain six 2026-07-29 entries,
assuming no concurrent backup appears. `edit-article` remains the canary, and
the same five-part authorization boundary applies: exact fourteen uninstalls,
named backup eviction, post-success cleanup of exactly fourteen broken
`.agents` links, one verified configured remote snapshot, and exact recovery/
CDP artifact cleanup.

The durable refresh is
`CC-SWITCH-FOURTEEN-SKILL-SUBTRACTION-PREVIEW-3.19-REFRESH-2026-07-30`.
No uninstall, restore, toggle, remote snapshot, archive, listener, link cleanup,
Skill execution, model, config, Hook, rule, commit, or push occurred.

## 2026-07-30 fourteen-Skill live preflight checkpoint

The refreshed fourteen-item preview now has a read-only, fail-closed live
preflight at
`scripts/preflight_cc_switch_fourteen_skill_subtraction.py`. It fingerprints
the exact CC Switch executable, selected Skill settings, safe columns for all
55 rows, all 55 CC Skill trees, all four top-level projection roots, all 20
backup identities and modified-time order, and the two Codex `doc`/`pdf`
disable rows. It then separately checks the fourteen targets and the protected
first-party-three, Matt-22, and doc/pdf sentinels.

The first live run rejected the only multi-file target because the earlier
manifest wording omitted that the Windows/PowerShell path ordering was
case-insensitive. No body had drifted. The algorithm is now explicit:
case-insensitive normalized relative path, original path tie-breaker, then
`path NUL byte-count NUL file-sha256` lines joined by LF. The corrected live
whole-state fingerprint is
`4275f647d365be09862eb7cf0f86f3f85f78be3910c58f4514071746c69e5c60`.

The corrected live preflight passed with 55 database rows, 14 targets, 20
backups, 3 first-party physical sentinels, 22 Matt sentinels, and 2 doc/pdf
policy sentinels. This is a point-in-time check, not a transaction lock or
mutation authority; it must run again immediately before an authorized
canary. The durable contract is
`CC-SWITCH-FOURTEEN-SKILL-LIVE-PREFLIGHT-CONTRACT-2026-07-30`. No live Skill,
row, link, backup, host, config, remote, model, temporary artifact, commit, or
push changed.

## 2026-07-30 Claude carrier and subtraction sequencing checkpoint

Current Claude Code documentation explicitly supports one symbolic link per
Skill directory on Claude Code 2.1.203 or later and states that duplicate
links to the same target load that target once. Direct discovery from
`~/.agents/skills` is not documented. The official documented priority is
enterprise, personal, then project, while upstream issue `#53288` reports a
contrary observed runtime order; documentation is therefore a contract
candidate rather than current-host exposure proof.

The recommended no-mutation carrier option is source-owned
`.claude/skills/<name>` links targeting the current physical
`.agents/skills/<name>` bodies for `intent-contract`, `capability-router`, and
`closure-contract`. This avoids promoting the older divergent CC bodies into
first-party authority, but it remains only a preview until the owner chooses
the carrier, the exact Claude version and link behavior are observed, and
fresh no-model exposure is proved. CC uninstall, toggle, or sync remains
unsafe for the three colliding identities.

The durable records are
`CLAUDE-CODE-SKILL-CARRIER-OFFICIAL-RESEARCH-2026-07-30` and
`SELF-AUTHORED-THREE-CLAUDE-CARRIER-AND-SUBTRACTION-SEQUENCING-DECISION-PREVIEW-2026-07-30`.
No Claude root, source body, CC row, projection, Hook, rule, model route,
temporary runtime root, commit, or remote changed.

## 2026-07-30 dynamic discovery and runtime-lifecycle plan checkpoint

The main plan and canonical acceptance map now reject a fixed-capability
assumption. The current `capability-router` already says that existing-first is
not installed-only, that an evidenced current-capability gap may trigger
targeted discovery, and that routing must be reconsidered at phase, failure,
side-effect, capability-class, and closeout checkpoints. That is only a text
contract. A mature dynamic loop still requires observable gap detection,
exact discovery receipts, candidate review, explicit no-candidate or ask-user
outcomes, bounded authorization, verified exposure/invocation/behavior/value,
task-scoped release, and later rerouting after capability-health or ecosystem
drift.

The external runtime-lifecycle ecosystem is also no longer treated as empty.
The dated primary-source review identifies reusable host, gateway, scoped
session, lazy Agent, worker, lease, stale-resource, durable-recovery, policy,
and observability primitives across Codex, Claude Code, Docker MCP Gateway,
ToolHive, Microsoft MCP Gateway, LangChain/LangGraph, AutoGen, CrewAI,
Semantic Kernel, Temporal, Prefect, and Dagster. No reviewed component proves
the whole cross-host target, but a self-authored candidate is now restricted
to a thin host-aware coordination gap that remains after those components and
their compositions fail.

The refined chain separates portable `AGENTS` constraints, Skill semantic
desired state, optional Hook or host-event observation, ownership and lease
accounting, host/gateway/runtime actuation, and live reconciliation, release,
recovery, and cleanup evidence. Active turn, loaded thread, persisted thread,
subagent or worker, branch/worktree, connection, and process states are
separate. PoC 3 must compare the same workload under always-on,
static-minimal, and phase-gated profiles; separate local stdio from remote HTTP
lifecycle; include concurrent-owner and failure controls; preserve host-native
permission boundaries; and measure task quality, latency, context/tool
exposure, resource state where observable, user intervention, recovery, and
cleanup debt.

The durable research surface is
`RUNTIME-CAPABILITY-AND-AGENT-LIFECYCLE-ECOSYSTEM-RESEARCH-2026-07-30`.
This checkpoint authorizes no discovery for an unbound future task, candidate
installation or execution, trust expansion, MCP or Hook mutation, CC Switch
change, controller implementation, commit, or push.

## 2026-07-30 Huashu and PM current-component delta checkpoint

The next candidate-first read-only batch reused the existing user-starred
preflight, Round 02 decomposition, and governed scenario contracts rather than
restarting broad discovery. The live repository posture remained `main` at
`55659f30091990f7c589932e0379880de30dc403`, equal to `origin/main` and
`0/0` ahead/behind, with the intentionally large dirty worktree preserved.

Primary-source refresh bound:

- `alchaincyf/huashu-design` at
  `1572d431f1411c82ec0baea94dea6a45f6063b26`, tree
  `04501b8d8ac964e72d6b04afa933d9ab218dbc5f`;
- `phuryn/pm-skills` at
  `18468a95b427e70e258b51389796367c6f684e7d`, tree
  `514548cbf646ce42fb9ea9a8cc901f05373ab2ff`, exact release `v2.1.0`.

For `GEN-CREATIVE-01`, current Huashu has no atomic candidate: its single root
Skill remains coupled to a large media/toolchain/gate surface and an optional
blocking Hook, while the bound marginal ideas overlap current OpenAI-official
creative-production and product-design capabilities. Retain source metadata
and the current pin; reopen only after a fixed comparative brief proves a
named unmet marginal need.

For `GEN-ORG-DECISION-01` and `SE-MGMT-PRACTICE-01`, retain exactly two PM
static protocol candidates:

- `pm-execution/skills/stakeholder-map/SKILL.md`, blob
  `08e5ad24a830a8faef9942a6fd9acdd274261835`;
- `pm-execution/skills/strategy-red-team/SKILL.md`, blob
  `fe7b7feaaa7a7662d72aac226d40d3abea7e0596`.

The pair is complementary but scenario-incomplete. It is not approved,
installed, exposed, invoked, behaviorally preferred, or proof of a residual
self-authored gap. `pre-mortem`, `prioritization-frameworks`, and
`outcome-roadmap` are excluded only from this batch.

The durable evidence is
`USER-STARRED-HUASHU-PM-CURRENT-COMPONENT-DELTA-RESEARCH-2026-07-30` plus its
machine-readable registry and validator. No source checkout, temporary source
root, dependency install, third-party execution, CC Switch/config/Hook
mutation, model dispatch, portfolio mutation, commit, or push occurred.

## 2026-07-30 release/change current CC and Codex no-model checkpoint

`SE-RELEASE-CHANGE-01` now has a current local CC identity preflight for the
two approved Addy candidates. `ci-cd-and-automation` and
`shipping-and-launch` still match their exact release payload bytes and hashes,
have physical CC true-source trees, enabled CC database rows, exact
common/Claude/Codex links, and appear in the current task runtime inventory.
This is listing and projection evidence only.

The frozen fourteen-Skill whole-state preflight now fails, but the candidate
cohort did not drift. Database and projection differences are exactly:
`doc.enabled_codex 0 -> 1`, `pdf.enabled_codex 0 -> 1`, and the two
Codex-private links changing from absent to resolvable CC symlinks. Reversing
only those four changes in a deep-copied observation reproduces the frozen
database, projection, and whole-state fingerprints exactly. Manager binary,
settings, all CC trees, backups, and Codex disable-policy rows remain frozen.
Do not refresh the baseline or attribute the failure to the release candidates.

The npm CLI 0.146.0 app-server returned only six system Skills in both Plugin
arms and therefore cannot substitute for the Desktop carrier. A fresh Desktop
app-server process could not be created even after sandbox escalation because
WindowsApps ACLs returned WinError 5. No executable was copied, no ACL was
changed, and no model request occurred. Independent fresh Desktop exposure,
loader invocation, instruction delivery, behavior, and value remain open.

The durable record is
`HUMAN-AI-COLLABORATION-RELEASE-CHANGE-CURRENT-CC-CODEX-NO-MODEL-PREFLIGHT-2026-07-30`.
It authorizes no CC, projection, config, portfolio, Skill, model, release, or
self-authored mutation. The separate `doc`/`pdf` manager/host lifecycle
contradiction now requires reconciliation before any corrective action.

## 2026-07-30 repository-local temporary cleanup checkpoint

The former closeout cleanup-debt preview is now frozen historical evidence.
Under explicit authority to clean temporary/process artifacts, the thirty-five
exact repository-local `.tmp` targets in that preview were removed: 2,459
files, 811 nested directories, and 60,346,279 bytes. No unexpected path was
included, and the `.tmp` root is absent after cleanup.

The user-supplied research ZIP already had an exact durable copy under
`sources/user-supplied-human-ai-sdlc-research`. The only validator that still
depended on raw temporary state was migrated to a compact normalized audit
containing all five Sentinel events plus isolated configuration semantics,
without raw runtime state, machine-absolute paths, or secret material.

The durable transaction record is
`registry/closeout-cleanup-execution-2026-07-30.json`. This is a reproducible
stage checkpoint, not final program closeout. The external
`C:/Projects/agent-skills-curated` workspace, CC Switch, global configuration,
and consumer Skill roots were not inspected or modified by the cleanup.

Pre-commit screening found and then removed eight additional exact runtime-only
directories inside the multi-connection audit: four complete `codex-home`
trees and four empty workspaces, totaling 292 files, 124 nested directories,
and 9,058,784 bytes. The audit README, calibration log, and three compact run
reports remain. No SQLite, WAL, copied runtime Skill tree, or temporary lock
from that cohort is eligible for commit.

The post-cleanup 16-file SEM-03/control-chain test rerun also exposed one stale
live assumption: the Hook-mode probe still treated the dated
`~/.codex/hooks/capability-router` registration, handler, and policy as
mandatory current files. Those Hook assets are currently absent. The probe now
returns an explicit zero-model unavailable classification with zero handler
invocations instead of raising `FileNotFoundError`. This does not invalidate
the historical 2026-07-28 direct-handler evidence and does not authorize Hook
reinstallation, a weak-model run, or self-authored improvement work.

## 2026-07-31 Matt promoted-22 reuse and resource-pressure checkpoint

The exact current Matt promoted manifest remains 22 paths at
`2ab958093e83e0ec752e6c1c5932da465bf23e0c`; a live read-only upstream lookup
still resolved to that revision. The
[reuse boundary audit](../strategy/MATT-CURRENT-22-SKILL-REUSE-BOUNDARY-AUDIT-2026-07-31.md)
read all 22 packages and verified their 66 current CC Switch files against the
exact Git objects with zero mismatch. None is in upstream's `skills/personal/`
class. Seventeen contain substantive reusable methods or workflows, while two
are suite control-plane entries and three are thin composition entries.
Workflow and host prerequisites are not defects: reuse remains preferred when
a bound scenario fits them and incremental behavior is evidenced. This audit
performed no third-party execution, model trial, installation, enablement,
disablement, deletion, Hook change, or global configuration mutation.

The product and research plan now also retain user-observed Agent resource
pressure as a high-priority falsifiable hypothesis. "Zombie thread" is not one
accepted runtime state: context growth, active/loaded/persisted threads,
subagents/workers, MCP connections/subscriptions, child processes, and host
cache/rendering state must be separated. The target is an Agent-owned bounded
closed loop that observes, schedules or releases within existing authority,
verifies the result, and asks the user only at a genuine permission, trust,
cost, shared-ownership, destructive, or host-observability boundary. No cause
or release claim is accepted without repeated same-workload and idle-control
telemetry.

## 2026-07-31 Agent resource-pressure attribution continuation checkpoint

The resource-pressure direction now has a machine-checked offline contract:

- `registry/agent-resource-pressure-attribution-protocol-2026-07-31.json`;
- `scripts/evaluate_agent_resource_pressure_attribution.py`;
- `tests/fixtures/agent-resource-pressure-attribution-fixtures-2026-07-31.json`;
- `scripts/validate_agent_resource_pressure_attribution_protocol.py`;
- 26 deterministic zero-model cases plus unit and protocol tests.

The contract reuses the context-pressure advisory and MCP task-lifecycle
contracts. It does not treat "zombie thread" as a single state and does not
infer pressure from persisted-thread count or release from task completion.
Autonomous action is eligible only inside existing authority with a real host
actuator, reversibility, bound verification, and no new permission, cost,
destructive, or shared-owner boundary.

This is not live host telemetry, causation, resource saving, release, weak-Agent
acceptance, cross-host parity, or proof that a self-authored controller is
needed. Resume with the validator and targeted tests, then full
`scripts/verify.py`. Any live observation or actuation needs its own exact
host, workload, authority, metric, rollback, and claim boundary.

## 2026-07-31 Codex Desktop resource-observability preflight checkpoint

A live read-only Desktop preflight has now been recorded in:

- `registry/codex-desktop-resource-observability-preflight-2026-07-31.json`;
- `docs/strategy/CODEX-DESKTOP-RESOURCE-OBSERVABILITY-PREFLIGHT-2026-07-31.md`;
- `scripts/validate_codex_desktop_resource_observability_preflight.py`.

The bounded task listing returned 20 Codex tasks: two active and eighteen
not-loaded. The non-pinned page was truncated at 50 and is not a complete
persisted-task inventory. `codex mcp list` returned nine enabled configuration
entries, which remain configuration rather than loaded-runtime evidence.

Three observer-excluded process samples each found 118 Codex-runtime processes
and 34 TCP connections. The process tree contained six near-identical startup
cohorts, each with node_repl, Neo4j, Playwright, Context7, CodeGraph, and four
unclassified Node roots. Together the repeated cohorts contained 114
processes, about 5.49 GB summed working set, and about 3.58 GB summed private
bytes. Working-set sums may double-count shared pages, and the sample was
neither idle nor a controlled workload.

No surface joined task status, MCP configuration, and process identity through
an owner or lease. No context telemetry, lifecycle release receipt, or safe
task-scoped actuator was observed. Therefore pressure attribution, release
attribution, autonomous action, zombie-thread claims, leak claims, and
self-authored controller justification all remain false.

Pinned `rust-v0.146.0` source inspection found official app-server methods and
events for loaded threads, thread status, thread-scoped MCP startup ownership,
token usage, compaction, and background-terminal cleanup. The current Desktop
task surface did not expose the owner-bearing inputs. The older
manager-replacement leak issue is closed as completed, while broader
multi-task MCP, Playwright retention, and subscriber-presence issues remain
open. Treat those reports as corroborating risk, not as local causation.

No model turn, task mutation, MCP mutation, process stop/restart, Hook,
Plugin/App, or global-configuration change occurred. Continue with current
host exposure or safe read-only access to the official ownership/release
primitives. A disposable Desktop lifecycle transition requires a separate
exact authority and cleanup gate.

## 2026-07-31 Codex Desktop official control-surface access checkpoint

The installed `codex-cli 0.146.0` generated its experimental app-server schema
into an isolated `.tmp` root. The probe produced 349 files and 3,343,741 bytes;
seven relevant artifacts were hashed. Node process count and identity stayed
unchanged at 72 before and after generation. The generated root and empty
parent were removed.

The exact local schema confirms:

- `thread/loaded/list`;
- thread-scoped `mcpServerStatus/list`;
- MCP startup status with nullable `threadId`;
- thread token usage with model context window and compaction surfaces;
- thread background-terminal list, RSS/CPU/PID telemetry, clean, and
  terminate.

Background-terminal telemetry is not MCP-process telemetry. MCP status exposes
neither per-process resource metrics nor lease/subscriber count.

The current Desktop App tools exposed task list/read status but not the
owner-bearing protocol methods. No attached task terminal, default app-server
control directory, or Codex TCP listener was observed. Managed app-server
daemon lifecycle is unsupported on Windows. Upstream issue `#25914` remains
open for supported discovery/attachment to the current Desktop thread.

The resulting classification is:

- official protocol-definition gap: false;
- current Desktop owner-telemetry access gap: observed;
- current Desktop actuator access gap: observed;
- autonomous runtime action: ineligible;
- self-authored controller gap: unproved.

Do not start a separate app-server, mutate tasks, or infer ownership from
persisted sessions, timestamps, process cohorts, or UI labels. Prefer a
supported Desktop bridge. An isolated same-version no-model lifecycle trial
requires separate authorization.

## 2026-07-31 specialist tree-identity reconciliation checkpoint

The six-specialist static triage and the fourteen-Skill live preflight used
different record encodings for their tree-manifest digests. The static triage
used TAB between relative path, byte count, and file hash; the live preflight
used NUL. Both used LF between casefold-then-codepoint ordered records. The
five removal-preview candidates still match their static `SKILL.md` hashes,
file counts, byte counts, database content hashes, and CC-owned host links, but
the two tree digests are not directly comparable.

`registry/skill-portfolio-six-specialist-static-triage-2026-07-30.json` now
binds both algorithms, forbids direct cross-algorithm comparison, and requires
future previews to re-encode one ordered record set with one declared
algorithm. The validator rejects an unlabeled tree identity; the targeted
eight-test module and full `scripts/verify.py` pass.

The live fourteen-Skill whole-state preflight currently fails closed because
`doc` and `pdf` are again Codex-enabled and their Codex links are present.
That is a database/projection baseline drift, not a five-candidate failure.
The local CC Switch log records `show_main` at 2026-07-30 22:37:39, followed
by explicit Skill-state updates setting `doc` and `pdf` Codex enablement to
`true` at 22:38:17 and 22:38:23. Periodic Codex session-usage synchronization
continued before and after those events but did not record a Skill-state
change. This supports an explicit host-toggle event rather than automatic
session synchronization; the log does not identify the actor.
No CC Switch row, Skill, link, global configuration, or user-level state was
changed. At this checkpoint, restoring the prior CC Switch Codex flags was
treated as an explicit mutation gate. The canonical-identity checkpoint below
supersedes that proposed mutation after stronger official implementation
evidence showed the existing Codex host-disable still applies.

## 2026-07-31 layered doc/pdf identity and fourteen-preflight refresh checkpoint

The prior `doc`/`pdf` diagnosis conflated manager projection state with Codex
host enablement. Current CC Switch rows and private Codex aliases are enabled,
but the two official `skills.config enabled=false` entries remain in the Codex
configuration.

Pinned OpenAI Codex `rust-v0.146.0` source closes the identity question:

- path-based Skill config selectors are canonicalized;
- discovered `SKILL.md` paths are canonicalized;
- merged Skill roots deduplicate by canonical `path_to_skills_md`.

The common-root and private Codex aliases therefore resolve to the same
CC-owned identities, and the existing two disabled selectors continue to
apply. Alias presence alone does not prove duplicate host exposure. No
CC Switch toggle, config write, restart, separate app-server, task, turn, or
model request was needed.

The live preflight now accepts either no private alias or a complete pair of
private aliases resolving to the protected CC trees, while still requiring
both canonical host-disable rows. The refreshed contract is:

- `registry/cc-switch-fourteen-skill-live-preflight-contract-2026-07-31.json`;
- `docs/strategy/CC-SWITCH-FOURTEEN-SKILL-LIVE-PREFLIGHT-CONTRACT-2026-07-31.md`.

The current read-only preflight passes with 55 database rows, 55 CC trees,
57 Codex-root entries, 20 backups, all fourteen targets, and
`docPdfPolicyMode=canonical-host-disable`. It remains a point-in-time drift
gate and authorizes no uninstall or other mutation. A fresh separate
app-server exposure result is not claimed.

## 2026-07-31 fourteen-Skill layered transaction forecast checkpoint

The 2026-07-30 manager transaction preview still froze the correct fourteen
targets, CC Switch 3.19 manager semantics, backup rotation, canary, rollback,
cleanup, and exact authorization boundary. Its Codex post-state count became
stale after CC Switch restored the two private `doc`/`pdf` aliases.

The compact delta overlay is:

- `registry/cc-switch-fourteen-skill-subtraction-preview-3.19-layered-refresh-2026-07-31.json`;
- `docs/strategy/CC-SWITCH-FOURTEEN-SKILL-SUBTRACTION-PREVIEW-3.19-LAYERED-REFRESH-2026-07-31.md`.

It reuses the frozen transaction rather than duplicating it. The current Codex
root forecast is corrected from 57 to 43 after removing the unchanged fourteen
targets. Database, CC, common agents, and Claude forecasts remain 41, 41, 27,
and 41. The two `doc`/`pdf` aliases and canonical host-disable rows remain
unchanged sentinels.

No uninstall, backup eviction, common-link cleanup, remote snapshot, recovery
archive, debug helper, host toggle, configuration write, restart, or model turn
was authorized or executed. Rerun the live preflight immediately before any
separately authorized canary.

## 2026-07-31 AI-era engineering-principles revalidation checkpoint

The product, program, research, and existing acceptance carriers now include a
bounded AI-era revalidation lens for classical software-engineering
principles. The durable package is:

- `docs/strategy/AI-ERA-CLASSICAL-SOFTWARE-ENGINEERING-PRINCIPLES-REVALIDATION-2026-07-31.md`;
- `registry/ai-era-classical-software-engineering-principles-revalidation-2026-07-31.json`;
- `scripts/validate_ai_era_classical_software_engineering_principles_revalidation.py`.

The four outcomes are candidate invariant, adaptive practice, obsolete
ceremony, and insufficient evidence. The package keeps productivity and
quality findings bounded, separates a principle from its historical workflow,
and requires original failure mode, AI-era effect, current capability
coverage, deterministic verification, residual human judgment, creativity and
maintenance cost, counterexamples, fallback, and retirement conditions.

It reuses four existing acceptance carriers and keeps the acceptance inventory
at 61. `acceptance.standard-candidate-contract` advances only from `planned` to
`partial`: the checked-in contract and negative tests do not prove repeated
cross-host value, CALIBRATION review, ASSETS admission, or hard-standard
authority.

When the paused goal is resumed, append this exact decision constraint:

> Treat AI-era revalidation of classical software-engineering principles as a
> horizontal research and evaluation lens. Separate candidate invariants,
> adaptive practices, obsolete ceremonies, and insufficient-evidence items;
> assess original failure mode, AI-era effect, current ecosystem coverage,
> deterministic verification, residual human judgment, creativity and
> maintenance cost, counterexamples, and retirement conditions. Do not restore
> rigid legacy process because a principle regained importance, and do not
> weaken intent, evidence, responsibility, verification, rollback, or long-term
> system-health requirements because AI improved generation speed. This lens
> does not reorder current candidate work, prove a self-authored gap, or promote
> a hard standard without separate repeated evidence and authority.

This checkpoint authorizes no capability installation, CC Switch, Skill, Hook,
model, runtime, cross-repository, CALIBRATION, ASSETS, commit, push, or release
mutation.

## 2026-07-31 multidimensional software-engineering evaluation checkpoint

The repository now has a carrier-neutral evaluation contract that is broader
than the AI-era classical-principle revalidation lens:

- `docs/strategy/MULTIDIMENSIONAL-SOFTWARE-ENGINEERING-EVALUATION-CONTRACT-2026-07-31.md`;
- `registry/multidimensional-software-engineering-evaluation-contract-2026-07-31.json`;
- `scripts/validate_multidimensional_software_engineering_evaluation_contract.py`.

The contract composes current official lifecycle, product-quality,
quality-in-use, evaluation-process, security, supply-chain, delivery,
developer-productivity, and AI-risk sources. It defines twelve dimensions,
six adaptive profile axes, evidence grades, uncertainty, six candidate
hard-floor hypotheses, and a report contract. One scalar total score,
cross-dimension cancellation, treating unknown as zero, and hiding a blocked
floor are prohibited.

The architecture keeps the evaluation ontology, candidate hard floors,
deterministic validators, adaptive profiles, optional orchestration, and
accountable acceptance separate. Existing native, official, repository, and
reviewed Skill capabilities cover subproblems only. A new evaluation Skill is
not currently justified. The deterministic report schema, positive fixture,
and cross-field validator now pass without a model:

- `registry/multidimensional-software-engineering-evaluation-report-schema-2026-07-31.json`;
- `tests/fixtures/multidimensional-software-engineering-evaluation-report-positive-2026-07-31.json`;
- `scripts/validate_multidimensional_software_engineering_evaluation_report.py`.

The first bounded application is
`registry/multidimensional-software-engineering-evaluation-contract-package-assessment-2026-07-31.json`.
It binds the immutable `bb65a26` eight-file evaluation core through exact Git
objects and a deterministic manifest. It is deliberately a same-system
self-assessment with `independentReview=not-performed`,
`acceptanceAuthority=not-sought`, and `statusClaim=needs-verification`.
It preserves insufficient-evidence and unassessed dimensions instead of
turning local verification into independent validity, real-domain usefulness,
cross-host value, efficiency, project-wide quality, or standard eligibility.

The next bounded implementation is the same schema applied to one
non-self-referential software change with independent review.

The package reuses five existing acceptance carriers, keeps the inventory at
61, changes no assessment, and promotes no hard standard. No CC Switch, Skill,
Hook, model, external capability, cross-repository, CALIBRATION, ASSETS,
commit, push, release, or deployment mutation follows from this checkpoint.

## 2026-07-31 dynamic evaluation and non-self-referential checkpoint

The multidimensional evaluation direction remains inside the Harness north
star: software engineering is one priority lifecycle specialization of
human-AI collaboration, not the whole problem domain. The evaluation system is
not a Skill-only design. It now separates six carrier-neutral layers:
evaluation ontology, source intelligence and freshness, candidate hard floors,
deterministic validators, adaptive soft profiles, and optional orchestration.
Accountable acceptance remains outside all six.

Task-bound network research may refresh time-sensitive standards, law,
vulnerabilities, dependencies, hosts, capabilities, and reviewed practices.
Dynamic discovery is not dynamic truth. Every evaluation must freeze source
identity, owner, retrieval time, revision or digest, freshness, conflict,
bounded use, limitation, and applicability. A search or catalog hit is not
evidence. If refresh is unavailable, use the last reviewed pinned snapshot,
mark it stale or unavailable, narrow the claim, and schedule revalidation.

Soft evaluation policy and hard red lines may coexist. Profiles and
recommendations adapt to domain, lifecycle stage, risk, host, and evidence.
Any future admitted hard floor must remain AI-independent and mandatory where
applicable. Admission waits for the floor-relevant collaboration, lifecycle,
domain, counterevidence, and operational dependencies, not every unrelated
research question. A high-confidence safety, truth, authority, privacy, or
destructive-action floor may receive independent early review, but urgency
cannot waive proof, scope, false-positive, fallback, migration, retirement, or
acceptance authority. The current candidates remain below that gate. A future
Skill may orchestrate refresh, snapshot construction, profile selection,
evidence collection, validation, and explanation only after repeated
residual-gap proof; it cannot be the sole carrier, own truth, waive a floor, or
mark acceptance.

Preserve the existing neutral evidence-and-authority boundary. Separate task
inputs, observation, evidence, interpretation, preference, inference,
authorization, and acceptance. Claim scope must match affected parties,
context, domain, jurisdiction, and accountable authority. Broad claims require
representative evidence, material counterevidence, applicable accessibility,
exclusion and distributional-impact review, independent review, and
accountable acceptance. A collaboration correction does not create a new
project value direction.

The second schema-conformant report is:

- `registry/multidimensional-software-engineering-codex-desktop-resource-observability-assessment-2026-07-31.json`.

It binds the non-self-referential commit
`202cf04732272c646be2c8c15bf2bbccb749d113` and all eight changed Git objects.
The target validator and its unit test pass in an exact isolated archive.
The same archive's full `scripts/verify.py` fails on an inherited historical
process-fidelity `RAW-REPORT.json` hash: the retained Windows-CRLF runtime-byte
digest is `6c40c5...`, while the Git-normalized LF archive digest is
`821861...`. All four files in that historical package have the same semantic
class of divergence, and downstream records preserve those captured hashes.
This is an exact-checkout portability and hash-semantics defect, not evidence
that the resource-observability target logic failed. No partial hash migration
was attempted without a governed compatibility contract.

The report therefore keeps `statusClaim=needs-verification`, records no
independent review or acceptance, and blocks the evidence/provenance floor.
Passing target checks do not cancel that counterevidence. The next order is:
independent review, frozen source-snapshot implementation, broader profile
applications, orchestration-burden comparison, then a Skill necessity decision.
Hard-floor admission remains later and separately governed.

The exact temporary archive root and tar were deleted after evidence capture;
`.tmp` is absent. This checkpoint authorizes no capability installation,
CC Switch, Skill, Hook, model, runtime, process, thread, global configuration,
cross-repository, CALIBRATION, ASSETS, release, deployment, or hard-standard
mutation.

## 2026-07-31 deterministic evaluation source-snapshot checkpoint

The source-intelligence layer now has one real calibration instead of only a
policy statement. It separates a non-deterministic read-only observation from
a pure offline builder and verifier:

- `registry/multidimensional-software-engineering-source-snapshot-contract-2026-07-31.json`;
- `registry/multidimensional-software-engineering-source-observation-2026-07-31.json`;
- `registry/multidimensional-software-engineering-source-snapshot-2026-07-31.json`;
- `scripts/build_multidimensional_software_engineering_source_snapshot.py`;
- `scripts/validate_multidimensional_software_engineering_source_snapshot.py`;
- `tests/test_multidimensional_software_engineering_source_snapshot.py`;
- `docs/strategy/MULTIDIMENSIONAL-SOFTWARE-ENGINEERING-SOURCE-SNAPSHOT-2026-07-31.md`.

The observation binds all twelve official locators already declared by the
parent evaluation contract. The builder performs no network operation. It
requires exact parent source-set, owner, and locator equality; merges bounded
use and limitation from the parent into separately labeled parent fields;
retains the narrower snapshot use from the observation; binds parent,
snapshot-contract, and observation bytes; sorts source rows; and adds a
canonical manifest digest.
The verifier reconstructs the checked-in snapshot offline. Negative tests
reject source omission, locator drift, an unadmitted refresh trigger, ISO
rights-boundary escalation, normative promotion of a mutable unpinned source,
an archive without a digest, and manifest drift.

No raw external body or page digest is retained. Five ISO sources are
bibliographic-metadata-only because their official pages state AI-use
restrictions. OWASP SAMM v2 remains a mutable unpinned model-family page.
ISO/IEC 5055 remains explicitly ambiguous because its official page presents a
published standard and stage 90.60 close-of-review or under-review language.
Two parent-status claims are only partially observed on their bound locators.
These are evidence-strength and rights findings, not formatting defects.

The snapshot records a model-mediated dated locator observation and proves
exact parent-source-set freezing plus deterministic offline repository
reconstruction. Because no network receipt is retained, it does not
independently prove locator reachability, external content bytes, full
normative meaning, correct interpretation, current cross-jurisdiction
applicability, independent review, evaluation Skill necessity, hard-standard
eligibility, or acceptance. The acceptance inventory remains 61 and no
assessment is promoted.

A pre-push exact Git archive confirms that the new snapshot builder `--check`
and dedicated validator both pass after tracked JSON is normalized to LF. The
same archive's full `scripts/verify.py` still fails at the inherited historical
process-fidelity `RAW-REPORT.json` hash mismatch: the retained
Windows-CRLF runtime-byte digest differs from the Git-LF durable bytes. This is
the already-recorded repository portability debt, not a new source-snapshot
failure. The exact archive root and tar were deleted; no process path remains.

The source snapshot is retained as a provisional screening input. Candidate
capability coverage, overlap, conflict, host exposure, behavior, and value
evidence now precede further contract expansion or standard admission. The
prepared independent-review mechanism remains available for a later revised
target; its frozen pre-correction packet is not the current critical path. No
automatic refresh, content
vendoring, license override, Skill, Hook, MCP, Plugin, App, model, CC Switch,
global configuration, hard-standard, CALIBRATION, ASSETS, release, or
deployment mutation is authorized by this checkpoint.

## 2026-07-31 multidimensional engineering independent-review readiness checkpoint

The next review gate is prepared but has not been performed. The durable
package is:

- `docs/strategy/MULTIDIMENSIONAL-SOFTWARE-ENGINEERING-INDEPENDENT-REVIEW-READINESS-2026-07-31.md`;
- `registry/multidimensional-software-engineering-independent-review-readiness-contract-2026-07-31.json`;
- `registry/multidimensional-software-engineering-independent-review-packet-2026-07-31.json`;
- `scripts/build_multidimensional_software_engineering_independent_review_packet.py`;
- `scripts/validate_multidimensional_software_engineering_independent_review_packet.py`;
- `tests/test_multidimensional_software_engineering_independent_review_packet.py`.

The packet binds nineteen exact Git blobs at
`c634d83d84b269ab66a94eba1540d04133e308dd`, including the contract, both
bounded reports, source observation and snapshot, deterministic mechanisms,
tests, and then-current program projections. It records each Git object id and
SHA-256 content digest plus a canonical manifest digest. Reconstruction needs
the repository Git objects but no network or model.

The standalone packet validator rederives all nineteen bindings from Git.
Repository-wide verification uses a separate retained-integrity entry point so
an exact Git archive without historical objects can still validate the
contract, packet digest, embedded manifest, and program projections. That
archive-only path does not claim historical Git-object rederivation.

A pre-push exact Git archive passed the source-snapshot builder and validator
and the new packet retained-integrity check. Full `scripts/verify.py` stopped
only at the inherited process-fidelity `RAW-REPORT.json` hash boundary: the
Windows-CRLF working-tree digest remains
`6c40c51606cc6ed2330b3d725d9a81d0beea351a8420bbbefb4a93f8c7273c88`
and the Git-LF archive digest remains
`821861719e20fa601caf861781bdb32d005d5266fa3ab76e03024c37144453fd`.
This is the already-recorded portability counterevidence, not a new packet
failure. The exact archive root and tar were deleted after inspection.

The future receipt requires a reviewer identity and process distinct from the
packet producer and both report evaluators, prior-involvement disclosure,
identity evidence, no private-reasoning transfer, and no reviewer mutation of
the target. Ten non-cancelling axes cover standards/specification, rights,
evidence strength and freshness, conflicts, dimension/floor judgments,
evidence scope, affected-party and authority boundaries, CRLF/LF
portability, deterministic negative controls, and capability reuse. The
repository can validate the declaration shape but cannot cryptographically
prove an identity that the host does not attest.

The current task did not create a review receipt, dispatch a model, create a
task or subagent, or exercise acceptance authority. Same-task rereading and
review-Skill invocation are explicitly insufficient for independence. The
acceptance inventory remains 61 and
`acceptance.standard-candidate-contract` remains `partial`. Readiness does not
promote a hard floor, prove a new Skill necessary, or authorize release.

The packet is a reusable review-mechanism calibration for its frozen target,
not current standard-admission evidence. Candidate capability intake,
verification, coverage mapping, and subtraction are the main critical path.
When a revised evaluation target becomes decision-relevant, build a new packet
for a separately accountable reviewer. No external capability, CC Switch,
Skill, Hook, MCP, Plugin, App, global configuration, weak-model, CALIBRATION,
ASSETS, acceptance, release, or deployment mutation is authorized by this
checkpoint.

## 2026-07-31 sequencing correction

The project's neutral scope and authority model remain unchanged. Current
authority surfaces express the general evidence-scope, affected-party,
permission, and acceptance separation needed for bounded claims. Historical
test artifacts remain historical; no new value doctrine is created by this
checkpoint.

The standardization sequence is also corrected. The current multidimensional
ontology remains a provisional screening coordinate system, not a final
standard. The next mainline work is to broaden and verify native, official,
reviewed external, and composed candidate coverage; map overlap, conflict,
fallback, lifecycle, host, permission, and evidence states; and subtract or
revise the ontology from comparison results. A new Skill, hard floor, or final
standard remains downstream of repeated residual-gap and admission evidence.
The existing independent-review packet is retained as mechanism calibration
but deferred because it binds the pre-correction target.

The immutable `bb65a26` same-system assessment is likewise retained as
historical pre-correction evidence. It does not assess the revised contract and
must not be projected as current review or standard-admission evidence.

## 2026-07-31 GEN-LEARNING-01 capability-baseline checkpoint

Candidate coverage has moved from the corrected sequencing statement into one
bounded general-domain slice. The durable package is:

- `docs/strategy/HUMAN-AI-COLLABORATION-LEARNING-CAPABILITY-BASELINE-2026-07-31.md`;
- `registry/human-ai-collaboration-learning-capability-baseline-2026-07-31.json`;
- `scripts/validate_human_ai_collaboration_learning_capability_baseline.py`;
- `tests/test_human_ai_collaboration_learning_capability_baseline.py`.

`GEN-LEARNING-01` remains `planned-no-live-domain-evidence`. Its bound task,
educator authority, minimum-data boundary, delayed and transfer measurements,
misconception oracle, teacher review, and human-only control are unchanged.
The Shen and Tamkin claim remains a bounded design input rather than a general
learning-effect claim.

The static representative set is native/no dedicated capability, one
host-specific official learning mode, exact current Matt `teach` as an
explicit task-bound treatment, and a human instructor control. OpenAI Study
mode, Anthropic learning mode, and Gemini Guided Learning are official product
surfaces, not a single portable treatment and not current-account, outcome, or
cross-host proof. Matt `teach` is pinned at
`2ab958093e83e0ec752e6c1c5932da465bf23e0c`; its exact six-file projection and
CC Switch authority-root topology are recorded, while instruction delivery,
behavior, retention, and transfer remain unproved.

Broader third-party discovery stops for this scenario until a concrete recheck
trigger appears. The residual is a behavioral-evidence gap, not a
capability-name gap. The next bounded action is protocol design with no
installation or account expansion, followed by simulation; only effects that
cannot be established deterministically should advance to manual or
real-learner testing. No candidate execution, model dispatch, CC Switch,
runtime, Skill, Hook, hard-standard, cross-repository, or remote mutation is
authorized by this checkpoint.

## 2026-07-31 GEN-LEARNING-01 fixed-fixture protocol checkpoint

The capability baseline now has a zero-model protocol successor:

- `docs/strategy/HUMAN-AI-COLLABORATION-LEARNING-FIXED-FIXTURE-PROTOCOL-2026-07-31.md`;
- `registry/human-ai-collaboration-learning-fixed-fixture-protocol-2026-07-31.json`;
- `scripts/evaluate_human_ai_collaboration_learning_protocol.py`;
- `scripts/validate_human_ai_collaboration_learning_fixed_fixture_protocol.py`;
- `tests/fixtures/human-ai-collaboration-learning-protocol-fixtures-2026-07-31.json`;
- `tests/test_human_ai_collaboration_learning_fixed_fixture_protocol.py`.

The ordered phases are zero-model mechanism simulation, no-model material and
oracle calibration, separately authorized mechanism pilot, and only then a
preregistered human-learning trial. The current phase is the first. Ten
fixtures cover a valid mechanism-only record and fail-closed boundaries for
immediate-completion claims, provider pooling, private-oracle exposure, missing
authority, weak-model substitution, installed-only Matt exposure, incomplete
cleanup, missing delayed assessment, and premature effect claims.

Any future weak-Agent native/Matt pair must visibly use
`gpt-5.3-codex-spark` with `low` reasoning. The official arm selects exactly
one real host product and remains a product-bundle comparison. The formal
design is between-subject to prevent treatment carryover. Delayed unaided
competence is primary; immediate competence, transfer, misconception,
autonomy, accessibility, teacher review, and lifecycle cost cannot cancel one
another.

No model, candidate, account, or participant surface was used. The next action
is an offline synthetic micro-domain public packet and private oracle with
parallel assessments, misconception map, accessibility review, and cleanup
manifest. Model dispatch, candidate execution, account access, participant
contact, live trial, CC Switch, runtime, Skill, Hook, hard-standard, evidence
promotion, commit, and push remain unauthorized by the protocol itself.

## 2026-07-31 GEN-LEARNING-01 Noru material-calibration checkpoint

The no-model material gate now has a concrete synthetic micro-domain package:

- `docs/strategy/HUMAN-AI-COLLABORATION-LEARNING-NORU-MATERIAL-CALIBRATION-2026-07-31.md`;
- `registry/human-ai-collaboration-learning-noru-material-calibration-2026-07-31.json`;
- `tests/fixtures/human-ai-learning-noru-public-packet-2026-07-31.json`;
- `tests/fixtures/human-ai-learning-noru-private-oracle-2026-07-31.json`;
- `scripts/validate_human_ai_collaboration_learning_noru_material_calibration.py`;
- `tests/test_human_ai_collaboration_learning_noru_material_calibration.py`.

The public `Noru cards` packet carries all meaning through text and requires no
personal, education, employment, color, image, or audio input. It contains five
rules, four worked examples, eight practice items, interaction limits,
accessibility options, and a minimum-data boundary. It contains no assessment
answer or misconception key.

The evaluator-only oracle contains a three-item non-Noru rule-application
pretest, six immediate items, six delayed items, three novel-transfer items,
eight misconception classes, and scoring boundaries. The validator executes
the Noru rules, recomputes twelve transform or compatibility cases plus one
inverse case, rejects answer leakage and item overlap, and binds future cleanup
to `.tmp/aah-learning-noru/<trial-id>`.

Structural and oracle-logic calibration passed. Empirical form difficulty,
human readability, accessibility in use, misconception coverage, participant
safeguards, power, treatment exposure, and learning effects remain unproved.
The same producing identity cannot supply independent human review. No model,
candidate, official account, participant, live trial, CC Switch, runtime,
Skill, Hook, hard-standard, matrix promotion, external write, commit, or push
is authorized by this calibration record.

## 2026-07-31 GEN-CREATIVE-01 capability-baseline checkpoint

The source-bound creative baseline now lives at:

- `registry/human-ai-collaboration-creative-capability-baseline-2026-07-31.json`;
- `docs/strategy/HUMAN-AI-COLLABORATION-CREATIVE-CAPABILITY-BASELINE-2026-07-31.md`;
- `scripts/validate_human_ai_collaboration_creative_capability_baseline.py`;
- `tests/test_human_ai_collaboration_creative_capability_baseline.py`.

It binds native open divergence, current OpenAI Creative Production `0.1.25`,
conditional explicit `grilling`, and a human-led control. Superpowers
`brainstorming` is excluded as a treatment because its complete workflow is
software-development specific. Current Huashu still supplies no atomic
candidate for this scenario.

The existing six-example Unknown Knowns creative preference packet is reused;
do not create a second equivalent fixture. The package records that project
neutrality did not change: aesthetic judgment stays a legitimate human-owned
input and outcome, while no route receives automatic evidentiary authority.

`GEN-CREATIVE-01` remains `planned-no-live-domain-evidence`. No model, plugin,
MCP, board, generation, account, install, CC Switch, Hook, Skill-authoring,
standard-promotion, cross-repository, or remote-push authority follows from
this checkpoint. The next live gate, if separately authorized, is exact
exposure and instruction-delivery preflight followed by deterministic packet
validation, provenance logging, and blind human review against native and
human-only controls.

## 2026-07-31 GEN-ACCESS-COMMS-01 capability-baseline checkpoint

The durable package is:

- `registry/human-ai-collaboration-access-comms-capability-baseline-2026-07-31.json`;
- `docs/strategy/HUMAN-AI-COLLABORATION-ACCESS-COMMS-CAPABILITY-BASELINE-2026-07-31.md`;
- `scripts/validate_human_ai_collaboration_access_comms_capability_baseline.py`;
- `tests/test_human_ai_collaboration_access_comms_capability_baseline.py`.

It reuses the frozen nine-case access/comms protocol and fixture as the
deterministic structured semantic gate. The ordered candidate route is source
and terminology lock, native draft adaptation, that existing gate, conditional
OpenAI Documents for a relevant document carrier, and human bilingual, domain,
accessibility, recipient, and release review. No duplicate fixture was added.

Official Documents `26.730.11710` is a carrier and narrow structural
accessibility supplement, not a translation engine or full WCAG engine.
Installed `edit-article` remains excluded as a treatment and is not removed.
Targeted external discovery records Microsoft Azure Translation SDK Skills,
Weblate glossary checks, Translate Toolkit `pofilter`, and W3C WAI references;
none closes the combined semantic, accessibility, recipient, and authority
surface for this bounded scenario without extra lifecycle or account costs.

Project neutrality is unchanged. `GEN-ACCESS-COMMS-01` remains
`planned-no-live-domain-evidence`. The residual is behavioral and recipient
evidence, not another candidate name or a proved self-authored gap. No model,
service, account, installation, CC Switch, Plugin, MCP, runtime, Skill, Hook,
standard, cross-repository, or remote-push authority follows from this
checkpoint.

## 2026-07-31 GEN-ORG-DECISION-01 zero-model checkpoint

The durable package is:

- `docs/strategy/HUMAN-AI-COLLABORATION-ORG-DECISION-ZERO-MODEL-CALIBRATION-2026-07-31.md`;
- `registry/human-ai-collaboration-org-decision-zero-model-protocol-2026-07-31.json`;
- `tests/fixtures/human-ai-collaboration-org-decision-zero-model-calibration-2026-07-31.json`;
- `scripts/evaluate_human_ai_collaboration_org_decision_zero_model_calibration.py`;
- `scripts/validate_human_ai_collaboration_org_decision_zero_model_protocol.py`;
- `tests/test_human_ai_collaboration_org_decision_zero_model_calibration.py`.

The exact PM candidate pin remains current. The future comparison arms are
native, `stakeholder-map`, `strategy-red-team`, their ordered composition, and
a facilitated human-only control. No source projection or installation was
performed. OpenAI Data Analytics is a conditional evidence and monitoring
supplement only; current `grilling`, `research`, and `review` are not treatment
arms.

The synthetic option packet calibrates one control and fourteen faults. It
fails closed on missing evidence or affected parties, flattened dissent or
incentives, option collapse, absent rollback or monitoring, Agent decision
authority, erased unknowns, inferred stakeholder facts, unsupported scores,
unauthorized communication, individual surveillance, and false consensus. The
existing cumulative-loss implementation is reused, and terminal repair cannot
erase historical process loss.

This is zero-model mechanism evidence only. `GEN-ORG-DECISION-01` remains
`planned-no-live-domain-evidence`; simulated recovery is not human or
affected-party participation. No model, candidate, organization, account,
installation, CC Switch, Plugin, MCP, Hook, communication, decision,
implementation, self-authored Skill, hard-standard, cross-repository, or
remote-push authority follows from this checkpoint.

## 2026-07-31 SE-MGMT-PRACTICE-01 zero-model checkpoint

The durable package is:

- `docs/strategy/HUMAN-AI-COLLABORATION-ENGINEERING-MANAGEMENT-ZERO-MODEL-CALIBRATION-2026-07-31.md`;
- `registry/human-ai-collaboration-engineering-management-zero-model-protocol-2026-07-31.json`;
- `tests/fixtures/human-ai-collaboration-engineering-management-zero-model-calibration-2026-07-31.json`;
- `scripts/evaluate_human_ai_collaboration_engineering_management_zero_model_calibration.py`;
- `scripts/validate_human_ai_collaboration_engineering_management_zero_model_protocol.py`;
- `tests/test_human_ai_collaboration_engineering_management_zero_model_calibration.py`.

The route stops broad discovery and retains five future arms: native, current
OpenAI Data Analytics, exact PM two-component composition, ordered
official-plus-PM composition, and a team-led human control. The PM pin still
equals upstream `main`. Current `wayfinder` is retained as a useful
tracker-backed long-horizon planning near match but excluded as an estimation,
calibration, anti-surveillance, or management-authority treatment.

The protocol reuses the organizational-decision cross-cut, then adds
management-specific forecast/outcome, non-coercive commitment, quality and
economic trade-off, aggregate measurement, anti-surveillance, professional
accountability, and perception-versus-observed-outcome gates. Four synthetic
forecast/outcome records are parent-recomputed. One control and seventeen
faults pass deterministic calibration; terminal repair cannot erase historical
loss.

`SE-MGMT-PRACTICE-01` remains `planned-no-live-domain-evidence`. Simulated
team-owned recovery is not team participation, worker review, management
acceptance, forecast improvement, delivery value, fairness, competence, or
organizational causality. No model, candidate, real team, account, installation,
CC Switch, Plugin, MCP, Hook, staffing, budget, deadline, contract,
professional-accountability, self-authored Skill, hard-standard,
cross-repository, or remote-push authority follows from this checkpoint.

## 2026-07-31 cleanup-boundary unknown-key fail-closed correction

The broad non-`test_verify_integration` suite exposed two pre-existing negative
tests in `test_harness_three_lane_program_acceptance_reconciliation`: they
injected legacy or misspelled cleanup-authority keys, while the validator
checked required values but did not reject unknown keys. The mutations were
therefore ignored rather than failing closed.

`scripts/validate_harness_three_lane_program_acceptance_reconciliation.py` now
requires the exact seven-key `cleanupBoundary` schema. The focused 26-test
module and the 1,845-test non-slow-integration set pass. This correction grants
no cleanup or deletion authority and changes no historical cleanup evidence.
The initial complete discovery run that includes `test_verify_integration`
reached a 304-second outer timeout without diagnostic output. A subsequent
root-cause timing pass established that this was an insufficient caller budget,
not a code-stack failure: the integration module alone passed all 379 tests in
347.590 seconds (350.011 seconds wall clock), and the complete discovery run
then passed all 2,224 tests in 504.803 seconds (508.521 seconds wall clock).
The module contains 114 negative-test call sites that route through helpers
which re-run top-level `verify()`; one successful verifier profile included 31
subprocess calls and took about seven seconds. The repository CI already runs
the complete discovery command without a repository-imposed short timeout, so
no duplicate runner, weakened integration assertion, or validation-semantic
change was introduced. Future short caller timeouts must be recorded as
inconclusive unless they also return a test failure.

## 2026-07-31 SEM-03 Codex 0.146 no-model exposure refresh

The fixed Matt revision
`ed37663cc5fbef691ddfecd080dff42f7e7e350d` was re-materialized through the
existing fetch-all-then-write builder into
`.tmp/aah-sem03-composition-projection-20260731-v3`. All eight raw files and the
MIT license matched their pinned Blob and SHA-256 identities; the projected
tree digest remained
`295c4f5819f38e49cd4955d81294a5da1ce3197d78fc52c24bfecaf92027daa5`.

The first Codex `0.146.0` no-turn exposure attempt stopped before Skills
enumeration. Its stderr identified an inherited-probe compatibility boundary:
adding `enabled=false` to each configured MCP caused the newer parser to reject
the `codegraph` table as an invalid transport. No candidate instruction,
thread, turn, or model request ran, and no report was written. Two test-first
regressions now require a temporary Codex home and a whole empty MCP-table
override; a third requires the isolation receipt in every passing report.

The repaired probe passed on `Codex Desktop/0.146.0`. It observed exactly the
three required repo Skill paths and raw hashes, zero configurable Skills in the
unselected arm, and only those three in the selected arm. The isolated snapshot
contained 50 Skills (`3` repo, `6` system, `41` user); the older `0.145.0`
snapshot contained 114. This is dated host/inventory drift only. The temporary
Codex home was removed, and global config, projected bytes, and repository
status were stable. The durable report is
`audits/human-ai-collaboration-semantic-authority-current-matt-no-model-exposure-2026-07-31/REPORT.json`.

This refresh proves current-host dependency-complete exposure only. It proves
no loader or instruction delivery, named composition, relative-file delivery,
behavior, semantic continuity, value, portfolio preference, or cross-host
equivalence. All model runs remain zero. Before any separate weak-model gate,
complete native-disabled exposure, local adapted-monolith exposure, and
public-packet private-oracle leakage rejection.

## 2026-08-01 SEM-03 remaining no-model admission checkpoint

The remaining three declared no-model gates are now evidenced by
`audits/human-ai-collaboration-semantic-authority-native-local-no-model-exposure-2026-08-01/REPORT.json`.
The current Codex `0.146.0` app-server used a temporary empty Codex home, an
empty MCP-table override, and one temporary treatment root under `.tmp`.
Across one stable 48-Skill identity set (`1` repo, `6` system, `41` user), the
native arm enabled zero configurable repository/user Skills and preserved the
six system Skills. The local arm enabled only the exact repository-pinned
5,340-byte `grill-with-docs` monolith at SHA-256
`e1078020c41b954638ba94acda95a3340739908bd68b1db9bc2af129d3936035`.

The public packet builder now rejects file-set and digest drift plus a
non-public private-oracle canary. The positive packet passed. A full oracle-file
mutant failed with `hard-fail-unmanifested-public-file` and
`hard-fail-private-oracle-leak`; a partial-canary mutant failed with
`hard-fail-private-oracle-leak` and
`hard-fail-public-file-digest-drift`. This proves those declared negative
classes, not universal semantic side-channel absence.

No thread, turn, or model request ran. The temporary Codex home, packet
mutants, and treatment root were removed; `.tmp` is absent. Global config,
local Skill bytes, and repository status were stable. The protocol status is
`no-model-admission-complete-live-dispatch-not-authorized`. Loader and
instruction delivery, named composition, relative-file delivery, behavior,
semantic continuity, treatment value, and cross-host value remain unproved.
Do not dispatch a weak Agent directly. First bind one exact disposable packet,
visible `gpt-5.3-codex-spark`/`low` routing, parent-observed evidence,
cost/sandbox/stop boundaries, and separate dispatch authority.
