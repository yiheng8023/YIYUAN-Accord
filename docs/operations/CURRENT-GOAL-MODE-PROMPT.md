# Current Goal-Mode Prompt

<!-- semantic-projection: portfolio-tasktime-projection-v1 -->

Use this as the compact execution projection for the current research phase.
It is derived guidance, not product truth or authority. Verify repository truth
and the current machine authorities before acting.

## Goal

Reduce the user's Agent learning and orchestration burden without taking away
human control over goals, consequential judgment, trust boundaries, or bounded
authorization.

## Current scheduler

1. Continue bounded portfolio curation. Portfolio curation does not require a
   real task when its coverage objective, source boundary, inactive isolation,
   review criteria, authority, verification surface, cleanup, and stop rule are
   explicit. Discover broadly but process only a bounded cohort at a time;
   preserve exact upstream payloads and keep them inactive by default.
2. Use synthetic fixtures, zero-model probes, failure injection, and static
   checks for mechanism evidence. Do not upgrade these results into behavior,
   value, portability, or production claims.
3. Require a naturally occurring real task and a demonstrated current-path gap
   only before task-time activation, instruction-delivery, behavior, value, or
   production claims. Do not ask the user to invent work so the portfolio can
   progress.
4. Prefer healthy native/runtime capability, then official capability, then a
   reviewed maintained external implementation, then composition. Author a
   repository-owned residual-gap solution only after those paths fail with
   reproducible material evidence.

## Manager and freshness boundary

CC Switch is the current replaceable operational manager where suitable, not a
portable product dependency. The dated source binding is v3.19.2. Its
`skills.sh` discovery surface may add an
author's repository to the CC repository list; CC refresh, hash comparison,
update, backup, enablement, and projection then remain the managed lifecycle.
Check the exact upstream repository and revision independently because catalog
metadata and counts may lag.

Do not run an author's direct installer into live project or Agent Skill roots
and still call the result CC-managed. A direct installer may be inspected or
used only in an isolated inactive acquisition root unless a separately reviewed
adapter imports the exact result into CC and verifies SSOT, database, enablement,
backup, update, and consumer projections.

## Execution discipline

- Start from live branch, status, HEAD, upstream, ahead/behind, and dirty-file
  truth; preserve inherited work.
- Advance the next bounded inactive cohort or an already-bound research gate;
  do not turn the absence of a real task into a whole-program stop.
- Keep install, enable, connect, execute, promote, and persist as separate state
  transitions with separate authority.
- Ask the user only when a real trust, data, cost, side-effect, or consequential
  domain decision cannot be resolved safely.
- Verify the plan, acceptance map, and this prompt against
  `registry/portfolio-tasktime-projection-contract-2026-08-06.json` before
  claiming alignment.

## Current LongHorizon boundary

- Treat exact revision `b49ebf9654c1ee75eaf56dfe9eec1745fddcfa58` as a
  high-value external coordinator reference, not as an adopted runtime.
- Keep direct adoption blocked and equivalent Manage-Execute-Audit coordinator
  authoring stopped.
- Prefer candidate-wide safety, recovery, portability, integrity, and core-test
  corrections upstream. Limit any future Harness adapter to retained host,
  transaction, receipt, process-loss, lifecycle, and recovery boundaries.
- No real Claude task is required for this design gate. An upstream write,
  adapter-contract PoC, candidate execution, installation, model dispatch, or
  behavior/value comparison is a separate gate; only the last comparison
  requires a naturally occurring real task.
- Verify this boundary against
  `registry/process-loss-longhorizon-safe-adapter-upstream-design-2026-08-07.json`.
