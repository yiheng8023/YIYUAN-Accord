# 3.3 基线：恢复并兑现高度自动化

当前共识和工序统一在 [节点与计划](PLAN-v3.3.md)；本文件仅展开 F01–F08 结果定义，冲突时先按节点和最新用户决定处理，不独立扩张范围。

修订：r31 · 2026-09-23。状态：已恢复功能推进；可靠上游与普通交付主线保持，外部参考按权威来源、源码与实际适用性采用，完整功能验收未完成。

八项自举维度与认知/工程边界统一见[当前计划](PLAN-v3.3.md#自举的能力维度与证据)，映射原 A01–A08；生态关系及作用域、安装/上下文暴露/连接/运行状态按 F02/F06/F07/F08 既有职责管理。本轮共识澄清不增加已实现或已验证计数。

F03/F05/F06/F07的恢复职责同时包含故障时可判断和有执行者可继续：状态不可读、损坏、读写竞争及输入越界不能被解释为没有旧责任或可以重放；保留必要原件、未知效果、暂停和共享归属。当前status已改用无写入快照，识别损坏结构、不可访问路径和观测到的竞争，保留原始状态；诊断可用不等于修复或恢复完成。有效字节一致性检查和原子写机制继续保留，新包的普通宿主行为仍须独立核验。[外部核实与处置](PLAN-v3.3.md#外部参考的核实与采用)记录原问题与处置。

F02/F08在3.3通过宿主已有模型、支持的工具与必要Accord组件承担判断、校验和反馈。内化Jev/Laya等适用设计，专用第三方决策模型的产品接入与用户部署支持留待后续版本评估；不新增自训模型、托管服务、通用决策引擎或固定provider顺序。普通用户无需额外部署专用模型，成熟工具和用户已有组件仍按价值协作。Jev私有直连初筛及Laya公开演示观察保留其原边界，不是现有方案已优于外部模型或产品通过的证明。服务可用性、费用、数据边界、退路与完整结果必须共同成立，结构化输出和置信度不能代替授权或真实验证；必要自动化有真实Token、CPU和时间成本，不作零成本承诺。

r25补充：八项为不同层级的交叉视角，不构成完备架构证明；自证指举证与可验证性，自进化指授权内可检验、可继承、可回退的系统改进。建立条件、候选试验、评价保护、继承落地和成本/停止判断按计划映射核验；最小自举基础可以复用宿主和外部资源。有限递归已有研究依据，强通用递归仍不作本版能力或发布前提。可靠入口与Skill分层见[对应共识](PLAN-v3.3.md#可靠入口与skill分层)，保留简短协调入口及四个独立专用Skill，细节在需要时取得，不用隐式调用必然性承担必要职责。

当前主线按[推进顺序](PLAN-v3.3.md#当前推进顺序)先核实可靠上游到普通交付：F01/F02/F03承接目标、条件、职责及能力，F04/F07落实反馈、纠偏与核验，F05按实际连续性需要参与；F06及入口差异可交叉推进，F08保留整合与净影响判断。此为既有结果的依赖关系，不增加固定步骤或降低底线。核心入口、Skill分层、transcript连接及恢复提示已经实现；其实际采用、完整行为和系统价值仍分别取证，静态源码、调用形状、组件连接或局部机制通过不等于真实全链效果或本表完成。

可靠上游须由Accord及受支持宿主连接共同提供，不把修改用户AGENTS.md作为部署或完整交付前提。每次输入的目标关联、权限、暂停与未完责任判断必须有人承担；Skill/Hook/脚本/运行时的数量及组合服从当前任务，当前一个简短入口加四个专用Skill只是实现选择；语义、事件和反馈可以触发交叉组合或返回重判。继承原三Skill的动态职责关系，分别验证简单直达和复杂组合，不以固定逐步流程或固定文件调用序列定义覆盖。

F02/F06/F08的配置协调默认保留用户主动选择的模型和推理强度，按对象核对调度授权、实际切换和包括误判、缓存、重处理及协作在内的净影响；子代理授权不扩张为主模型授权。此为既有动态职责的明确边界，不新增模型切换机制或提高已验收计数。

整体闭环还要求F04/F05/F06/F07具有可恢复的任务与动作状态、受控转换及实际唤起/核对连接。宿主和Accord可以共同承载状态；掉线后必须允许“效果未知”，先核对当前授权、实际后态与执行归属，再续作或处置，不能只保存一个上游提示或完成标记。现有本地检查点、epoch/revision及恢复保护是部分实现，不代验断电持久性、远端副作用或完整自主恢复。

## 使命与本轮授权

全链路完整覆盖与交付闭环须在不依赖Plan/Goal模式的普通执行下成立。兼容用户主动选择的模式不等于依赖，模式也不自动增加目标或授权。该项是当前要求与待验基线，不是已实现声明。

职责定位、用户直接操作与必要确认统一见[当前节点](PLAN-v3.3.md#用户操作与环境变化)。Accord 提供持续协作的协调与可靠性保障，插件是交付形式；不强制所有调用中转，也不取得高于用户或宿主的权限。默认非技术用户条件不排斥熟练用户。变化事实、实际影响及意图分别核对，确认后仍需纠正受影响历史并复验；不擅自恢复用户明确选择。开发环境可有自定义和已启用的旧包，验收必须区分开发辅助、待测字节、实际暴露和结果归因。

动态自适应与驱动宿主自举面向实际遇到的环境。借现有获准能力主动建立缺失条件，并验证执行、恢复和结果；不能把可补足条件直接判为“不支持”。开发隔离服务因果定位，不是用户必须恢复默认环境的要求。真实无法补足的能力、权限或资源限制仍须有证据与可行替代分析，启动和失败恢复不能循环依赖尚未具备的最终能力。

F05信号连接的历史增量：所属App Server客户端可通过原生动态工具把通知交给现有助手，窗口观测与未知占用/效率/完整性分开。该路径已观察两次显式查询及正确使用；换轮、reroute、压缩、断连及过期的失效规则有离线反例，该查询路径的真实压缩/模型变化及自主择时仍待验。其它原生压缩续作观察按[接续](CONTINUATION.md)中的实际包、路径与条件保留，不能相互代验；该适配也不等于现装Desktop或ChatGPT入口已自动接通。

F01/F03/F06 输入变化增量：当前源码已将原生 UserPromptSubmit 的 model/permission_mode 及相邻输入差异送入既有入口；缺失保持未知，恢复或中断后的旧观察不作当前值。宿主自动续做遇到条件变化会使旧续做决定失效，保留原用户目标与暂停。一个无预置检查点的原生三轮案例保留未决口径、明确恢复后完成三文件与接口重试，原件、检查点和所属进程后态已核。该观察不证明 Agent 因新字段而改变行为，不覆盖全部设置、中途变化或完整安装包；详见[输入变化记录](PROCEDURE-v3.3.md#原生输入条件变化与暂停恢复2026-09-09)。

对外插件名称统一为 **YIYUAN Accord**，宿主是适配维度，不再进入展示名。现有技术包标识保留兼容；3.3 只绑定 OpenAI 开发包；退出当前分发的适配由旧 Git 版本保留。旧发布名称与证据保留历史身份。

F05的择时必须为下一工作段、交接材料、目标接续验证、失败恢复及安全余量同时留空间；窗口硬上限不代表效率。官方core以`tokenUsage.last.totalTokens`为最近响应边界的上下文基值，`total.totalTokens`才是累计处理量；两者都不能冒充之后未观察增量的实时状态。原生`get_context_remaining`另提供压缩阈值与完整窗口约束下的剩余预算；本机0.154.0的`token_budget`默认关闭，不能宣称已暴露。已有只读评估应按实际原生来源及新鲜度判断；可靠剩余预算足以与工作段及恢复余量比较，不强制再取得私有占用/效率证明。测量或估计依据缺失时返回unknown，缩短工作段并提早检查点，不等待耗尽，也不强迫健康载体交接。

F05按[当前推进顺序](PLAN-v3.3.md#当前推进顺序)保持关键状态保真，允许不影响接续的历史与过程细节压缩或丢弃。安全阈值前触发，接收方按具体缺口多轮补交、核对并接管，再释放源恢复职责；无需先证明全部历史完全无损。优先使用可核对的原生持久化、resume、压缩和迁移能力，源恢复能力不绑定旧进程必须存活这一形态。

9月21日实现补齐SDK源任务调用者与持久写者记录：创建时注册上下文/交接工具、处理源任务事件、复用既有交接与独立核验接口，使用Node内置SQLite保留scope、revision和fencing token。仅适用于已经授权持有控制连接的调用方；失败不自动重放；同一控制者可在重新核验当前权限、实际目标和写者记录并settle后接续目标，再发起下一次交接。该次观察未覆盖跨控制者冷恢复，后续子范围与证据见下。普通GUI控制连接、实际模型择时及完整接管仍须分别验证，不据机制测试将F05整项改为完成。

用户已展示原对话的手动项目归属移动。F05继续以目标、状态和责任的连续性为结果，项目关联只是可变环境之一；界面归属、原生cwd、执行位置与权限变化须分别观测，不能把项目移动默认等同于新载体或文件迁移。

SDK组合此前在Windows真实Codex0.155.1与固定localhost响应下完成两次交接/显式接管，原始请求、回执、持久状态和进程退出一致。该两轮观察证明原生连接可执行，不单独证明模型自主择时、业务语义接续、跨controller恢复或普通GUI采用；恢复使用下述独立证据。

同一组合的Linux/macOS托管原件也已与ac5dd3e8逐字节核对，通过限定协议与退出检查。已实现的不同controller恢复包括已完成并settle的目标，以及身份、原创建来源和scope回执已确认的普通source。两者均要求无未知在途效果，并在原生resume前原子旋转标记，拒绝旧依据和旧执行者后续写入；普通source不伪造transfer记录。旧controller静止、当前权限/暂停及实际恢复的设置、历史、工具仍须独立核验；存在未确认动作或活跃交接时保留待核实，不能一概视为可自动恢复。

上述已settle子范围已在Windows真实0.155.1新controller执行一次：使用原件副本恢复旧目标、保持历史和工具，完成一轮context调用，旧原件及两条交接历史不变，进程自然退出。固定响应与独立回读支持机械恢复，不代验模型判断、任意crash/未知业务效果或GUI；Linux/macOS同项原生CI及独立artifact回读也已通过，13份冻结源码均匹配47d8e606；该轮整体CI因两项离线测试的临时路径别名准备错误而失败，两处测试准备修正后的73f4c6b8完整CI已11/11通过；运行时代码和候选包身份不变。

9月25日普通source分支在Windows真实Codex0.156.1上补核：初始原生创建及持久工具证据确认后，关闭原controller，从原件副本恢复同一任务并完成第二轮context调用；原历史保留、无新增thread/transfer，旧scope依据再次恢复被拒，配置及原件在执行期间保持。测试进程自然退出，所属Job活动进程为0；临时profile/state/temp回收后保留证据仍可独立回读。首次恢复本身成功，但检查器误将旧轮次工具回执计入当前轮而报错，原记录保留；检查器修正后在新隔离副本复验通过。该分支随后在b49dd36a的Linux/macOS原生CI通过，保留的启动/恢复四份episode均经本机独立回读，每份13个源码Hash与Git提交一致；POSIX退出证据只覆盖原有进程组边界。固定响应、源身份与控制权核验只支持该恢复分支；模型择时、业务语义、任意故障及GUI采用仍未由本案验证。

已找回首次续作回执并reconcile后的确定性断边现由finalizeReconciledHandoff接到既有settle/restore；按同原连接退订或独立旧连接关闭证据记录实际释放，不重放续作或未知退订。其自身CAS/确认丢失也保留可核对状态，已知reconciliation/finalizer连接的闭合分别核证。93项本地检查及真实SQLite组合通过，尚无新增原生/模型验收；此前原生证据不因此升格为本批完整故障恢复。

F05的保守估计路线在证据新鲜、继承完整性已核、工作与全部恢复余量严格容纳于窗口时，可建议保存关键状态后有界继续；效率区间和自动压缩阈值可以保持未知。该建议不证明效率或保证工作段内不压缩，发生压缩/代次变化须恢复并重评。旧实现将效率未知一律合并为decision未知，现予纠正；缺少占用/尾部/工作估计、来源或时效的情况仍保持未知，暂停和源释放边界不变。

F05新增随包交付的可调用原生交接适配器。拥有获准且幸存的App Server控制者时，可组合原生停止/创建/续作/取消订阅、作用域原子记录和独立现态核验，支持只读补交多轮后转移写者并证明安全续作。源提议可由同一模块订阅已有有序事件流、应答一次、收齐原生回执并自动调度，保留直接调用接口；接收器必须从实际请求锚点回放并连续接入，源新轮次或连接漂移使后续动作停止核对。它不创建进程、存储或宿主连接，不使普通Desktop/IDE Hook自动获得控制权限；不明回执或写者归属须先由既有控制者核对。该执行连接与自主择时、普通入口采用及完整恢复验收分别判断，不以调用成功代替全部能力。

F03/F05/F06状态管理通过原生MCP入口复用既有checkpoint runtime。inspect_task_state和read_task_input只读；manage_task_state只绑定、暂停或退休所属检查点，不写业务文件、不派发或重放。宿主元数据与模型参数、当前调用与已存状态分开；子线程不套用祖先记录，写入保留调用者已观察的epoch/revision，并在最终写锁内核对当前原生turn。宿主审批条件与实际用户授权仍需满足，不能因工具可见或理由字段齐全而推断；拒绝不自动换通道绕过，效果未知须先查后态。三个工具共用一个宿主管理的stdio进程和原状态存储，其加载、运行、占用与退出纳入既有生命周期职责，不视为零开销。

需要上下文判断时，同一入口可按需读取Hook绑定的原生计数并匹配当前调用的thread/turn/model；历史session版本与当前宿主版本单列，不拿旧版本填补当前身份。默认状态读取不增加该文件读取。其可选contextAssessment直接复用既有预算helper，绑定先前input epoch、checkpoint revision、context generation及原始时效、完整性和有来源的尾部/工作/交接/恢复余量；同代次的新计数可正常增长，条件变化须重评。更宽观测年龄不自动证明更新鲜或更安全。评估只给有条件建议，不改持久状态，不授予交接、派发或释放权限。

F05的获准控制连接可注入Node stdio流或已连接的标准WebSocket，复用同一有界消息解析、请求匹配、原生终态等待、精确请求锚点回放与live订阅，并从同一原始接收时间线提供上下文观察。WebSocket协议及网络由成熟实现承担，模块不另造握手/分帧或新增依赖。连接身份、授权和进程必须已有可靠来源，模块不启动宿主、建立连接或授予权限；日志缺失、溢出、过期和连接断开不形成肯定结论。持久写者记录、语义核验及自主择时仍由实际控制者与Agent组合承担。

F01/F03/F08的执行跨度按当前不确定性、后果、反馈和用户期望参与动态选择；目标明确不能覆盖路径/环境/权限变化。Agent承担主动校准和适时反馈，在已授权充分条件下完成工作，在必要决定处交还控制；保持未完责任，不盲跑或为继续而扩张任务，也不依赖固定逐步确认。

源Agent可通过已注册的accord_inspect_context动态工具取得调用绑定的只读原生信号；参数不能改写thread/turn/model或指定操作。首次调用早于用量通知时仍返回unknown，后续新信号由同一原始接收链提供；信号可达不等于自主判断、交接授权或整体验收。

F06中长驻读取进程的代码依赖按进程加载代次保留；原生更新退休旧缓存后，首次按需上下文读取不得重新解析旧目录中的代码。转录数据仍按需读取，缺少可选代码时普通状态可用、上下文保持未知；该保护不等于当前运行进程自动采纳新代码，实际刷新与数据读取分别核对。

F05的连续性不以同一项目或执行位置为前提；跨项目、同仓库跨主机、本地/云端委派与回接的适用性按[当前计划](PLAN-v3.3.md#跨项目与执行位置的连续性)分别处理。继承的是获准目标、状态与责任，源权限和本地路径不能原样冒充目标端权限与有效引用。单写者按转交责任和共享可变成果限定；各项目的独立工作不被无故停掉。当前本地基础观察不证明跨边界支持，具体实现、入口与恢复后态仍待验。

入口范围遵循[适用性与版本承诺](PLAN-v3.3.md#入口适用性与版本承诺)：Agent执行入口承担生产交付主线，先按真实用途、必要职责的全链路可行性和全生命周期成本确定开发范围；普通Chat不作为默认独立交付项。网页、云端、插件可安装或是否支持Hook均不能单独决定适用性；当前组件无法直接复用时，比较原生能力、Agent判断、MCP及其它合理组合。辅助对话或执行衔接按其真实价值与职责评价，保留未决前提。此前侧载实验只回答有界兼容性问题，不建立新的适配承诺。

所有功能均由Agent按任务状态和真实需要自主择时调用，并负责后续状态、执行反馈、必要恢复、验证和收尾。fork只是示例；研究、模型选择、协作、上下文处理、工具与生态、资源控制、验证和交付同样适用。用户不应知道工具名或先发操作指令。需要时主动用、无需求时不制造调用；必要职责可靠参与不等于所有功能每轮常开。实际授权、能力及副作用边界继续约束具体动作。

2026-09-09 ChatGPT入口事实补充：未公开上架的5文件独立Skill已在当前个人账户上传、安装，并在个性化临时Chat显式选用；宿主导出保留原5文件字节，另加2份元数据/图标。单次虚构订单请求生成三份可下载文件，独立核对A60/B80/总140及原始信息保留。可见工具记录只显示读取电子表格Skill，Accord实际参与仍未证实；显式选用不代验普通触发，更不证明完整插件或原生Hook可用。试验后已卸载、保留创建副本，恢复模型与不个性化设置并关闭临时页。Work下未见临时按钮，不能沿用CLI的ephemeral假设。具体字节、条件、结果及限制见[工序](PROCEDURE-v3.3.md#chatgpt入口候选准备2026-09-09)。

模型调度采用动态发现与协调：模型名称、版本、套餐权限和推理选项都可能变化，按当前账号、实际调用入口、用户限制与任务要求决定候选，不将某模型写成固定最低档或默认下限。本轮按用户要求不选 Spark；这属于当前测试约束，产品不能依赖 Pro 专属选项，也不能把今天的套餐名称永久编码为权限规则。

用户重申旧Bob/Matt讨论已达成的吸收原则：工程判断与可靠性机制是Agent各类工作的横向基础，不只用于写代码；应内化必要能力，也应借力宿主。已从旧Git对象核实2026-07-31的软工原则再验证记录：保留目标保真、正确性、责任、验证、恢复及长期维护的有效要求，具体实践随任务和宿主调整，用结果与反例检验。该历史记录支持方法连续性，不是原谈话逐字来源或旧流程的恢复授权。专业领域正确性仍需对应领域检验；不把通用工程检查当作领域验收。当前实现按实际缺口分工，宿主足够则复用，需要稳定内化的判断/契约/反馈连接就实现，不能借“宿主负责”留下无人执行的职责。

用户确认运行时、状态机及其它结构均为可选手段，按必要结果选择；复用宿主时仍须保有目标、授权、状态解释、核验、暂停和撤回的控制，不能越过宿主实际权限。持久状态、跨任务关联、恢复执行者和再次唤醒分别验收。当前Codex源级检查点已改用稳定用户目录并刷新后再替换文件，旧临时状态保持可见、冲突不自动合并；同一session/cwd的跨进程恢复已有局部验证。它仍不是跨任务接管、无人唤醒或断电持久性的证明，现装包和新源字节亦须分开。

用户进一步明确：其判断同样可质疑和校准，以事实和共同理解约束行动；有界研究先解决影响方案选择的不确定性，不能机械执行或把研究变成每步重复仪式。模型及子代理选择由 Agent 负责，先核实实际接口、可用模型、质量与总成本，再按任务分配；不要求用户会切换模型。必要时可备份并初始化开发变量，但开发对话不得删除；测试按最新授权使用原生临时对话、用完结束。优先可回退的任务隔离。已安装的 Matt Skills 按实际适用性采用，其局限和流程冲突仍须判断。

本次与上轮实现、失败和验证均属于同一个3.3开发目标。全局逻辑和结果依赖先自洽，再按需拆解实施；基线、计划、工序、验收以及相关逻辑、规则和机制随证据联动校准，不冻结方法，也不因局部修订重启目标。方向、原则与方法论未发生实质转向；涉及必要决定或尚不能判明的关键分歧时再共同探讨。

用户不愿阅读资料或学习工具操作也是默认条件。Agent 主动查阅官方资料、GitHub 等，理解比较、给出结论并执行；来源供按需核查，阅读不是使用前置。不得用自行查文档、研究配置或选工具替代完成工作。无法代办的本人动作及关键决定，先准备具体事项，再以简短普通语言说明必要一步，保留真实授权边界。

课程、教程和第三方指导的筛选、核实与适用性判断由 Agent 承担。免费/收费、热度、作者名气和官方推荐都不能单独证明当前任务效果；官方资料用于核实支持范围、接口与明确限制，实用性还需适用条件、可复现结果与总成本。减少无实际价值、重复或难退出的第三方依赖，但不设自研优先，也不因第三方身份拒绝可靠生态；宿主、现有工具和成熟实现能充分承担时优先复用，新增关键依赖则验证必要性、替代与退出。用户获得经筛选验证的方案、结果和影响决定的取舍，不需先学成专家再判断教程是否可信；资料供核查，不成为用户的新作业。

默认用户仅会自然语言输入，可能不知道想法是否可行，不懂工具、配置、并发、资源或上下文，也不会主动 fork、开新任务或救场。用户可以持续在同一对话交流；Agent 主动判断可行性、开展必要研究、说明取舍，完成已授权的执行、纠偏、连续性和收尾。关键决定与必要本人授权仍由用户承担，以普通语言交互；不要求用户代办可由 Agent 完成的技术操作。此条件是普通入口与连续性、资源管理验收的起点，不是已实现的效果。

用户专注创造与决策，Agent 承担实现目标所需、已获授权的工作。Accord 与用户、宿主及生态共生协作、各自主权独立。过程管理是支撑手段，不能成为产品结果；少让用户催促、协调、纠错和救场，是必须兑现的功能目标。

用户已要求把系统性纠偏加入 3.3，建立并动态调整基线、计划、工序和验收标准；现进一步授权交接到新任务继续功能开发，按需排除环境变量影响。3.3 仅交付 ChatGPT/Codex 适用入口，其它适配退出本版分发。本修订进入普通入口运行时切片，当前 Codex 开发身份与历史包身份分别绑定；共享安装、新账户或信任、显著费用及新发布依具体授权。现有产品版本、发布标签、代码与安装保持各自身份。

功能与系统闭环共同构成交付：按最短必要链路实现功能，同时检查共享状态、接口前提、故障传播、恢复执行者和资源归属；组合后失效的功能不算可用。先修影响结果最大的薄弱依赖，关键质量底线不能被其它长项抵消。完整对照与成本评估基于可用实现开展，但耦合风险不能留到最后。安全、授权、原始输入保护贯穿实施，不把全面治理或资料整理变成长期前置。预算和方法可修订，不能通过降低必要结果要求使检查通过。

3.3 不推倒全部历史：保留有效结果、机制与可复用证据，只对受影响的缺口修正、替换或退役。信息不确定或时效性重要时，主动联网核实官方或其它可靠资料；涉及可复用实现、生态方案且有改善任务路线的实质可能时，再去 GitHub 等来源寻找成熟实现，完成有界比较后决定行动。即使原生能完成，也不能跳过后一条件下的比较。调查不是扩展安装或新增权限的授权。

用户最新纠正：除了必要人工介入，目标是高度自动化；为此增加合理的计算、状态存储、运行时或维护投入可以是正确选择。本计划中的“减轻负担”首先指用户被迫承担的催促、协调、纠错和救援，不等于最小 Token、最少代码或零运行成本。成本不是必要自动化的否决条件；先实现完整职责，再在保住效果的方案间优化无效消耗。显著新增费用仍须实际授权，不能把价值优先解释成无限支出授权。

智能判断主要由 AI 宿主中的 Agent 承担，Accord 接通必要的自动化职责：可靠进入任务、承接当前目标与授权、组织执行、接收反馈、纠偏和恢复。必要入口参与不能只靠 Agent 偶尔选择 Skill；详细指导仍按需要加载。Accord 在宿主与生态之间承担逻辑协作职责，不强制代理所有工具，也不接管宿主、用户或扩展的主权。

F02/F06/F08补充（r31）：模型/推理支持、客户端可见/选择加入设置和本次有效选择分别取证，自定义Max/Ultra不成为默认规格；模型分工按实际质量和完整成本校准。第三方Skill沿现有管理器复用，安装、启用、可发现、隐式许可、显式选择与实际采用分别判断；用户无需记住技能名与时机，由获准协调者按适用性代选；默认关闭隐式匹配与禁止委托调用分开，沿宿主支持的显式路径使用原技能并记录真实选择来源。停用/排除、目标任务控制权和新副作用边界仍须遵守；确有缺口再决定适配。指导存在或静态匹配不证明组合效果。

## 事实、体验和待验证项

| 基线项 | 当前依据 | 能说明什么／不能说明什么 |
|---|---|---|
| 产品发布与开发起点 | v3.2.1 发布身份 `cf13486db9e5d0e9a6eef2d9df187d5e0405ee88`；2026-09-09开发起点为`22d01083462966d83c77ee6b02d914b90083edf3`；当前main/upstream及工作区以实时Git核对为准 | 发布身份、提交起点和滚动开发字节分别绑定；局部检查不是全部使命完成证明。详见 [续作记录](CONTINUATION.md)。 |
| Codex 安装与候选身份 | 2026-09-09的旧名称/12文件观察属于历史；滚动安装、市场来源和实际IDE准备状态见[当前接续](CONTINUATION.md#当前事实)。本轮候选已变为17文件，不能沿用13文件包的安装或参与结论 | 同版本不等于同字节；安装、Hook信任、实际暴露及结果分别绑定。候选代码更新不代表用户共享安装已更新，已有IDE交付按原身份保留。 |
| 当前实现 | [Codex Hook](../../plugins/yiyuan-accord-codex/runtime/accord-hook.cjs)、[Skill](../../plugins/yiyuan-accord-codex/skills/deliver-demand-driven-outcome/SKILL.md)、[检查点](../../plugins/yiyuan-accord-codex/runtime/task-checkpoint.cjs)；其它宿主旧包仅属于历史 | 有指导、事件提示、文件状态和受支持续做连接；不能据此推断所有职责已自主履行。 |
| 历史用户体验 | 用户报告 前身系统 可高度自动化地交接、推进并减少操心，当前体验较弱 | 是需要追溯的结果基准与回归线索，不能因缺少统计而丢弃；已从2026-08-17/18原始Codex Desktop记录定位自主判断风险、同目录fork、投递、目标核验及源停写后目标续作；不再把成功链当未定位。安装机制的因果归属和源进程卸载仍未证明，详见[回溯更正](PROCEDURE-v3.3.md#既有交接判断的回溯更正2026-09-14)。 |
| 历史机制差异 | 提交 65466c565096b5c175c8b3b1242aa5bf6439b82d 的 [历史 Harness Hook 源码](https://github.com/yiheng8023/YIYUAN-Accord/blob/65466c565096b5c175c8b3b1242aa5bf6439b82d/adapters/agent-autonomy-harness-codex/scripts/carrier_hook.py) 有持久生命周期计数和迁移建议；当前startup/clear提供不依赖状态存储的整体职责，resume/compact另有持久输入与检查点恢复提示，不能再统称为无状态提示 | 差异已查到；该历史脚本标为候选，尚未证明它是用户成功体验的实际执行版本，也未建立因果归因。当前恢复提示仍需按实际恢复行为验收，不能整代回滚或因有Hook就认定交接已完成。 |
| 连续性验收缺口 | [架构说明](../architecture.md)区分用户要求的接管、健康压缩后的续做和自主时机判断 | 前两者不能代验最后一项；当前对话经用户提醒后的判断属于辅助行为。 |
| 系统性使用反馈 | 本次开发与传播中用户多次提醒推进、工具选择、纠偏、交接和收尾，并指出功能优先及宿主覆盖未落实 | 足以重开相关结果审查；不把所有开发失误直接归为插件缺陷。每个问题再分宿主、Agent、Accord、配置、观察器或其组合。 |
| 宿主与自定义环境 | 已有普通入口及安装观察均有时间、版本、入口范围 | 不假设当前宿主为初始环境；Desktop、CLI、IDE、Web、云端等不互相代验。未测入口保持未验证。 |
| 运行资源管理 | [既有整形指导](../../product/reshaping-guidance.json)的 resourceStewardship 已定义容量/需求观察、归属、并发准入、压力监测、再分配、释放和后态核验，并要求借宿主自动化；当前完整运行闭环证据待核 | 承接既有职责，不宣称全部无设计或已实现；源码线索不是普通运行闭环，临时文件清理不能代验操作系统与宿主联动的运行态管理。 |
| 传播与资产 | 2026-09-09 中国标准时间约 00:24 已核验撤回：B 站 BV1haYJ6ZE5H 仅自己可见，YouTube eC7IQGTp8I8 私享，X 中英文帖 2097336959643582802 / 2097335402365669385 已删除且原链接均不可用；完整后态见私有 YIYUAN NEXUS 的发布记录 | 本地素材保留，GitHub 产品发布未撤回。传播不是功能证据，素材与平台操作记录不进入产品仓库。 |

“20 条线程、80 天”等投入由用户陈述，不作为已测量的工程耗时或净收益数据。后续分析需要从必要记录核对；不为核对数字而拖延可用功能。

## 宿主覆盖边界

“覆盖所有能力、功能、按钮、设置、配置”指建立对实际目标宿主完整可见面的责任覆盖，按任务选择使用；不是每轮点击全部按钮，也不是一张能力名称表就算完成。由 Agent 主动发现实际接口、UI、配置及官方说明，尽量复用宿主已有状态、执行与调度，不重复建设全量静态能力库，也不让用户代做可安全完成的宿主调查。范围包括模型与推理、多模态、工具与计算机/浏览器操作、文件与网络、执行与并发、任务/会话/上下文、后台与计划任务、插件/Skill/MCP/应用、账户/权限/信任、设置/配置/记忆、安装更新恢复、资源和发布分发；发现新类别必须补入。

逐宿主版本、入口和适用账户条件，记录：官方或实际来源、可发现性、读/执行途径、必要权限、调用者、预期结果、失败及恢复者。明确区分未调查、已发现未执行、已执行未核验、已验证、受限、真实不适用。没有接口、缺账户或没有可靠信号不能写成“不适用”来关闭必要缺口；记录具体阻塞、替代路线与剩余义务。

本版在OpenAI范围内按具体执行条件判断候选入口，不默认将ChatGPT或Codex的每个界面列为交付项。CLI是无头环境的一等入口，Desktop的UI能力不能替代CLI；共有实现可以复用，差异和实际效果仍须分别核验。Work和云端保留执行能力依据与待判条件，纯对话仅按实际辅助用途考虑。其它适配保留历史身份并进入后续版本研究，不随3.3分发；未知或受限不等于自动不适用。已纳入目标的宿主功能与设置覆盖要求保持完整。

用户自定义环境是正常使用条件。读取 AGENTS.md 的先后，不等于授权层级或执行优先级；兼容有效配置，并保证必要职责在相关实质动作前参与、动作后获得反馈。开发排除变量优先采用任务级、可回退隔离，随后回到真实自定义环境验证；共享配置调整先明确范围、备份、受影响任务和恢复路径，不擅自扩大信任、数据或费用权限。

入口按工作形态、执行位置、接入方式和运行平台分层，具体IDE品牌作为集成实例。用户没有Mac/Xcode和JetBrains、无购买意愿；现有设备、官方资料及托管CI承担可完成的共同适配。实例专属证据不足只限定其支持声明，不自动否定平台适配或阻塞独立主线，也不产生采购要求。

F02/F06的能力接入以当前有效盘点为起点：已有合适且可用的组件复用，启用、重连、升级、修复和新安装分别判断，库存未知不能当作缺失。满足需求与是否值得优化分开评价；按实际收益和全生命周期成本有界比较，不默认换新或追逐全局最优。盘点按相关变化刷新，不要求每条需求全机重扫。

资源覆盖包括 CPU/GPU、RAM/VRAM、磁盘 I/O 与空间、进程、终端、浏览器、服务、并发任务和多代理的分配与回收。通过可用的操作系统与宿主原生接口联动观察负载、调整并发、限流、暂停或释放归属清楚的任务资源，再验证负载恢复与必要续做；外围生态能力也要正确发现、启停、复用、回收或退役，不能持续积累。不得盲杀未知进程、关闭防护或重置用户环境；真实不可观测或不可控制项保留具体限制。

既有 resourceStewardship.implementationGate 的接口盘点要求按当前功能切片解释和修订：先查足以决定当前实现的相关原生能力，不把全宿主、全接口盘点设为功能前置。必要缺口可借宿主执行或补充连接闭合，不预先要求 Accord 自建完整操作系统控制器。

## 三组比较起点

1. 当前宿主原生能力：同类任务、相近条件下实际能做什么。
2. 用户实际用过的 前身系统：先定位真实版本、消息触发和接管记录；无法重放时保留历史观察范围，不伪造等条件对照。
3. 不可变 3.2.1 历史包与当前 3.3 开发候选：绑定实际包、模型、入口、工具、配置及任务，区分开发者补救与产品自身作用；当前安装另行核对，不假定它仍为历史包。

没有历史证据不妨碍修复当前已复现缺口；没有因果对照不能声称净收益。替换或删除机制必须保住其仍需结果，减少代码、提示或状态不是独立目标。

F04/F06/F07/F08同时承担全局校准和资产退役：核对局部结果、关联职责及整版目标，按用途、依赖和证据/恢复价值复核保留项。没有复用价值且不再承担必要职责的临时产物、重复副本、失效机制和历史债务应适时清理；当前保留不自动成为永久保留，仍需结果也不能为减少数量被删除。具体边界见当前计划的历史处置。

## 统一结果编号

下列编号与当前计划及验收判据保持一致；条目不是强制逐步 SOP。

| 结果编号 | 用户需要的结果 | 当前状态 |
|---|---|---|
| F01 | 模糊意图被整理为专业要求，侧问和新纠正不丢失原目标，输入与授权不混淆 | 历史包/案例的连贯认可、侧问、暂停及恢复保留原准入范围；当前包的普通任务语义承接与完整A01仍待验，不能绑定某个词或平移旧准入 |
| F02 | 全面识别宿主与生态可用能力，主动调研成熟实现，按目标比较、组织并实际使用合适路线 | 核心职责入口与按需Skill分层已实现；当前包的必要细节/能力实际采用、入口最终集合和选择效果仍待验 |
| F03 | 已授权工作自主推进；障碍、等待、失败后能选择有效下一步，不靠用户反复催促 | 历史CLI恢复、同任务暂停/明确恢复保留原包和案例边界；当前主线验证实际职责采用与连续交付，已纳入入口和更广恢复条件尚未完成 |
| F04 | 在已承接任务及获准依赖内，持续纠偏触及相关历史判断、实现和受影响成果，并保护仍正确的效果；安装或更新不接管用户无关的既往历史 | 历史关联成果修订保持原身份；当前已安装入口前瞻绑定发布准备02案，核对受众变化后四份成品的全部修订、事实一致性与原件保全。02已执行但首轮语义失真、次轮观察输出上限中止，未准入；主代理校订稿单独保留。A04整项还依赖原有系统整合核对历史债务与恢复未决，局部结果不计整项通过 |
| F05 | 动态感知上下文并控制继承损失；健康继续或必要接管，确认目标承担责任并能安全续做后才释放源恢复能力 | 源级resume核对与输入丢失已分开；SDK已有原生动态提议/单次派发及settled目标恢复，当前补入已确认普通source的执行器恢复并通过离线反例。自主择时、该分支真实行为及普通GUI采用仍未验 |
| F06 | 自定义环境、模型与配置变化下适配；生态能力安装、加载、更新、启停、复用和恢复实际闭环 | 既有包的版本标识变体热切换/回退有本机及托管证据；当前包在本机0.154.0→0.155.1同一暂停任务的恢复机制已核。固定响应不代验Agent行为，其它宿主差异、失配恢复和整体环境适应仍待验 |
| F07 | 成品达到意图和质量；联动操作系统与宿主管理运行资源，核验交付、资产归属及回收，形成维护反馈 | 局部交付/退役与所属进程退出已观察；CPU控制仅机制试验，普通资源管理完整链待验 |
| F08 | 功能可用后证明用户负担和系统表现值得保留，公开声明与分发匹配真实能力 | 已接通案例与A01–A08缺项判定；已有精确对象限定的子范围准入，最新身份及缺口见[接续](CONTINUATION.md)，3.3整体验收未完成 |

关联文件：[计划与当前工序](PLAN-v3.3.md#工序与依赖) · [历史观察](PROCEDURE-v3.3.md) · [验收](ACCEPTANCE-v3.3.md)。本基线是可修订的事实起点，结果定义与已实现/已验证状态分开。

### r10持续要求与交付依赖

2026-09-10新增要求由计划统一维护：F01/F07承接语义准确性、专业书面表达与关键术语一致性；F02/F06/F08承接正式发布后的宿主安装、后续版本的Claude适配及独立部署、传播更新、市场投放和项目/组织治理。前置准备、正式发布与发布后结果分开验收，历史支持不继承为当前能力。提交/推送和本地、远端CI均按变更及验证需要安排；本公开仓库不适用私仓2000分钟限制，不默认跳过远端CI。详见[当前计划](PLAN-v3.3.md#发布后的部署传播与治理)与[验收](ACCEPTANCE-v3.3.md#发布后义务与表达质量r10a01a02a06a07a08)。
