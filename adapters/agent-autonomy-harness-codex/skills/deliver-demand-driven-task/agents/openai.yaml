interface:
  display_name: "Demand-Driven Task Delivery"
  short_description: "Complete goals without tool or topology burden"
  default_prompt: "Use $deliver-demand-driven-task to complete this goal while owning capability, continuity, verification, and cleanup."

policy:
  allow_implicit_invocation: true
