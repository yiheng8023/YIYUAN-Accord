# 更新日志

## 3.2.1

本次维护修复普通任务入口与检查点恢复，不启动 3.3 开发。

- Skill 描述优先说明非平凡任务的执行、纠偏与交付职责；输入 Hook 提供
  完整 Skill 的包内入口，已加载且仍适用的指引可以复用。
- 检查点按具体风险和宿主现有保护选用。创建文件本身不要求绑定；已经
  绑定的任务仍须核对新输入、修正受影响结果，并按授权暂停、恢复或退役。
- 输入或产物路径变成目录等无法检查的情况，现在可以查询诊断、暂停，
  或在明确取消后退役所属检查点。正常完成仍要求严格检查，不能把检查
  失败当成验收通过；版本与输入并发保护保持有效。
- 分开验证 CLI 普通交付、Desktop 输入框复核和 Claude Code 双轮纠偏；
  工具输出续做不等同输入框提交，不用一种入口代验另一种入口。
- 安装说明区分启用登记、实际使用会话中的技能暴露、选用、执行与收益，
  不用另一个新会话的加载成功代替原会话刷新，也不默认创建或归档任务。

版本标识本身不证明已发布。Hook 提示不保证每次都选用 Skill，调用不等于功能
完成；CLI、Desktop 与不同模型路由的结果分别记录。正式发布以对应
GitHub Release 及精确候选验收为准，现有 v3.2.0 不变。

## 3.2.0

本节说明 3.2 的包内容和适用边界。版本号本身不表示已发布；正式状态、
精确提交和验收结果以同名 GitHub Release 为准。已发布的 3.1 tag 与 Release 不变。

### 交付内容

- Codex、Claude 各一个协作 Skill，把目标、授权、能力选择、执行、持续纠偏、
  结果核验、连续性与资源处置连接到宿主已有能力。指引可适应任务，计划和
  调度工具不是执行前提；用户目标和事实判断都允许被证据纠正。
- 可选的任务局部文件检查点：绑定输入与产物条件，以同一份文件字节核验
  谓词，检测输入变化，通过受支持的原生事件请求有界续做。宿主保留任务
  判断、执行权限和恢复责任；参考核心不是插件的运行依赖。
- 输入失效保护覆盖锁竞争、超长/无法绑定的输入，以及 Stop 发布状态期间
  新输入失败。锁外失败水位使旧回执失效；恢复须核对真实当前输入和派生 epoch。
- 退役删除窗口另有保护：若新输入失败或删除只完成一部分，恢复缺失的所属
  检查点/回执并拒绝错误退役，保留失败水位和恢复上下文。25 项本地运行时回归通过。
- Claude 保留建议性的工具结果反馈与可选原生更新检查器。安装、更新、撤销
  和必要补偿借用宿主及独立健康管理者，不要求损坏或已移除的插件自救。
- 当前源、派生计划、版本、README 和架构描述统一；清除当前参考副本中已
  退役的专用目标模式提示及依赖，历史 Git 和原始失败保持不变。

### 已观察范围与限制

Windows 本地样本包含真实交付与明确纠正、在途答复后的续做、能力失效与
来源冲突处置、声明的生命周期组合，以及用户要求的 Desktop 接管和健康
自动压缩后的核源继续。原执行日期、包、模型/提供商、人工协助和失败均保留；
完整包发生变化后，须独立复核受影响依赖，不给旧运行换版本标签。

原生输入观察在 dev.17 两宿主中实际捕获了匹配回执；Codex 还记录了收到
上下文。两个方法整体仍记为失败：Claude 一次回环请求用途未知、对应提示
消费未证实；Codex 主进程退出后强制清理了身份未捕获的子进程。局部输入
事实不因此消失，但不能称全树自然退出、模型业务成功或原生自主恢复。
后置本地调用者的真实事件重放与运行目录清理分别记录，原件完整保全。

Desktop 接管不是自主提前检测的证明；健康压缩后继续也不证明最优迁移余量。
如果实际任务依赖未证明的预测、迁移或恢复保证，缺证仍是必须处理的条件。
CLI/App Server、Desktop、IDE、云端和其它宿主不互相替代验收。

### 质量、成本与净影响

准入 v4 分开必需的功能、生命周期和每宿主影响评估，以及单独提出的增量
支持声明。必要损害或未知不能被平均分、中性标签或覆盖编号掩盖。
旧正向假设与未满足的结果保留，v3 验证语义不被改写。

一次完整 Claude 配对中，有无 Accord 都交付并核验了 140→60 两阶段结果，
未观察到可靠性增益。报告费用分别为 0.256797 / 0.213819，失败观察器尝试
另计 0.143105；计费口径和固定先后的一次样本不支持因果节省结论。
当前仅支持明确条件下使用，不支持默认必装、全任务净收益或优于原生的主张。

启用输入 Hook 即使未绑定检查点，也会产生回执、提示与运行开销；观察器
中的时长包含额外采集成本，不能当作生产净延迟。Node、原生事件和宿主信任
是 Hook 前提，输入/状态上限为 128 KiB。失败水位保留到所属状态目录可安全
退役；存储或补偿不可用时须由健康调用者处理，插件不提供权限屏障或外部
写入事务。没有新增常驻服务、模型调用或普通用户 Python 依赖。

### 版本与维护

精确 `3.2.0` 可以表示冻结候选，仍不能自行宣告外部验收、CI、公共发布或
本机激活。发布必须使用经过独立评审与同 SHA 检查的提交；安装后也要分别
核对登记、缓存、实际加载及普通入口。标签不会自动推进，失败更新须按已
授权的恢复路径处置。共享开发宿主的单次预发布安装例外已经结束。

这次没有修复宿主原生计划 UI；项目通过源与 `update_plan` 对齐进度。
没有将旧任务归档、未经验证的自动接管或用户恢复包装成成功功能。

完整开发过程与早期失败摘要保留在
[446d916 的更新日志](https://github.com/yiheng8023/YIYUAN-Accord/blob/446d916aa8ec37662d23b312158917cf9183c190/CHANGELOG.md)
和当前源的历史观察中。已发布版本见[历史发布账本](docs/operations/HISTORY.md)。
