# 更新日志

## 3.2.0 — 未发布

这是开发摘要，不是发布公告或功能验收凭证。当前候选包为
`3.2.0-dev.5`；未更新用户安装的 3.1。定版时须以精确候选、实际变更和
验收证据核对本节，再生成对应 Release notes，不改写已发布历史。

### 已实现的开发变更

- dev.5 将原生校准为低负担起点和同等适配偏好，不再以“能完成”阻断
  值得进行的外部比较；复用已有路由核心及宿主发现/生命周期接口，新增
  外部更优、跨领域深度和任务退出/复用反例。普通入口效果仍待验证。
- 自举同样借力宿主；删减 CONTRIBUTING 的旧固定工序、目标模式绑定及
  重复历史发布处方，转向当前开发源和精确历史链接。保留必要功能、
  校验核心与法律要求，不为减重新增执行器或能力数据库。
- 将架构与接续导航中的历史长尾改为精确版本链接，保留已发布文档所用
  的维护基线锚点；旧“当前状态”和旧工序不再与现行入口同页竞争。
  历史证据与回归对象仍在 Git 中，删减文档不等于已证明行为改善。
- 修复发布条件漏检：当前绑定的七项条件均须保留，并逐项派生到可见计划；
  新增条件和调整展示顺序仍可行，不能静默删掉既有验收、评审或证据要求。
  发布前检查与发布后收尾分别落实，此修复不代表发布就绪。
- 将八个质量维度与承担工序关联；校验拒绝未分配、缺失、重复或未知
  引用，可见计划同步展示质量底线及未验证状态，结构覆盖不冒充验收。
  开发源契约升为 schema v3，旧 v2 不能冒充新增映射要求；宿主描述仍为 v2。
- 将当前代码、文件和指令体积预算显式绑定到开发源，解除隐式继承旧版
  指令上限的耦合；保留余量与超限拒绝检查，预算调整不等于用户收益。
- dev.4 区分已有目标中的执行同意与独立讨论，加强持续执行和交付核验
  指引。普通入口仍有未调用 Skill、核验叙述失实的反例，修正效果待验；
  未新增全局 Stop 拦截或常驻运行时。长程规划不依赖目标模式，后续纠正
  和新证据可以要求改道或暂停，不以完成旧目标为由硬推。
- 重写中英文 README 的定位、使用说明与证据边界；删去无依据的速成
  标题和固定常量表述，纠正旧文件被当作当前开发权威的问题。安装参考、
  历史证据、社区与法律支持信息仍有明确入口；文案重写不是功能验收。
- 建立独立的 3.2 源头、计划、工序与验收映射；旧版已完成检查不再
  自动成为新包的行为、价值或发布证据。
- 两宿主候选 Skill 改为按需对齐、研究、复用和有效环境判断；纳入
  主/子代理任务适配指引。这里交付的是指引，自动匹配与实际效果仍待验。
- 建立有日期和官方来源的原生能力、Accord 职责及入口边界矩阵；区分
  CLI、桌面模式、IDE、网页/云端、移动/远控和可编程入口。矩阵由同一源
  数据生成，不意味着列出的每个入口已经适配。
- 当前包校验与历史回归分离，合批读取不可变历史对象并限制缓存到单次
  校验；工作区事实仍重新读取。收益限于已测维护工具，不外推为用户收益。
- 修复可见计划硬编码“检查全部配置”而偏离按需源策略的问题；环境与
  覆盖规则直接从当前源生成，新增缺失、伪造效果和过期投影回归检查。
- 校准连续性验收：自主接续、接收端核对最新纠正和上下游、单一写入者、
  失败保留安全状态及验证后释放来源。历史状态 Hook 与当前原生接口已
  做源码/资料对照；交接只是整体职责的一个例证，不缩减 3.2 功能范围。
  这是验收与路线修正，尚未恢复自动交接执行链。
- 修复当前开发校验对可核验历史引用的误判：允许正文中指向正式仓库
  完整祖先提交及真实对象的简单 Markdown 链接，仅排除已核验片段。
  保留旧版默认扫描、周边正文及代码/配置检查；新增伪造目标、字面量、
  嵌套伪装与有界查询回归。未添加依赖或改变宿主包。
- 完善现有验收职责：用户按结果表达即可，Agent 自主选择适用操作并
  核验、回收；论证区分目标与手段、证据与未知、可行条件与授权。
  这些是待兑现的验收要求，不是新建按钮控制系统或功能成功凭证。

### 删减与替代边界

- 开发设计不再受旧版固定纯核心路径、运行时升级阶梯及脚本/参考文件
  形式禁令约束；必要职责、精确包身份和安全边界仍保留。
- 尚未因原生能力重叠就宣布某项功能可以删除，也未新增持久运行时、
  全量能力数据库或 SDK 依赖。没有值得比较的净收益机会时减少干预；
  缺口或可信改善机会才扩展，删减必须保留仍需的完整效果。
- 不从历史良好体验推断独立运行时必要性；Hook、脚本、原生控制及其它
  组合都按实际触发、执行、状态和故障接管效果判断。

### 当前反例与未完成项

- 普通入口下的实质增益尚未证明。用户校准为能感受到一定影响，并认可
  项目重塑前的自主交接与纠偏体验；不记录为零感知或运行时因果证明。
  若干旧包对照任务原生已足够，安装、调用或更多提示本身不证明价值。
- dev.3 调研/复用观察暴露预算内未交付；相同参考条件下，原生组产出
  代码但说明有误，插件组调用 Skill 后仍未产出。离线控制流检查通过
  不等于实际 SDK 续接成功，单例也不能判定因果或模型优劣。
- 默认与自定义环境、具体入口的实际效果、故障后的接管者、自动交接、
  资源回收及完整包生命周期仍需按声明验证。未知项不得由局部 PASS 覆盖。
- 主动发现未列出的设计盲区和跨功能断点，范围不限于已知问题清单；
  按结果风险选择验证，不靠无限盘点增加完成度。
- 历史引用修正不涵盖任意未链接叙述、JSON 文本或完整 Markdown 语义；
  不将这项维护工具修正当作用户功能、自动交接或增量价值证明。
- dev.3 普通入口复测仍有研究后部分交付。仅改发现描述的实验未触发
  Skill，也未减少调用；单次完成不足以证明改法有效，未合入候选包。
  三份脚本各通过 7 项离线分支检查，不是真实 SDK 调用。
- 无需用户选择内部操作的订单场景，两组均正确交付并保护原始文件；
  原生已足够，不为此新增机制。单例不证明普遍易用性或 Accord 增量收益。
- 后续诊断纠正两项实验干扰：进度流触发采集上限、工具列出但访问被拒。
  旧调用计数不能证明成功取回资料。修正有界采集和临时域名授权后，仍有
  参数/文案缺陷或缺少正常终态；保持未验收。未改宿主包、个人配置或全局权限。

### 兼容与发布

- 当前开发覆盖 Codex/ChatGPT 与 Claude 家族的入口评审，其他厂商适配
  暂缓。安装记录、相似引擎及共享配置都不是跨入口效果证明。
- Claude 仅沿现有授权的 CLI / 第三方提供商路线做过本轮行为观察；
  官方账户不可用的云端入口保留资料支持与未实测状态，不规避访问限制。
- 未修复宿主原生计划 UI；[可见计划](docs/operations/PLAN-v3.2.md) 是
  项目进度投影。未安装、启用或改写用户个人宿主配置。
- 发布仍须提交并推送完整精确候选，完成必要功能、价值、生命周期、
  独立评审和托管检查，再发布同一提交并核验公共后态及清理。

已发布版本见 [历史发布账本](docs/operations/HISTORY.md)。开发依据与失败
摘要见 [当前源](product/development.json)；它们不代替运行时效果证据。
