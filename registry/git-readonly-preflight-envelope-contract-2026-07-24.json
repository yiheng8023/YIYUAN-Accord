{
  "schema": 1,
  "id": "git-readonly-preflight-envelope-contract-2026-07-24",
  "date": "2026-07-24",
  "status": "verified-local-readonly-envelope-builder-no-host-approval-trial",
  "scenarioId": "GIT-READONLY-PREFLIGHT-ENVELOPE-01",
  "builder": "scripts/build_git_readonly_preflight_envelope.py",
  "tests": "tests/test_git_readonly_preflight_envelope.py",
  "reuses": "scripts/observe_git_snapshot.py",
  "doesNotReuse": "scripts/evaluate_git_topology_trial.py",
  "invariants": [
    "exactly two read-only observations",
    "no retry or recovery/topology write-command attempt",
    "run-bound sequence 1/2 event identities",
    "RFC3339 started/completed times with before completion no later than after start",
    "exact snapshot equality or blocked-concurrent-drift",
    "all dirty ownership and raw porcelain path evidence are recomputed and remain unknown",
    "all proof counters remain false and digest-bound",
    "freshness is only none or local-ref-only and binds facts.remoteClaim with no network refresh",
    "filesystem zero-write is not proved because Git inspection may refresh internal metadata"
  ],
  "claimBoundary": {
    "dirtyOwnershipProved": false,
    "nativeApprovalObserved": false,
    "creationSafetyProved": false,
    "remoteFreshnessProved": false,
    "filesystemZeroWriteProved": false,
    "recoveryProved": false,
    "crossHostParityProved": false
  },
  "nextGate": "A native approval or denial claim still requires a separately authorized bounded create-or-denial trial with exact before-and-after snapshots."
}
