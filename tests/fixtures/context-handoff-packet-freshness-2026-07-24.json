{
  "schema": 1,
  "id": "context-handoff-packet-freshness-2026-07-24",
  "status": "verified-local-packet-freshness-contract-no-thread-or-host-action",
  "cases": [
    {"id": "exact-local-packet", "expectedStatus": "packet-current-read-only-pre-dispatch", "expectedFailureCodes": []},
    {"id": "source-hash-drift", "expectedStatus": "blocked-stale-packet-regenerate-required", "expectedFailureCodes": ["blocked-source-hash-drift"]},
    {"id": "contract-digest-drift", "expectedStatus": "blocked-stale-packet-regenerate-required", "expectedFailureCodes": ["fail-contract-binding"]},
    {"id": "repository-truth-drift", "expectedStatus": "blocked-stale-packet-regenerate-required", "expectedFailureCodes": ["blocked-repository-truth-drift"]},
    {"id": "remote-overclaim", "expectedStatus": "blocked-stale-packet-regenerate-required", "expectedFailureCodes": ["fail-packet-remote-freshness-boundary", "blocked-repository-truth-drift"]},
    {"id": "authority-promotion", "expectedStatus": "blocked-stale-packet-regenerate-required", "expectedFailureCodes": ["hard-fail-authority-promotion"]},
    {"id": "prompt-tamper", "expectedStatus": "blocked-stale-packet-regenerate-required", "expectedFailureCodes": ["fail-public-prompt-binding"]}
  ],
  "claimBoundary": {
    "createsThread": false,
    "readsConversation": false,
    "invokesLoader": false,
    "changesConfiguration": false,
    "networkRefreshes": false,
    "provesReceiverBehavior": false,
    "provesAtomicSnapshot": false,
    "provesSourceSemanticFreshness": false
  }
}
